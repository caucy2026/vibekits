# M6 重复文件闭环验收记录

日期：2026-08-16  
版本：`1.2.0+3`  
Build ID：`v1.2.0+3_20260816_DUPLICATES`

## 完成范围

- 原生文件夹选择、递归开关和 0/1/10/100MB 最小文件阈值。
- 文件大小预筛；只对同大小候选流式计算完整 SHA-256。
- 扫描进度、取消、不可读位置计数和扫描期间零写入。
- 硬链接物理身份去重，避免把零收益操作列为重复副本。
- 按可释放空间排序的重复组、最新文件保留建议和默认不选择。
- 选择建议项、逐文件复核、二次确认、回收站删除和结果摘要。
- 删除前复核文件身份、大小、修改时间；扫描后发生变化的文件自动跳过。
- 匿名 JSON 报告不记录文件路径或内容；报告失败不遮蔽真实删除结果。

## 自动与实机证据

| 检查 | 结果 |
|---|---|
| 同大小不同内容不分为重复 | 通过 |
| 完整 SHA-256 重复分组 | 通过 |
| 非递归扫描不进入子目录 | 通过 |
| 协作取消且文件零修改 | 通过 |
| Widget 扫描结果、建议选择、确认、删除、报告闭环 | 通过 |
| 共用清理删除器成功/跳过/失败/匿名报告回归 | 通过 |
| Windows 生成副本真实移入回收站 | 通过 |
| 保留副本内容完整 | 通过 |
| `flutter analyze` | No issues found |
| 全量 `flutter test --reporter compact` | 154/154 通过 |

实机脚本只操作系统临时目录内当次生成的随机内容，指定副本进入回收站后删除剩余测试目录，未扫描或修改用户文件。
