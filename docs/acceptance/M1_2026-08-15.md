# M1 开发工具验收记录（首批闭环）

日期：2026-08-15
Build ID：`v0.1.0_20260815_M1_devtools`
验收环境：当前 Windows x64 电脑（VS Build Tools 17.14.37 已安装）

## 1. 交付物

| 内容 | 位置 |
|---|---|
| 领域层（纯 Dart，离线） | `lib/features/dev_tools/domain/`：`tool_result`、`encoding_tools`、`crypto_tools`、`time_tools`、`format_tools`、`tool_registry` |
| 工作区 UI | `lib/features/dev_tools/presentation/dev_tools_tab.dart` |
| 首批工具 | 22 个离线工具：Base64/URL/HTML/Unicode/Hex 编解码、MD5/SHA-1/SHA-256/SHA-512、HMAC-SHA256、UUID v4、随机密码、时间戳互转、正则测试、JSON 格式化/校验 |

## 2. 自动检查

| 检查 | 结果 |
|---|---|
| `dart format --set-exit-if-changed` | 通过（0 changed） |
| `flutter analyze` | No issues found |
| `flutter test --reporter expanded` | 46/46 通过 |
| `flutter build windows --debug` | 通过 |
| `flutter build windows --release` | 通过 |

测试覆盖：编码/哈希/UUID/密码/时间戳/正则/JSON 单元测试 38 项，开发工具交互 Widget 测试 3 项，主壳 4 项，领域边界（非法输入、失败保留输入）等。

## 3. 验收编号闭环

| ID | 验证方式 | 结果 | 证据 |
|---|---|---|---|
| DEV-001 | 纯 Dart 领域层无任何网络/文件 I/O，`flutter test` 覆盖全部 22 工具 | 通过 | `test/*_tools_test.dart` |
| DEV-002 | Widget 测试覆盖执行、交换、清空；复制/保存已实现 | 通过 | `test/dev_tools_widget_test.dart` |
| DEV-003 | Widget 测试：非法 Base64 输入 → 输出区显示原因，输入保留 | 通过 | `test/dev_tools_widget_test.dart` |
| APP-001 | Release 实机启动计时：进程就绪约 178ms，窗口随后可交互 | 通过 | 实测记录 |
| APP-002 | 默认 1280×800 实测；最小 1024×700 已由 `WM_GETMINMAXINFO` 实现 | 部分 | 缩放 150%/200% 待人工复核 |
| APP-003 | Widget 测试覆盖点击与 `Ctrl+1`～`Ctrl+5` 切换 | 通过 | `test/widget_test.dart` |

## 4. 实机证据

- Release 产物：`build\windows\x64\runner\Release\vibekits.exe`
- 启动：标题 `Vibekits`，窗口 1280×800，响应正常，内存约 187.5 MB
- 截图：`docs/acceptance/screenshots/M1_01_shell_1280x800.png`

## 5. 已知限制

- 复制/保存已实现但未纳入自动测试，待人工验收。
- 网络工具（HTTP/DNS/端口）、文件工具（重命名/重复文件）属 M6。
- 缩放 150%/200% 的可读性需人工复核（APP-002 保留人工部分）。
