# R9 端口转发开发检查点（2026-08-17）

版本：`1.9.0-dev.3+13`

## 本次完成

- 本地、远程和 SOCKS5 三种转发；同一远程工作台可管理多条规则。
- 本地/SOCKS5 固定回环监听；远程固定服务端 localhost，默认不向局域网暴露端口。
- 端口占用显示具体端口且不产生运行项；支持逐条停止和全部断开。
- SSH、监听 socket、通道桥接和数据泵完整运行在专用后台 Isolate，UI 只交换控制与状态消息。
- 关闭工作区会关闭全部转发并终止后台 Isolate，不创建系统 SSH 子进程。

## 自动证据

- `test/port_forward_service_test.dart`：参数、描述、安全边界和后台 Isolate 失败释放 3/3 通过。
- `test/remote_workspace_widget_test.dart` 定向用例：三类型、端口占用、逐条停止、全部断开通过。
- 相关领域、UI 与测试共 4 个文件定向 Analyze 无问题。

## 尚未关闭

当前没有受控 SSH 服务端，REM-104-A～F 的真实 HTTP/SOCKS 响应、远端转发、3 秒端口冲突、关闭后监听与后台 Isolate/进程快照仍为 `🟡 待实证`。

下一代码项：`REM-105` 系统远程桌面。
