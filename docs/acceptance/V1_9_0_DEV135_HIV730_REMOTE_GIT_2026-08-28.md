# v1.9.0-dev.135 Harness 远端 Git 按需取码验收

日期：2026-08-28

## 目标

让 Harness 阅读 `HIV730_ANDROID_PROJECT_GUIDE.md` 后，通过 Vibekits 自有 Git 工具理解 HiV730 Android 多仓结构，为后续依据死机日志按需取得最小源码仓库做准备。禁止 Shell、系统 Git、无参数 `repo sync`、整包源码下载和私钥读取。

## 工具补齐

- `vibekits.git.list_remote_refs`：使用随 APP 打包的 MinGit 执行有界只读 `ls-remote`。
- `vibekits.git.read_remote_file`：浅取明确 ref 到 APP 临时目录，只返回一个最大 2 MiB 的文本文件，随后删除临时对象。
- `vibekits.git.clone_minimal`：只克隆明确的单仓库、ref 和深度；目标目录必须不存在，失败自动清理本次创建的残留。
- 网络 Git 禁止隐藏凭据交互；URL 禁止明文密码，仓库内文件路径禁止绝对路径和 `..` 越界。

## Harness 真实调用

1. `system.capability_check`：`definedTools=168`、Windows 运行时 `executableTools=148`、`missingHandlers=[]`。
2. `system.describe_tool`：三个新接口均返回完整 Schema；远端 refs/文件为只读，浅克隆为写数据。
3. SSH `ssh://172.21.16.194:29420/HiV730/manifest.git` 首次调用明确返回 `Host key verification failed`；未自动信任未知指纹，未改写 SSH 配置。
4. 根据指南已明确提供的 Gerrit HTTP 基址，Harness 调用 `git.list_remote_refs` 访问 `http://172.21.16.194:8092/HiV730/manifest.git` 成功。
5. 返回 `refs/heads/master`，SHA 为 `7ed6ea64eb96ea379aacdcd3319eac38d2257e82`。
6. Harness 调用 `git.read_remote_file` 成功读取 `master:default.xml`，大小 9,430 字节。
7. Harness 解析出 103 个项目：hisi/vendor 56、AOSP 26、Newlink 21；未执行任何业务仓库克隆。

## 死机问题的后续闭环

先取得 tombstone、dmesg、Logcat、串口尾部或重启现场，按崩溃进程、库路径、内核模块或服务名在 manifest 中确定最小仓库。只有映射唯一且原因明确时，才调用 `git.clone_minimal` 浅克隆该单仓库；若调用链进入 framework、HAL 或内核，再逐项扩展，不一次下载整条依赖链。

## Harness 仓库架构分析结果

Harness 在 APP 会话“HiV730 仓库架构分析报告”中再次独立执行了指南读取、远端 refs 查询和 manifest 读取，并输出完整分析报告：

- 远端共有 24 个 refs；`HEAD` 与 `refs/heads/master` 均指向 `7ed6ea64eb96ea379aacdcd3319eac38d2257e82`。
- `default.xml` 没有 `include`、`submanifest` 或 `remove-project`，默认 remote 与 revision 分别为 `origin`、`master`。
- 103 个项目分为 AOSP 26、hisi/vendor 56、Newlink 21。
- Android 启动链被映射为 Bootloader → Linux Kernel → init/native service → ART/Zygote → system_server/framework → HAL → Newlink 应用与中间件。
- 关键源码范围包括 `vendor/huanglong/bootloader`、`vendor/open_source/u-boot`、`vendor/huanglong/linux`、`vendor/open_source/common-kernel-5.10`、`system`、`frameworks`、`art`、`bionic`、`vendor/huanglong/interfaces`、`vendor/huanglong/haldefault`、`vendor/huanglong/hardware`、`vendor/thirdparty/gpu`、`vendor/newlink/frameworks`、`vendor/newlink/middleware` 和各 `NL*` 应用仓库。

Harness 给出的最小排查映射：

| 故障 | 优先证据 | 首批最小源码范围 |
| --- | --- | --- |
| 死机/重启 | 串口 console、pstore/ramoops、last_kmsg、dmesg、boot reason | 平台/公共内核、板级配置、system、平台 modules |
| 卡死/ANR | ANR traces、main/events Logcat、Binder 栈、dumpsys activity | frameworks、ART、Newlink middleware/frameworks、aosp_ext |
| 黑屏 | SurfaceFlinger/Display/HWC Logcat、显示/GPU dmesg、dumpsys SurfaceFlinger | 厂商 hardware/interfaces/haldefault、GPU、modules、frameworks |
| system_server 崩溃 | crash Logcat、ANR、tombstone、dropbox、watchdog | frameworks、aosp_ext、Newlink frameworks/middleware |
| native tombstone | tombstone、debuggerd/backtrace、crash Logcat、dmesg | bionic、ART、system、AOSP/厂商 hardware |
| watchdog | Watchdog Logcat、ANR trace、system_server 线程与锁 | frameworks、aosp_ext、Newlink middleware |
| kernel panic | 串口、pstore/ramoops、last_kmsg、dmesg | 平台内核、common-kernel-5.10、uapi、mp_linux |
| GPU/显示 | GPU/DRM dmesg、HWC/EGL/SurfaceFlinger、GPU 驱动日志 | GPU、厂商 hardware/interfaces/haldefault/modules、frameworks |
| 串口异常 | boot/kernel 串口字节流、NLSerialService Logcat、tty/uart dmesg | NLSerialService、Newlink middleware、平台内核/uapi、板级与 sepolicy |

Harness 明确标记当前仍缺少真实设备现场日志、个人 Gerrit 业务仓库权限证据以及具体故障对应的本地单仓源码。因此本轮通过的是“理解仓库并制定按证据取码方案”，尚未宣称已经定位某一次实际死机根因。

## 自动验证

- Git 领域、Harness 桥接和能力目录定向测试：35/35 通过。
- 目标文件 Flutter Analyze：0 问题。
- Windows Release：`v1.9.0-dev.135+2135` 构建成功。
- Windows Bundle：MinGit `2.55.0.windows.3` 与 29 项运行时检查通过。
