# V1.9.0-dev.144 Windows 与 192.168.3.62 LMCP/2 验收（2026-09-01）

## 修复

- Windows 的 UDP 47831 发现 socket 改为绑定实际 RFC1918 网卡地址，再按接口加入 `239.255.42.99`。这避免多个同机 APP 都绑定 `0.0.0.0` 时争抢同一组播数据报。
- 远端 `tools/call` 身份校验同时接受唯一标准信封字段 `toolName` 和历史扩展字段 `tool`；顶层与 `structuredContent` 出现冲突值时仍返回 `response_identity_mismatch`，没有放松实例、工具或目录 revision 校验。

## 自动化结果

- Windows 共享端口、提供方先启动、主动 discover、周期 announce、goodbye：10/10 通过。
- LMCP 发现、HTTPS MCP、目录认证、容量调度、三层目录和 Harness 桥定向回归：78/78 通过。
- `flutter analyze --no-pub`：0 issue。
- Windows x64 Release 重新构建成功；29 项自包含运行时核验通过。
- 全项目测试共 648 项通过、12 项按环境跳过。与正在运行的 Release/WebView 并发时，性能套件和 Harness 集成曾出现 3 项资源竞争失败；关闭验收 APP 后隔离复测 5/5 通过，Harness 工作区首次/热响应为 1051/264 ms，最慢开发工具复测也低于门禁。

## 192.168.3.62 实机结果

- 网络：`192.168.3.62` Ping 正常，ADB `192.168.3.62:5555` 为 `device`。
- 实时发现：`com.newlink.kemiscrollbench:41B8C7FDF4` / `KEMI-BM@hua-41B8C7FDF4` / APP `2.4.0`。
- 端点：`https://192.168.3.62:9443/mcp`；目录 revision `8`；`catalogState=verified`；`callable=true`。
- 实时目录：9 个工具，包括 5 个 benchmark 工具和 4 个 LMCP 容量工具。
- 真实调用：通过 `vibekits.mcp.tool_call` 调用只读 `kemi.benchmark.device_status({})`，196 ms 收到符合标准的结构化响应；返回身份、工具名和 revision 均已通过客户端校验。
- 业务终态：对端返回 `AUTH_SCOPE_REQUIRED`，traceId `a372ca09-ef4e-4a2e-a625-404b130cd117`。这表示 62 尚未把当前 VibeKits 调用方加入持久授权范围；客户端不得绕过。对端用户在授权页允许该调用方及只读工具后，应原样重试并取得 `ok=true/final=true/state=succeeded` 才能把业务调用记为完全通过。
