# 2026-09-24 改动记录与云端备份

本记录覆盖本地 `main` 相对上次 GitHub `origin/main` 的 50 个源码与文档提交（原 HEAD `8fae603`），以及本次补入的 Windows dev232 验收记录。该范围包含当天并行任务的提交，不能全部归于同一人；具体作者、时间和文件以 Git 提交历史为准。

| 范围 | 改动及证据 | 状态边界 |
| --- | --- | --- |
| Mac 远端 Harness | `1d0a2aa` 改用官方 `session.prompt` 下发命令并修复历史回执；`f4e7093` 从官方历史判断完成状态、修复等待回执；修复 Mac 签名脚本误报，增加回归测试。`f976b55` 记录 dev239/2239 商城包的签名、公证、CDN 和设备 `4456560334` 实测。 | 发布包仅以 dev236 为基线叠加这两项修复；其他当天主线改动不在 Mac 商城包中。详见 `V1_9_0_DEV239_MACOS_MARKET_2026-09-24.md`。 |
| PAD 远程仿真与 ADB | 完善 USB-only adbd 恢复、状态报告、开机保持、前台中继与服务诊断；记录 PAD63 两日需求审计及真机证据。 | 具体已通过与未通过的条件以 PAD63 各验收文档为准，不将尚未闭环的远程 ADB 视为全面通过。 |
| PAD 本机编译组件 | 新增按需安装的专用 Builder 组件、Harness 调用桥、SDK/工具链来源与许可证记录、设备框架 classpath 转换、构建状态与 IPC 探针；针对 ANR 和图标子集构建卡顿加入修正与回归证据。 | `8fae603` 记录 PAD75 最小候选及设备证据；商城可分发性、全部真机流程和授权限制仍以相应验收文档判定。备份只含源码、构建配方和许可证，不含大体积工具链快照或 APK。 |
| PAD 代理与应用中心 | 增加 Android 代理组件、组件发现与调用逻辑及测试，修正应用中心和 Harness 相关入口。 | 不把尚未完成的端到端验收描述为已发布。 |
| 远程目录挂载文档 | `c01048a`、`c8cae10` 记录 Mac/Windows 远程目录挂载方法及 SFTP、SMB 的取舍。 | 挂载依赖现有仿真连接，不代表开机自动恢复。 |
| Windows 商城验收 | `0081a5e` 记录 dev232/2232 商城发布；本次补充 58 号测试机后续启动状态，以及构建目录只读映射和哈希核验。 | 仅更新既有验收记录；没有重新构建或发布 Windows 包。 |

备份目标：既有 GitHub 仓库 `caucy2026/vibekits` 的 `main`，普通 fast-forward push；完成标准为远端 `refs/heads/main` SHA 与本地 HEAD 一致。临时构建目录、`.codex-artifacts/`、`android/build/`、`dist/`、`kemi-build-cache/`、`700`、`700.pub`、安装包、运行日志和凭据均不纳入 Git。推送结果以实际远端 SHA 核验为准。
