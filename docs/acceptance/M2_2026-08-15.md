# M2 文档阅读验收记录（首片）

日期：2026-08-15
Build ID：`v0.1.0_20260815_M2_documents`
验收环境：当前 Windows x64 电脑

## 1. 交付物

| 内容 | 位置 |
|---|---|
| 编码探测/解码（UTF-8 / UTF-16 LE/BE / GBK） | `lib/features/documents/domain/text_encoding.dart` |
| 大文本行索引 | `lib/features/documents/domain/line_index.dart` |
| Hex 视图与字节解释、Magic 识别 | `lib/features/documents/domain/hex_view.dart` |
| 日志等级分类 | `lib/features/documents/domain/log_level.dart` |
| 格式路由 | `lib/features/documents/domain/format_router.dart` |
| 阅读器 UI（打开/最近/文本/Markdown/Hex/信息面板/查找/编码切换） | `lib/features/documents/presentation/documents_tab.dart` |

## 2. 自动检查

| 检查 | 结果 |
|---|---|
| `dart format --set-exit-if-changed` | 通过 |
| `flutter analyze` | No issues found |
| `flutter test --reporter expanded` | 66/66 通过 |
| `flutter build windows --release` | 通过 |

## 3. 验收编号闭环

| ID | 结果 | 说明 |
|---|---|---|
| DOC-001 | 部分 | 文件选择（系统对话框）已实现；拖放与“打开方式”待后续 |
| DOC-003 | 通过 | 信息面板显示类型/路径/大小/编码/行数/Magic |
| DOC-004 | 通过 | 查找、上/下一个、匹配计数、高亮 |
| DOC-006 | 通过 | 打开失败、文件过大、编码解码失败均受控提示 |
| DOC-101 | 部分 | UTF-8 / UTF-16 LE/BE / GBK；GB18030 与 Big5 待原生层 |
| DOC-102 | 部分 | `LineIndex` 偏移索引已实现并有测试；大文件流式读取待接入 |
| DOC-103 | 通过 | `.log` 按 ERROR/WARN/DEBUG 着色 + 换行开关 |
| DOC-104 | 部分 | Markdown 预览/源码、GFM 表格、代码块；本地图片待后续 |
| DOC-301 | 通过 | 偏移/Hex/ASCII 三栏、8/16/32 字节切换 |
| DOC-303 | 部分 | 大小端整数/浮点解释已实现（领域层）；UI 交互待接入 |
| DOC-305 | 通过 | Magic Number 识别并在信息面板提示，不自动跳解析器 |

## 4. 已知限制

- 大文件（>64MB 文本 / >256MB Hex）暂提示超限，流式读取后续接入（DOC-102/304）。
- 拖放、Windows“打开方式”文件关联待后续。
- GB18030、Big5 待原生 C 层接入。
- CSV/JSON/XML/HTML/EPUB/SVG 属 M3。
