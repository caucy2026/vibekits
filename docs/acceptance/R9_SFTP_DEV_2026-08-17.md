# R9 SFTP 开发检查点（2026-08-17）

版本：`1.9.0-dev.2+12`

## 本次完成

- SFTP 复用远程会话资料、系统凭据和 SSH 主机指纹确认。
- 本地/远端双栏目录、选择传输、双向拖放和刷新。
- 同名冲突固定为覆盖、重命名、跳过、取消；默认焦点为跳过。
- 上传/下载先写临时文件，再替换目标；取消或失败清理临时文件。
- 字节进度、主动取消、失败状态和重试入口。

## 自动证据

- `flutter test test/sftp_browser_widget_test.dart --reporter compact`：3/3 通过。
- 定向 `flutter analyze`：远程会话、SFTP 服务、双栏 UI、远程工作台及相关测试共 6 个文件无问题。
- 覆盖场景：双栏列出与双向传输、冲突四动作和默认焦点、进行中取消。

## 尚未关闭

当前机器没有已配置的受控 SSH/SFTP 测试服务，因此 REM-103-A～F 的远端 SHA-256、1GiB 取消、50% 断网、重试和服务端临时文件搜索仍为 `🟡 待实证`，不能写成最终验收通过。

下一代码项：`REM-104` 端口转发管理。
