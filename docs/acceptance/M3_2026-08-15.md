# M3 结构化阅读验收记录（首片）

日期：2026-08-15
Build ID：`v0.1.0_20260815_M3_structured`

## 1. 交付物

| 内容 | 位置 |
|---|---|
| JSON 树构建（深度上限 128） | `domain/json_tree.dart` |
| XML 树构建（禁用 DTD/外部实体，深度上限 128） | `domain/xml_tree.dart` |
| CSV/TSV 解析（引号/内嵌换行） | `domain/csv_table.dart` |
| 结构化树/表格/源码回退 UI | `presentation/documents_tab.dart` |

## 2. 自动检查

| 检查 | 结果 |
|---|---|
| `flutter analyze` | No issues found |
| `flutter test --reporter expanded` | 74/74 通过 |
| `flutter build windows --release` | 通过 |

## 3. 验收编号闭环

| ID | 结果 | 说明 |
|---|---|---|
| DOC-105 | 通过 | CSV/TSV 表格、引号/转义/内嵌换行、固定表头 |
| DOC-106 | 部分 | JSON/XML 树 + 折叠；解析失败回退源码并提示 |

## 4. 已知限制

- HTML/EPUB/SVG（DOC-201～204）属 M3 后续，依赖安全 Web 渲染器。
- 树视图无搜索、无“全部折叠/展开”按钮，待补。
- CSV 列宽固定，拖拽调宽待补。
