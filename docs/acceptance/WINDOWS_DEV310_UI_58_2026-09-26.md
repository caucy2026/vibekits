# Windows dev310 Harness 界面修复与 58 真机记录（2026-09-26）

本记录只归档本次 Windows Harness 输入框右键和模型选择修复；不包含同时进行的 PAD、Mac、集群或商城工作，也不代表已签名、上架。

## 改动范围

- `assets/harness/codex_conversation_ux.js`：Windows WebView2 原生右键菜单被插件关闭时，为可编辑输入区提供复制、粘贴、全选菜单；Mac 行为不变。
- `lib/features/local_models/presentation/official_harness_workspace.dart`：Windows 收到复制、粘贴菜单事件后调用现有编辑操作。
- `tool/patch_harness_windows_model_menu.mjs`、`tool/prepare_harness_runtime.ps1`：准备 DSH `0.1.7-rc.2` Windows 运行时时，修补模型菜单在 `relatedTarget == null` 时被错误关闭的问题；补丁目标不匹配时直接失败，避免生成未修复的包。

## 58 真机证据

- 设备：VibeKits ID `4567540178`，Windows 主机 `LAPTOP-LUOPP1CH`。在 D 盘独立目录编译 Release，生成的 `vibekits.exe` 版本为 `1.9.0-dev.310+2310`。
- 2026-09-26 在该机交互会话启动 `D:\KEMI-Test\source\vibekits-dev310-58-20260926\build\windows\x64\runner\Release\vibekits.exe`；程序窗口、Harness、WebView2、DSH 和 relay 均启动，未观察到启动闪退。
- 在 dev310 的 Harness 输入框右键，实际出现“复制、粘贴、全选”菜单。
- 模型菜单实际列出 `DeepSeek-V4.1-Flash` 和 `DeepSeek-V4-Pro`；切换到 Pro 后又恢复测试前的 Flash。选择不再只能固定在一个模型。
- 测试时 58 上另有正在运行的清理磁盘会话，未停止或修改该任务。测试结束关闭远程桌面窗口，保留 dev310 供现场查看。

## 边界与备份

- 以上是开发版真机界面验收；尚未签名、打安装包或发布商城。SSH 授权在部分 Windows 机器等待很久是另一项待诊断问题，本记录不宣称已修复。
- 云端备份只包含这四处相关源码改动和本记录。当前本地 `main` 与远端 `main` 分叉，工作树还有其他同事未提交改动；为避免混入或改写其工作，本次使用独立备份分支，不改动远端 `main`。构建缓存、运行日志、截图、私钥和安装包不上传 Git。
