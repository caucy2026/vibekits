# v1.9.0-dev.47 本机清理联调与工具可见性验收

日期：2026-08-20

## 真实机器结论

- C 盘：80.00 GiB，总已用 77.71 GiB，剩余 2.29 GiB（2.9%）。
- 修复前外部规则合并因固定长度列表抛 `Unsupported operation`；修复后规则与内置目标正常合并。
- 快速路径 44 秒：115 个范围、20061 个候选、安全 3.121 GiB、需复核 1.014 GiB、合计 4.134 GiB、4 个不可读位置。
- 全盘旧 `.log` 默认扫描超过 10 分钟，不满足效率要求；v10 改为默认关闭，仅在深度清理显式启用。
- Windows 组件存储实际 7.04 GB、可回收包 1 个，DISM 明确不建议组件存储清理，因此没有手工删除 WinSxS。
- 只读补充审计找到约 9 GiB低风险/可再生成缓存；达到 10 GiB需要额外永久删除约 1 GiB企业微信可重新下载小程序运行包。不可恢复操作审批未通过，本报告不把候选容量写成已释放容量。

## 界面闭环

- 左侧工具列表不再放置固定 Harness 记录按钮，列表获得完整垂直空间。
- 记录按钮位于当前工具右侧顶部，键保持 `current-tool-harness-activity`，原日志行为不变。
- 工具列表直接显示“Clash Verge / 虚拟机”；进入后继续使用 Clash Verge、轻量虚拟机两个融合 Tab。

## 自动验证

- 开发工具、Clash/虚拟机 Harness 合同、后台清理、Windows 规则和清理界面定向回归：41/41 通过。
- 新增外部规则后台合并回归，防止固定长度列表问题再次出现。
- `flutter analyze`：0 问题；Windows Release 构建成功，172.0 秒。
- 永久清理未执行；只有用户明确批准具体目录和清空回收站后才能记录真实释放量。
