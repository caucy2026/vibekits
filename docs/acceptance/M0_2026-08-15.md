# M0 环境与骨架验收记录

日期：2026-08-15
Build ID：`v0.1.0_20260815_M0_skeleton`
验收环境：当前 Windows x64 电脑

## 1. 环境基线

| 项目 | 值 |
|---|---|
| Flutter | 3.47.0 stable（D:\tools\flutter） |
| Dart | 3.13.0 |
| Windows | Windows 10（10.0.19045.6466），22H2，x64，locale zh-CN |
| Visual Studio / MSVC | **Build Tools 2022 17.14.37，已安装到 D:\VSBuildTools** |
| MSVC | 14.44.35207（D:\VSBuildTools\VC\Tools\MSVC） |
| Windows SDK | C:\Program Files (x86)\Windows Kits\10（VS 安装器固定位置） |
| Android cmdline-tools | 未安装（不影响 Windows 首版） |
| Chrome | 可用 |
| 已连接设备 | 4 |

`flutter doctor` 关键结论：

- `[√] Windows Version`
- `[√] Chrome`
- `[√] Visual Studio - develop Windows apps`（Build Tools 2022 17.14.37）
- `[!] Flutter`（未加入 PATH，本工程通过全路径调用）
- `[X] Android toolchain`（cmdline-tools 缺失，Windows 首版不受影响）

## 2. M0 交付物

| 任务 | 结果 |
|---|---|
| Flutter Windows 工程 | 已创建（`flutter create --project-name vibekits --org com.vibekits --platforms windows`） |
| 主题 | `lib/app/app_theme.dart`，颜色与 docs/01 一致 |
| 主窗口 | `lib/app/main_shell.dart`：顶栏 56、Tab 栏 68、内容区、状态栏 28 |
| 五 Tab | `lib/features/*/presentation/*_tab.dart`，空状态占位，不伪装成功 |
| 快捷键 | `Ctrl+1`～`Ctrl+5`、`Ctrl+,`（`lib/app/app_shortcuts.dart`） |
| 空状态组件 | `lib/core/widgets/empty_state.dart` |

## 3. 自动检查

| 检查 | 命令 | 结果 |
|---|---|---|
| 格式化 | `dart format --output=none --set-exit-if-changed lib test` | 通过（0 changed） |
| 静态分析 | `flutter analyze` | 通过（No issues found） |
| 单元/Widget 测试 | `flutter test --reporter expanded` | 4/4 通过 |

测试覆盖：五个 Tab 展示、点击切换、`Ctrl+数字` 切换、`Ctrl+,` 打开设置。

## 4. Windows 构建

阻塞已解除（VS Build Tools 安装到 D:\VSBuildTools）。构建结果：

| 构建 | 结果 |
|---|---|
| `flutter build windows --debug` | 通过 → `build\windows\x64\runner\Debug\vibekits.exe` |
| `flutter build windows --release` | 通过 → `build\windows\x64\runner\Release\vibekits.exe` |

Release 产物实机启动验证：

- 冷启动：进程就绪约 178ms，窗口随后出现并响应正常；
- 窗口标题：`Vibekits`；
- 窗口尺寸：1280×800（GetWindowRect 实测）；
- 最小尺寸：代码已实现 1024×700（`WM_GETMINMAXINFO`）；
- 内存：约 187.5 MB（WorkingSet）。

## 5. 后续动作

1. ~~安装 VS 2022 C++ 桌面工作负载~~ 已完成。
2. ~~重新执行 flutter doctor、Debug/Release 构建~~ 已通过。
3. ~~本机启动检查窗口~~ 已启动验证 1280×800。
4. 进入 M1 开发工具（`DEV-001`～`DEV-003`）。
