# 9 月 23–24 日 PAD 需求逐项审计

本表以用户最近两天提出的 PAD 需求为准。`通过`指有对应真机或自动化证据；`部分`和`未验`均不能算整体验收通过。当前 PAD63 安装 `1.9.0-dev.252`（2252），设备 ID `6795854383`。历史测试的旧版本与当前版本分开标记。

| ID | 用户要求 | 对照与本轮复验 | 判定 |
|---|---|---|---|
| P01 | PAD Harness 无 Key 才提示；读取中显示“系统检测中”；401/403 分开 | dev228 实机记录；本轮 `deepseek_harness_test`、`deepseek_model_discovery_test` 通过，未用真实错误 Key 重放 | 部分 |
| P02 | DeepSeek 模型名称与 Mac/官方对应，不伪造名称 | dev228 保留兼容别名并显示真实模型 ID；未验证当前 Mac 实时模型目录与 PAD 完全相同 | 部分 |
| P03 | 推理过程与原版 Mac 一样，有实时过程、进度、结果，只有一个主转圈 | dev227 的工具步骤/结果与单主动画实测；实际模型会话 `reasoningTrace=0`，不能声称同 Mac 推理文本一致；取消和切会话并发未复验 | 部分 |
| P04 | 长响应不能被 PAD 自设 8 MiB 总量上限中断 | dev228 移除累计上限，超过 64 MiB 假 SSE 回归通过；本轮相关测试通过；未用真实超长模型重放 | 部分 |
| P05 | Harness CPU/内存降低、长任务不卡死、八核利用合理 | 历史 36 秒任务完成；本轮 dev251 真正下达只读长任务，状态从 running/cursor 2 到 completed/cursor 39，运行中单点主进程 CPU 10.3%、PSS 381192 KB，服务 CPU 13.7%；缺连续峰值、取消/并发/ANR 压测及八核利用证据 | 部分 |
| P06 | PAD Harness 自己新建会话、设计图标、编译安装原生俄罗斯方块 | dev225 实机安装运行原生俄罗斯方块，但 Gradle APK 是控制端根据 PAD 源码代编；不满足“PAD Harness 独立编译” | 未通过 |
| P07 | PAD Harness 自己重设计打地鼠，锤子击中头部、屏幕碎裂后恢复、计分重玩 | dev233 PAD Harness 真实修改源码/图标并调用 PAD 本机构建，双屏运行和重玩实测；本轮 PAD63 第二屏真实连续点击，分数从 0 到 20，截图同时捕获锤子、地鼠头部裂纹与 +10；随后超时画面无残留裂纹 | 通过 |
| P08 | Harness 会话重启仍记得打地鼠开发记录，删除会话后重启不复活 | dev232 修复持久路径；dev233 空测试会话删除后冷启不复活，原会话 10 条消息保留；旧版已经遗失的聊天原文不能恢复 | 通过（新记录） |
| P09 | Harness 在一屏，编译 APK 在另一屏调试；输入法默认另一屏且不影响视频 | 本轮 dev251 第一屏 `SingleScreenActivity`、第二屏 `WhacActivity` 同时为 resumed；输入法切屏与另一屏视频连续播放未有正向帧证据 | 部分 |
| P10 | PAD 仿真开启须密码 2580，错误不能打开 | dev237 真机与本轮仿真状态机测试；本轮不主动关闭当前启用状态重试错误密码 | 部分 |
| P11 | 代理在 Android 真能工作，订阅可由手机扫码输入 | dev243 PAD75 真实 HTTPS CONNECT 200、关闭恢复系统代理、手机扫码 URL 自动回填；PAD63 仅验到相机权限请求，没有 PAD63 识码成功证据 | 部分 |
| P12 | 默认 APK 尽量小，大组件在商城按需下载 | 当前签名 PAD APK 约 86.8 MB；代理核心按需组件已有实机功能记录，但“大组件均按商城下载”及商城安装链未完成 | 未通过 |
| P13 | 默认 APK 就能跑 Harness，并能生成原生 APK，不依赖临时桥 | Harness 可启动；打地鼠设备构建仍依赖测试时安装的 Termux/loopback 桥，非默认内置能力。本轮 PAD63 `com.termux` 虽已安装，18473 构建桥并未监听，说明重启后不能默认使用 | 未通过 |
| P14 | 仿真打开后跨网按设备 ID 远程 ADB；即使本地 5555 原来关了也可恢复 | dev250 服务独立时关 5555 后约 20 秒恢复；dev251 重启后本轮 ID 连接 `adbReady=true`，远程 shell 返回 PAD63 型号、5555、adbd running | 通过（已有组件、已开启状态） |
| P15 | 设备初装仿真关闭、ADB 关闭、无辅助组件时，只打开仿真就能跨网 ADB | 还没有在全新 PAD 的完整初装状态做真机闭环；不能以已开启状态的恢复试验代替 | 未验 |
| P16 | 仿真开启后低占用常驻，主界面退出/被回收仍能远程 ADB | dev248/249/250 实机验证主进程不存在仍可 ID 重连；dev251 重启后曾仅服务进程在线且远程 shell 有效；本轮服务 PSS 约 32 MB | 通过（ADB 最小能力） |
| P17 | 用户开启后重启继续开启，只有主动关闭才停止；关闭后彻底不占资源 | dev251 真机重启后未开 UI 即 ID 重连通过；主动关闭及停服零资源未在 PAD63 实测，因为会切断唯一远程通道 | 部分 |
| P18 | ADB 断开时不能完全失明，SSH 可作备用控制 | dev252 后台单独运行时保留一个状态工具，并按真实 5555 探测返回 `adbReady`；PAD63 正常态与恢复后真机通过。没有 SSH，辅助组件被禁用时无条件恢复未实现；`adbReady=false` 瞬态未捕获 | 部分 |
| P19 | 不影响 Mac/Windows/Linux | 本轮新增服务、BootReceiver、最小端点与 ADB 监测都在 Android Kotlin；`pubspec.yaml` 为共享版本号。未对桌面各平台重新运行回归 | 部分 |
| P20 | 正式签名并在 KEMI 商城可更新、安装 | dev252 已正式证书签名并装在 PAD63；本候选尚无商城开发者记录、CDN 字节核对或商场内升级证据 | 未通过 |

## 2026-09-24 本轮新增复验

- `flutter test --no-pub` 七个相关测试文件共 **83 项通过**：仿真、Key/推理、模型、扫码输入接收器、代理及商城客户端。
- PAD63 本轮设备 ID 连接成功：`connected=true`、`mcpReady=true`、`adbReady=true`、`toolCount=1`；实际远程 ADB 读取 `KEMI Vibe Pads S1`、5555、adbd running、versionCode 2251。
- PAD63 已安装原生打地鼠、俄罗斯方块和系统 UID 1000 的 ADB 辅助组件 v5；本轮未打开游戏或重复动态图像测试。系统全局 HTTP 代理值为 `null`。
- 本轮单点 `top`：主进程与仿真服务均 0% CPU；服务 `dumpsys meminfo` PSS 32092 KB。单点采样不能证明长任务无峰值。
- 打地鼠第二屏视觉证据：[锤子、裂纹与得分](evidence/PAD63_WHAC_HAMMER_CRACK_DEV251_2026-09-24.png)。屏幕左上有设备自带性能浮窗，但目标游戏区域无遮挡。
- PAD63 UI 打开后按同一 ID 重连，完整远程目录从后台最小模式的 1 个工具恢复到 214 个，`adbReady=true`。真实 Harness 只读长任务完成于原会话 `session-1790161281297150`（cursor 39）；会话最终文本确有 12 个测试场景。该命令落在原活动会话，不能拿它证明“新会话”功能。
- 签名 PAD APK SHA-256 `d5873e3732a74e1b55d76a4fb2d9c6c397bd7a4f649fddd8fb99e2f9d2cfd0bc`。包内最大文件为 `librustdesk.so` 25.4 MiB、`libonnxruntime.so` 21.2 MiB、`libapp.so` 16.2 MiB、`libflutter.so` 10.8 MiB、`libsherpa-onnx-c-api.so` 4.3 MiB；OCR/VAD 模型本体已拆出，但 ONNX 运行库仍占主包，未达到“大组件均按商城使用时下载”。
- 后续 dev252 已覆盖安装并在 PAD63 重启复验后台状态；见 [dev252 状态记录](PAD63_ADB_STATUS_DEV252_2026-09-24.md)。上行 dev251 哈希保留作历史证据，不能代替当前候选哈希。

## 总验收结论

编译组件的分包路线、体积实测、Android 执行/签名难点和阶段验收见 [PAD 编译组件研发路径](../PAD_ANDROID_BUILDER_COMPONENT_ROADMAP.md)。宿主商城识别与安装白名单已有代码和单测；这不改变 P06/P12/P13 的未通过状态。

2026-09-24 后续增量：PAD75 已通过独立组件同签名 Binder 两轮真实本机编译测试，首次 89.02 秒、客户端重启后 10.7 秒，临时测试包已清理。新候选工具链已移除随包的 Android SDK `android.jar`，改从设备系统框架生成编译类路径。此证据解决了“专用组件能否在 PAD 本机编译”的一个子问题；宿主 Harness 新会话、商城实际下载/安装、用户游戏源码与第二屏运行、不同设备和断网/重启仍缺正向验收，P06/P12/P13/P20 的总判定保持未通过。

前一重建候选 `1.0.2+3`（SHA-256 `aa4c98a16e3cf481c4ed4da2854ea65c74b76638a9ea26802828f7688bbfde76`）已在 PAD75 再做同样双轮，89.37 秒与 10.73 秒均完成，APK 包名、字节、SHA-256 一致且无新 ANR；测试后原 dev.245 宿主未改、临时组件已卸载。该结果仍只覆盖独立组件，不代替 PAD Harness 用户会话与 KEMI 商城安装链验收。

当前更新候选为 `1.0.3+4`，包体 67,333,295 字节、SHA-256 `b26c8f2bf7d51c42ee275b4c81f9556f22f954824c33b85ad0c61a7afc6e7216`；11 个最小转换依赖下，PAD75 首次和再次编译分别 86.76 秒、10.92 秒，通过同一双轮负向 ANR 门禁，测试包卸载后无残留进程。此前 1.0.2 的结果保留历史，不代替当前候选精确字节；P06/P12/P13/P20 仍需宿主与商城闭环验收。

PAD75 后续安装同签名宿主 `1.9.0-dev.257+2257` 测试候选（SHA-256 `b74027e548b6d7d9368d8c35cee4f73fe9782016c50c633078b1eb5dea0f0aca`）和 1.0.3+4 编译组件，实际从宿主原生桥经 Binder 完成本机编译、包名/大小/SHA-256 校验以及安装缓存准备；第二轮同时证明工作区外路径被拒绝。测试产物为 8,535 字节原生 APK，SHA-256 `bc4800b0756fc8c33cb07af3be421802d17d6578ffd75e0ecea043391418387d`。设备级测试包已卸载，组件活动服务已停止，Android 留有可回收的缓存空进程；空闲 3 次 CPU 采样 0%，`dumpsys` 报告该缓存进程 PSS 约 7 MB。主 App 再启动正常；测试入口已纳入源码。PAD75 页面要求设置模型 Key，故尚无真实 Harness 模型会话证明；商城无组件条目，不能证明自动下载/安装。P06/P12/P13/P20 仍按未通过保留。

PAD75 进一步使用 PAD63 Harness 已生成的打地鼠源码和图标作为仪器化测试资源，从新版宿主原生桥调用专用组件**在 PAD 本机**编译。精确产物 `com.vibekits.whacdemo` 版本码 1、25,053 字节、SHA-256 `11aaeebf4d6ddc61cf67909933b5a3a75165b6e0e5baabc6fdafcd5d7ce9d156`；覆盖安装旧测试 APK 成功，在 display 2 启动并接受触摸，display 0 的 VibeKits 同时处于 resumed，未发现新 ANR/FATAL。游戏仍留在 PAD75 供验收；临时测试 APK 和导出缓存已清理。此证据提升了 P06/P09/P13 的组件构建与双屏子项，但本轮不是模型自己在会话里发命令，安装由控制端 ADB 完成，商城条目尚无，输入法与视频连续性未测，故这些总项仍不能改判通过。

PAD75 随后强制停止宿主与组件进程，未清除数据、未断开 ADB；组件仍为版本码 4，重启调用后同一原生游戏再次在 PAD 本机编译出相同字节数和 SHA-256。此项证明进程恢复与工具链缓存，不替代整机重启/断网。公开 Android 商城查询 `VibeKits PAD` 返回 `status=200,total=0`，商城自动安装链继续 BLOCK。

PAD75 还完成旧编译组件 `1.0.2+3` 的干净安装和主 App 调用，再以同签名 `adb install -r` 覆盖升级至 `1.0.3+4`（版本码 3→4，组件数据保留）。升级前后同一打地鼠源码均在 PAD 本机编译成功、APK 均为 25,053 字节、SHA-256 均为 `ebb9425edaa5c0e44d76d24d3f8c22ffd2ea7688671b93bf37fc7f0aae5970ba`；无新增组件 ANR/FATAL。设备最终保持新组件，临时测试包已卸载。这证明直接 Android 升级兼容；未验证商城下载和系统安装确认，P12/P20 仍未通过。

PAD75 又以正式同签名候选 `1.0.4+5`（67,392,855 字节，SHA-256 `6ca9febbac89b4f47877a4487b2d8fe2fb0a7b2ecacdd013795e9175dd41352c`）保留数据覆盖升级，版本码 4→5。其工具链把来源未闭环的两份运行库替换为与固定 Termux 包索引及包文件哈希一致的 `libc++ 30`、`zlib 1.3.2` 成员。宿主原生桥在 PAD 本机连续两轮编译打地鼠 APK，包名、25,053 字节和 SHA-256 `ebb9425edaa5c0e44d76d24d3f8c22ffd2ea7688671b93bf37fc7f0aae5970ba` 均一致，测试结果 `passed`。临时测试 APK 已卸载；随后 PAD75 的 ADB 地址变为 `No route to host`，故后续在线状态与断网/整机重启尚未验收。该结果不改变 P06/P12/P13/P20 的总项判定，也不等于商城下载安装验证。

**BLOCK。** P06、P12、P13、P20 尚未实现或交付；P15 未做首装冷态闭环；P03、P09、P17 等仍缺用户要求的实机正向证据。不能把当前 PAD63 远程 ADB 子目标已通过写成“两天需求全部解决”。
