# VibeKits 1.9.0-dev.158 CLI/MCP 验收报告

## 1. 验收对象

- 版本：`1.9.0-dev.158+2158`
- 日期：2026-09-06（Asia/Shanghai）
- 当前平台：Windows x64
- 工程、测试临时目录和构建产物：`D:\vibecode\vibekits`
- 设计基线：`docs/60_GITHUB_CLI_HARNESS_MCP_API.md`、`docs/61_UNIFIED_AGENT_CLI_HARNESS_MCP_STANDARD.md`

本轮验收覆盖 GitHub CLI 自包含运行时、GitHub CLI 三个 Harness/MCP 接口，以及七种智能体 CLI 的目录、检查、同步执行、长任务启动、查询和取消接口。

## 2. 实现核对

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| GitHub CLI 自包含 | 通过 | Debug 包内 `gh 2.100.0`，准备阶段固定来源与 SHA-256 |
| GitHub CLI MCP | 通过 | inspect/auth_status/execute 均有完整 JSON Schema、风险级别与测试 |
| 智能体 CLI 目录 | 通过 | codex/claude/copilot/cursor/gemini/aider/opencode 七个稳定 providerId |
| 同步执行 | 通过 | argv 直接执行，禁止 shell、密钥参数，返回真实 exitCode/stdout/stderr |
| 长任务 | 通过 | start/status/cancel、超时、终态、幂等取消及伪造 taskId 拒绝均有测试 |
| MCP 自动导出 | 通过 | 六个智能体 CLI 工具进入 Harness 能力目录与实时 `tools/list` |
| 用户配置隔离 | 通过 | 集成测试使用 D 盘独立 Harness Home，正式 APP 默认路径不变 |

本机真实扫描时只有 Codex CLI 可用；Claude Code、Copilot CLI、Cursor Agent、Gemini CLI、Aider、OpenCode 均明确报告 `available=false`。这是动态目录的预期行为，不会阻止 APP 启动，也不会用其他提供方冒充。

## 3. 自动化结果

| 门禁 | 命令/范围 | 结果 |
| --- | --- | --- |
| 静态分析 | `flutter analyze --no-pub` | 通过，0 issue |
| CLI/MCP 定向回归 | Agent CLI、GitHub CLI、能力目录 | 通过 |
| 产品关键回归 | ADB、清理、点击性能、Harness 真实集成 | 通过 |
| 全项目顺序回归 | `flutter test --no-pub --concurrency=1` | 684 通过，14 跳过，0 失败 |
| 点击性能 | Harness 一级入口首次响应 | 1463 ms；门槛 2500 ms |
| Windows 构建 | `flutter build windows --debug --no-pub` | 通过 |
| 自包含包校验 | `verify_windows_bundle.ps1 -Configuration Debug` | 通过：Git 2.55.0.windows.3、gh 2.100.0、33 项运行时 |
| 补丁卫生 | `git diff --check` | 通过 |

14 个跳过项均带显式环境门禁，涉及真实 ADB 设备、真实 LMCP 节点、生产联网资源、真实代理配置或 Release 专用路径；没有把跳过计为通过。

并发组合回归曾使性能用例与其他编译/集成测试争抢 CPU，Harness 首次点击为 2741 ms 并超过 2500 ms。无并发隔离复测为 2061 ms，最终单并发全量复测为 1463 ms，因此判定为测试负载干扰，不修改产品阈值掩盖问题。

## 4. 构建与运行时

Windows Debug 产物：

`D:\vibecode\vibekits\build\windows\x64\runner\Debug\vibekits.exe`

验证器确认应用版本、内置 Git、固定 GitHub CLI 和全部 33 项必需运行时均存在。Windows Release 目录中的旧 VibeKits 进程 PID 36960 正在运行并锁定可执行文件；本轮未强制结束用户正在运行的 APP，也未覆盖该 Release 候选。

## 5. 尚需对应环境验收

- macOS：Universal x86_64/arm64 构建、Developer ID 签名、公证、staple 和真实 CLI 扫描。
- Linux：amd64/arm64 构建及对应 CLI 扫描。
- 局域网：在已配对 LMCP/2 节点上读取新增目录，并分别验证只读调用、控制审批拒绝与批准路径。
- 各第三方 CLI：只有在目标机已按官方方式安装和登录后，才能执行各自真实非交互业务命令；VibeKits 不代存第三方密钥。

## 6. 验收结论

当前 Windows 开发机范围内，设计、实现、自动化、真实 Harness 集成、Debug 构建和自包含运行时门禁全部通过。跨平台签名发布和局域网真机项目保留为环境验收项，不宣称已经完成。
