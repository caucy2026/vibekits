# R9 系统远程桌面开发检查点（2026-08-17）

版本：`1.9.0-dev.4+14`

## 本次完成

- 远程工作台新增“桌面”模式并复用最近/收藏会话记录。
- Windows 使用 `mstsc.exe /v:host:port`；macOS 使用 `/usr/bin/open vnc://host:port`。
- 启动参数为独立数组且不经过 shell；应用不显示、读取或传递远程桌面密码。
- 桌面记录只保存名称、模式、主机和端口；IPv6 正确包裹，非法主机/端口拒绝。
- 系统客户端缺失时给出 Windows 可选功能或 macOS 屏幕共享的明确提示。

## 自动与本机证据

- 系统参数、错误映射和记录测试 9/9 通过。
- 桌面模式脱敏保存与一次启动 Widget 1/1 通过。
- 相关 7 个文件定向 Analyze 无问题。
- Windows 本机实查：`C:\Windows\system32\mstsc.exe`，版本 `10.0.19041.5965`。

## 尚未关闭

没有用户授权的真实 RDP 目标，未启动外部窗口；当前也不是 macOS。因此 REM-105-A～C 的真实进程参数、目标连接、macOS 系统屏幕共享和错误截图仍为 `🟡 待实证`。

下一代码项：`ADB-106`。
