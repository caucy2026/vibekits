# v1.9.0-dev.48 APP 10 GiB 清理链路验收

## 验收目标

以当前 Windows 真实机器为样本，严格通过 Vibekits 与界面共用的领域链路完成“扫描→选择→后台清理→回收站复查→磁盘前后对比→JSON 日志”。不允许使用 PowerShell 删除命令替代 APP 服务。实际可用空间增量达到 10 GiB 才能 PASS。

## 已完成证据

| 检查 | 结果 | 证据 |
|---|---|---|
| 规则库 | PASS | Windows 内置 v10 + 随包 v6/23 条，外部 JSON 只允许有界扫描与回收站动作 |
| 后台扫描 | PASS | `CleanupBackgroundRunner` 两个 Isolate 只读扫描完成，共发现 11.284 GiB |
| 验收计划 | PASS | 纳入明确可再生或旧诊断类型，约 11.28 GiB，不纳入项目、模型、账号数据和系统修复包 |
| 批量回收 | PASS（代码/定向测试） | Windows 每批 128 项，批间可取消；最终清空严格在所有移入批次之后 |
| 目标去重 | PASS | 回收站与系统盘日志清单可共用 `C:\` 而不相互覆盖 |
| 定向测试 | PASS | 28/28 |
| 静态分析 | PASS | `flutter analyze` 0 问题 |
| Windows Release | PASS | `build/windows/x64/runner/Release/vibekits.exe` 成功产出 |

## 本机计划范围

- 旧用户 Temp 和已识别的旧 Cache / Code Cache / GPUCache / 日志；
- Gradle、Pub、npm、Puppeteer、ccache、VS Code C++ IntelliSense 索引；
- 企业微信旧升级/补丁包、诊断日志和一周未修改的可再下载小程序运行包；
- Antigravity、哔哩哔、M-AI、wsMySQLtools 旧更新包；
- Codex 一天前的 `.tmp` 和可再生 `cache`，不包含会话、设置、技能或 Key；
- 现有回收站内容，以及本次移入回收站的上述内容。

## 当前结论

**PENDING USER APPROVAL**

代码、扫描计划、定向测试和 Release 已完成；真实删除会不可恢复地清空回收站，必须在用户明确批准上述范围后执行。未执行前不将 10 GiB 写为验收通过。
