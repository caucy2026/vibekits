# v1.9.0-dev.54 即时启动与清理入口验收

## 启动路径

1. Win32 窗口立即显示“Vibekits 正在启动”。
2. Flutter 首帧显示完整导航骨架，不创建重型工作区。
3. 下一帧挂载上次工作区。
4. Harness WebView、凭据读取、端口探测和 DSH 在工作区完成首帧后启动。

本机 Release 实测从启动进程到窗口真正可见为 **171 ms**，窗口响应状态为 `true`。

## 清理功能分工

- **扫描可清理项**：读取版本化安全规则，定向查找缓存、日志和临时文件；输出可选择候选，确认后才能清理。
- **分析全部占用**：递归计量所选磁盘，归属到 Windows、软件、用户数据、回收站等类别；只读生成解释和报告，不自动删除。

## 验证

- 启动首帧定向测试：通过。
- Cleaner Widget：11/11 通过。
- 修改文件静态检查：0 问题。
- Windows Release：编译通过。
