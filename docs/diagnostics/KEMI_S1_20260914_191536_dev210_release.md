# KEMI S1 dev.210 Release 真机验收记录

## 1. 任务与范围

- 时间：2026-09-14 19:15:36 CST
- 目标：`192.168.3.63:5555`
- 代码基线：`7d486b3`
- 范围：使用 VibeKits Harness 内置能力核验 S1 身份、文件往返、截图、安装并启动 `1.9.0-dev.210+2210`。
- 结论：**未通过，目标主机当前离线；APK 未安装，不能声称 63 真机可用性已通过。**

## 2. Harness 目录与工具

- 协议：`vibekits.tools.v1`
- 已实际调用：
  - `vibekits.serial.list_ports`
  - `vibekits.git.list_remote_refs`
  - `vibekits.git.read_remote_file`
  - `vibekits.adb.list_devices`
  - `vibekits.adb.connect`
- 计划在设备在线后继续调用：`vibekits.adb.command`、`vibekits.adb.push_file`、`vibekits.adb.pull_file`、`vibekits.adb.screenshot`、`vibekits.adb.install_apk`、`vibekits.adb.logcat`。
- 真机门禁：`test/manual_real_s1_release_acceptance_test.dart`。

## 3. APK 构建与身份

- 文件：`dist/candidates/Vibekits-1.9.0-dev.210+2210-android-test-debug-signed.apk`
- 大小：`206564702` bytes
- SHA-256：`1d29471a4c9bc5895a83e796fb75ee3b2d3de51e8c5a1a6e869708f0494378c9`
- 包名：`com.vibekits.vibekits`
- 版本：`versionName=1.9.0-dev.210`，`versionCode=2210`
- 最低 Android API：24；目标 API：36。
- APK Signature Scheme v2 校验通过，但证书为 Android Debug。该文件只用于 63 真机测试，不得作为正式发布包或上传商场。

## 4. ADB 身份与操作结果

`adb.list_devices` 未返回已就绪的精确目标，因此门禁按流程调用 `adb.connect`。连接在内置 10 秒边界内没有完成，被 Harness 终止：

```text
Bad state: ADB 命令 10 秒未完成，已终止
```

独立 TCP 可达性复核返回：

```text
nc: connectx to 192.168.3.63 port 5555 (tcp) failed: Host is down
```

因此本轮没有获得 model/manufacturer/device/Android 四字段，且没有执行文件传输、截图、APK 安装、启动、PID 或 Logcat。禁止把历史身份值代替本轮真机证据。

## 5. 串口发现与连续监控

`serial.list_ports` 成功，仅发现：

- `/dev/cu.Bluetooth-Incoming-Port`
- `/dev/cu.debug-console`

当前没有 CH340/CH341、USB VID `0x1A86`、PID `0x7523` 候选，因此不能执行 115200/8-N-1/no-flow-control 标记往返，也没有启动连续串口会话。不存在需要清理的串口 session。

## 6. 时间关联

| 时间（CST） | 通道 | 结果 |
| --- | --- | --- |
| 19:14-19:15 | Harness Git | manifest `master` 引用和 `default.xml` 读取成功 |
| 19:14-19:15 | Harness Serial | 无 CH340/CH341 物理串口 |
| 19:15 | Harness ADB | 精确目标未就绪；connect 10 秒超时 |
| 19:15 | TCP 复核 | `192.168.3.63:5555` 为 `Host is down` |

ADB 与 TCP 证据一致指向设备当前离线；没有串口通道可提供独立设备侧时间线。

## 7. Git manifest 与源码路由

- 远端：`http://172.21.16.194:8092/HiV730/manifest.git`
- `refs/heads/master`：`1eaf70aacec7b952c0d4aea81c56c7401df630c2`
- `default.xml` 同一提交读取成功，默认 revision 为 `master`。
- 本轮问题发生在目标网络可达性之前，没有设备故障签名或应用栈，不能归因于 HiV730 源码。
- 若设备恢复后出现 VibeKits 应用级异常，最小候选路由为 `vendor/newlink/packages/apps/<App>`；若仅是产品预装/权限策略，再按证据扩展至 `vendor/configs`、`vendor/device/mp`、`vendor/device/feature` 或 `vendor/device/sepolicy`。本轮不执行无证据的源码克隆。

## 8. 结论与置信度

- `confirmed`：dev.210 Android APK 已成功构建，包名/版本/SHA 与 v2 签名结构已校验。
- `confirmed`：63 当前不在 Harness ADB 已连接列表，且主机 5555 端口返回 `Host is down`。
- `unresolved`：63 上的安装、启动、Harness/PAD 可用性及运行稳定性，必须待设备在线后重跑。

## 9. 后续验证

设备在线且 `192.168.3.63:5555` 可达后，重跑同一门禁，必须一次完成：

1. 精确设备状态为 `device`；
2. 四字段严格匹配 `huanglong / HL2.0 / hi3781v730 / 12`；
3. 上传—下载字节一致并保存真实截图；
4. 覆盖安装 dev.210，确认 `versionCode=2210`；
5. 启动后 PID 非空并取得有界 Logcat；
6. 若连接了 CH340，再补做串口 marker 与连续监控。

## 10. 证据与清理

- 基线证据：`/Volumes/ORICO/kemi-build-cache/vibekits-dev210-android/acceptance/63/baseline-1789384787529160.json`
- 构建缓存：`/Volumes/ORICO/kemi-build-cache/vibekits-dev210-android/`
- ADB connect 超时后子进程由 Harness 终止；未创建 ADB 长连接 session。
- 未打开远程桌面、未连接 Windows、未向外部模型发送设备信息。
