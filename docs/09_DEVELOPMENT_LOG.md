# Vibekits 开发日志

## 2026-08-28 · v1.9.0-dev.137 KEMI 品牌文案纠正

- `KEMI` 作为品牌名不再翻译；启动按钮改为“启动KEMI远程办公”，设备号改为“KEMI办公 ID”。
- 设置、连接状态、网页端和错误提示同步保留 `KEMI` 英文品牌。

## 2026-08-28 · v1.9.0-dev.136 KEMI远程办公连接标识

- Harness 顶部“远程分享”增加不占位状态点：灰色未连接、橙色已发现/握手中、绿色真实握手与心跳正常、红色协议错误/过期。
- 新增独立连接状态机，只发现或启动客户端不会误报“已连接”；握手校验协议/版本/peer，6 秒无心跳自动过期。
- 用户可见品牌统一为“KEMI远程办公”，设备号改为“KEMI办公 ID”，仍在运行时通过兼容客户端自动获取。

## 2026-08-28 · v1.9.0-dev.135 Harness 远端 Git 按需取码

- Harness 实读 HiV730 Android 项目指南后发现既有 Git 工具只面向本地仓库；新增 `git.list_remote_refs`、`git.read_remote_file` 和 `git.clone_minimal`，统一使用 APP 内置 Git，不回退到任意 Shell 或系统 Git。
- 远端探测只执行有界 `ls-remote`；远端文件读取仅浅取指定 ref 并返回单个受限文本，临时对象随即删除；最小克隆要求目标目录不存在、明确单仓库和 ref，禁止无参数 `repo sync`。
- Git 网络调用关闭隐藏凭据交互，拒绝 URL 明文密码、越界仓库路径和覆盖已有目录；仍复用用户已配置的 SSH 身份与已核验主机指纹，不读取或输出私钥。

## 2026-08-28 · v1.9.0-dev.134 APK 降级安装显式控制

- 真实 Harness 使用 `network.download → adb.connect/list_devices → adb.install_apk` 下载并安装指定 KEMI-PAD APK；下载与设备连接成功，设备返回 `INSTALL_FAILED_VERSION_DOWNGRADE`。
- `adb.install_apk` 补齐标准 `allowDowngrade` 参数，显式为 true 时使用 `adb install -r -d`；Android 仍拒绝时只报告设备策略，不会擅自卸载应用或清除数据。
- 重写仓库首页项目介绍，以“智能体理解工具、自动配置、受控调用、证据闭环”为主线，集中说明统一能力注册表、机器可读 Schema、风险权限、模块原始日志和跨工具工作流，并按实际能力目录整理当前工具集合与三端状态。

## 2026-08-28 · v1.9.0-dev.133 Harness 网络下载与 ADB 安装链

- 新增 `vibekits.network.download` 通用网络文件工具，流式写入 APP 配置的工具下载目录，返回绝对路径、HTTP 状态、最终 URL、字节数、SHA-256、耗时和证据来源。
- 下载采用 `.part` 临时文件并只在完整接收和校验后原子完成；失败自动清理半截文件，默认不覆盖同名文件。`.apk` 会验证 ZIP/APK 文件头，避免 CDN 错误页被交给 ADB。
- Harness 使用准则增加“网络下载 → 内置 ADB 安装 → 设备侧验证”标准链路，禁止绕过 APP 使用 curl、PowerShell 或系统 ADB。
- 定向下载服务、失败清理、Harness 目录/调用合同与既有桥接回归共 27 项通过。

## 2026-08-28 · v1.9.0-dev.130 外部智能体 MCP 与 OCR 空间理解

- 将既有环回 HTTP 工具桥和 stdio MCP 从 Codex 专用措辞升级为任意 MCP 智能体接入；生成目录补齐通用 JSON、Codex TOML、工具名映射、安全边界和调用链说明。
- 接口目录一致性测试会从真实注册表自动重写并核对文档，发布质量门禁必须运行；新增工具无需手工维护第二份接口清单，也不把原生 Hook 的成本和失败风险塞进 APP 构建路径。
- 截图 OCR 在兼容旧 `bounds` 的同时返回 `boundsPx`、0..1 `boundsRelative`、九宫格 `region`、`spatialText`、坐标原点和阅读顺序，使无视觉模型的 Harness 能按位置理解页面。
- OCR 档位实现为桌面高精度 `PP-OCRv6_medium`（34.5M 参数）与移动/离线快速 `PP-OCRv6_tiny`；Medium 三文件按官方大小和 SHA-256 主动下载、事务导入，安装后桌面自动优先使用，高精度模型不进入基础 APK、也不拖慢首屏。

## 2026-08-27 · Windows 满盘实机清理与算法回填

- 80 GiB C 盘仅剩 0.90 GiB 时实机定位 `.gradle` 7.92 GiB；清理 Gradle/Pub/临时缓存并运行 Windows 组件清理后，关闭已启用的 Windows 保留存储，最终物理可用空间稳定为 14.52 GiB。
- Gradle 构建缓存由“30 天才显示”改为“24 小时未更新的完整顶层目录”，新增 `.gradle/.tmp` 1 小时残留规则；`devCache` 分类保持最终人工确认，确认后则允许强力永久删除可再生缓存，修复只移入回收站而不增加可用空间的缺口。
- Windows `Users` 深度范围新增非当前用户配置容量盘点：排除 Public/Default/当前用户，其他配置固定为系统管理且受保护，不因容量大或盘空间低进入 10 GiB 自动计划。
- 实机证明 JSONL/压缩或稀疏文件的逻辑长度不等于 NTFS 物理释放量；完成标准继续以卷 API 清理前后可用字节差为准，不以候选 `File.length` 之和宣称达标。
- 系统盘可用空间低于 5% 时，快速扫描现在会按 10 GiB 实际释放目标自动勾选已验证的可再生缓存，但执行前仍保留一次永久删除确认；回收站、下载、未知目录和用户配置不会自动凑数。
- 新增 Codex `.tmp` / `tmp` / `cache` 24 小时规则，明确排除 `sessions` / `plugins` / `skills` / 凭据与当前工作区；目标发现、平台边界和低空间自动选择回归 40 项全部通过。

## 2026-08-27 · v1.9.0-dev.122+132

- 存储路径从静态规则升级为运行时能力：启动时验证设置、模型、下载、缓存和 Harness 调试目录真实可写。
- 接入平台官方应用目录 API；安装目录、应用支持目录不可写时按数据持久性分级自动降级，并把结果同步显示给 UI 与 Harness。
- 53 项路径、迁移、清理与 Harness 回归通过，目标代码 Analyze 0 问题。
- Windows Release 与真实冷启动路径探针通过，探针残留 0；Android ARM64 Release APK 编译通过。
- macOS GitHub Actions `33024241863` 完整通过平台 Analyze、存储与清理策略测试、unsigned Release 编译、校验和打包及产物上传，三端编译门禁闭环。

## 2026-08-27 · v1.9.0-dev.121+131

- 修复跨宿主平台路径仿真：Windows 可执行文件父目录不再依赖当前宿主的 `File.parent`。
- macOS GitHub Actions 增加 Analyze、平台策略测试和 unsigned Release 编译门禁，产物版本自动读取 `pubspec.yaml`。
- GitHub Actions `33022087516` 已通过 Analyze、平台策略测试、macOS Release 编译、校验和打包及产物上传。

## 2026-08-27 · v1.9.0-dev.120+130

- 新增统一平台存储布局：Windows 使用 LocalAppData，macOS 使用 Application Support/Caches/Logs，Android 使用应用私有 files/cache；设置页直接显示当前真实位置和安全凭据库。
- 修复 macOS/Android 设置或下载目录可能退到当前工作目录的问题；旧设置自动迁移到新持久目录，SSH、ADB 等历史不丢失。
- Windows、macOS、Android 清理能力改为机器可读策略，UI 展示当前扫描范围与删除边界，Harness 能从能力检查读取相同信息。
- 50 项平台路径、清理边界、UI、设置迁移及 Harness 回归通过，Analyze 0 问题；Windows 本机只读实扫 21 秒识别 6.494 GiB 候选。

## 2026-08-27 · v1.9.0-dev.119+129

- 建立 19 个独立工作区的真实连续使用合同：再次进入、多目标并存和秘密保存策略由 UI 与 Harness 共用；缺少合同会直接测试失败。
- 修复网络抓包已有实现却未进入独立工具列表的问题。
- API 调试新增最近 30 个请求并支持一键恢复；只保存方法、URL 和非敏感请求头，请求正文、Authorization、Cookie、Token、Secret、API Key 均不落盘。
- ADB 新增最近 20 个无线设备和 50 条命令；串口新增最近 50 条发送历史，APP 重启后仍可复用。历史存储异常不会阻断真实连接、命令或串口发送。
- SSH/SFTP 已确认具备多设备、多终端、安全凭据和同会话文件传输；数据库、代理等 19 个独立工作区均有机器可读的连续使用合同，剩余缺口记录在 `docs/39_TOOL_REAL_USAGE_SCENARIO_MATRIX.md`。
- 29 项工具/UI 合同与 3 项设置持久化测试通过，目标文件 Flutter Analyze 0 问题，Windows Release 构建成功；详见 dev.119 验收记录。

## 2026-08-26 · v1.9.0-dev.118+128

- Android 移动 Harness 不再过滤桌面工具，全部 130 个公开接口 Schema 均可被智能体发现；桌面专属项统一返回 `requiresDesktopNode`，杜绝手机端伪执行。
- Pad 开发工具侧栏和底部导航扩大触控热区；平台说明页提供一键预填 Harness 任务，减少 IP、工具名和验证要求的重复输入。
- ARM64 Release 已安装到 `192.168.3.63:5555`，冷启动 584 ms；五个一级入口、双屏连续画布、扫码 Key 与平台交接真机通过。
- 自动化 118/118 通过；真实在线模型联调因 Windows 凭据库没有 DeepSeek Key 停在明确门禁，未伪造为通过。完整记录见 `docs/acceptance/V1_9_0_DEV118_ANDROID63_HARNESS_PAD_2026-08-26.md`。

## 2026-08-24 · v1.9.0-dev.93+103

- 根据 `kemi-rd/unique` 跨屏规范，废弃 dev.86～dev.88 的双 Activity 异显方案，改成唯一 `MainActivity`、唯一 Flutter Engine/View、唯一状态树运行一张 `1920×2560` 连续画布。
- D2 `Presentation` 显示逻辑 `Y=0..1279`，D0 显示 `Y=1280..2559`；两屏沉浸式占满各自 `1920×1280` 物理区域，不再出现系统栏造成的中缝。
- Android Flutter 渲染切换为 texture；D2 复用源 FlutterView 绘制并把触摸转发到同一 View，解决“看似异显但两个页面状态独立”的根本问题。
- `192.168.3.62:5555` 真机在 D2 点击 OCR 后，D0/D2 同步显示同一 OCR 页上下区段；D2 退出后 Activity 与 Presentation 同时消失，重新启动可恢复。
- 合同测试 5/5、Flutter Analyze 0 问题、ARM64 Release 构建通过；完整证据见 dev.93 验收报告。

## 2026-08-24 · v1.9.0-dev.88+98

- Android 默认双屏工作区明确为纵向 `1920×2560`：D0 对应上半区 `0–1280`，D2 对应下半区 `1280–2560`，继续保持 Harness + 资源诊断异显分工。
- Android 底部导航由“只显示选中项文字”改为所有入口始终显示图标与文字，减少记忆和试点成本。
- 两屏顶部新增“退出应用”按钮；双屏模式调用原生协调器原子关闭 D0/D2，单屏模式关闭当前任务。
- 顶部增加上屏/下屏位置标识，用户可直接确认当前窗口在 1920×2560 工作区中的角色。

## 2026-08-24 · v1.9.0-dev.87+97

- Android 双屏从“两个窗口显示同一默认页”升级为异显分工：D0 默认进入智能体（Harness），D2 默认进入资源诊断（CPU/GPU），两边仍可手动切换其他工具。
- Flutter 正式消费原生 `vibekits/display` 的 `mode / role / displayId` 上下文；单屏启动仍只显示 Harness，不误切副屏工作台。
- 纠正 Android 启动时强制跳到开发工具和 OCR 的旧兼容逻辑，智能体子页现在明确默认 Harness。
- CPU、内存、GPU、磁盘、网络流量、PCM 波形与频谱图表补充文字说明、颜色图例和判断边界。

## 2026-08-24 · v1.9.0-dev.86+96

- 按 `kemi-rd` 正式启动规范新增无 Flutter Engine 的 `DualScreenLaunchActivity`，普通点击默认进入双屏模式。
- 非 D0 屏启动时先移除临时任务，等待 400 ms 后显式路由至 D0；D0 总控再枚举真实在线外接屏创建配对窗口。
- 长按应用图标新增“单屏模式”，只在发起操作的当前显示屏运行。
- 双屏任一窗口返回时原子关闭两个 UI Activity，但不强杀进程，保持 SSH、传输和智能体后台任务的独立生命周期。
- 新增 `vibekits/display` 平台上下文接口、双屏启动契约测试和完整验收文档。
- 在 `192.168.3.62:5555`（D0 内置屏 + D2 HDMI，均为 1920×1280）安装 ARM64 Release：关闭 D0 后两屏同时退出，关闭 D2 后两屏同时退出，Logcat 无 VibeKits FATAL/ANR。
- Android 构建链固定为 AGP 8.13.2、Kotlin 2.3.0、Gradle 8.14.3；标准 `flutter build apk` 无需跳过依赖检查即可生成 61.2 MB 的分 ABI ARM64 APK。

## 2026-08-24 · v1.9.0-dev.85+95

- 独立工具入口统一为“中文用途（英文缩写/标准产品名）”，例如“安卓调试（ADB）”“远程连接（SSH/SFTP）”“网络代理（Clash Verge）”“轻量虚拟机（QEMU）”。
- 智能体主入口改为“智能体（Harness）”，截图入口改为“截图识别（OCR）”；远程工作区 Tab、设置项和页面副标题同步统一，不再一处中文、一处英文。
- 新增命名合同测试：所有独立工具必须同时包含中文用途和全角括号中的标准名，防止后续新增功能重新引入混乱命名。

## 2026-08-23 · v1.9.0-dev.84+94

- 使用本机正在运行的 Clash Verge 截图逐项对照，代理工作区收敛为相同的 8 项导航：首页、代理、订阅、连接、规则、日志、测试、设置；选中态改用 Clash 的浅蓝色，代理组保持双列节点、整组测速和单节点测速。
- 轻量虚拟机不再伪装成 Clash 的第九个栏目，改为开发工具中的独立入口；无需启动代理即可创建磁盘、选择 ISO 和启动 QEMU。代理与虚拟机仍分别向 Harness 注册完整工具接口。
- Windows 新增原生系统托盘生命周期：点击窗口关闭只隐藏界面，SSH/SFTP、端口转发、Mihomo 等显式后台服务继续运行；托盘提供“打开 Vibekits”和“退出并停止后台服务”。Windows Release 原生编译通过。
- 闭环测试发现并修复虚拟机拆分后的重复滚动容器问题；针对性 Widget 用例和 Harness 合同用例通过，静态分析 0 问题。

## 2026-08-23 · v1.9.0-dev.81+91

- 产品定位统一为“智能体原生开发者工具工作台”：智能体理解目标和解释结果，本地工具负责确定性执行和真实证据，二者共享任务上下文。
- 音频分析增加有界时间轴窗口，Harness 默认获得谐波与噪声热点时间段；合成夹具分别在 1～2 秒注入谐波、2～3 秒注入噪声并通过定位断言。
- 高频 Harness、MCP、ADB、SQLite、SSH/SFTP、OCR、文件、API、Git、代理/虚拟机合同和音频测试单线程通过；真实本地模型端点完成一次模型发起 MCP、APP 执行和最终回复。
- 新增每周 Harness 上游演进工作流：发现官方 npm 新版后在隔离目录准备候选、运行补丁和 CLI 冒烟并创建评审工单，不允许开发预览版直接覆盖客户端。
- 根 README 不再保留 Flutter 模板文字，改为正式产品简介；新增智能体逐工具六层验收和灰度回滚规范。

按时间记录开发过程、关键决策、问题与解决、里程碑状态。里程碑详细验收见 `docs/acceptance/`。

## 2026-08-23 · v1.9.0-dev.79 Android 操作习惯与局域网 Key 输入

- 手机端一级导航改为 Material 3 底部导航，只显示当前项文字；移除桌面式顶部横排 Tab。安卓返回键/返回手势按实际访问历史返回上一个工作区，历史为空时退出。
- 手机端工作区改为贴边内容、无桌面阴影/圆角外框和底部桌面状态栏；仍只挂载当前重型页面，避免后台累积 OCR、数据库等状态。
- Harness 设置新增“同局域网扫码输入 Key”：APP 生成一次性二维码，另一设备扫码打开内置临时网页，粘贴并确认后 Key 自动回到安卓 APP；二维码不包含 Key，错误令牌拒绝，接收一次或五分钟后立即停服。
- Android 新增原生 Keystore 凭据通道，API Key 使用随机 IV 的 AES-GCM 加密后写入私有 SharedPreferences；Key 不进入普通设置、二维码、URL 或日志。
- Android 模型、下载和 Harness 调试目录改到应用 files/cache 沙箱，不再按桌面可执行文件目录推导。
- 引入 BSD-3-Clause `qr_flutter 4.1.0` 本地绘制二维码；专项 HTTP 闭环 2/2、定向静态分析 0 问题。
- 首次 APK 构建识别出用户 Gradle 9.3.1 全局缓存损坏；切换项目隔离缓存后 Android arm64 Release 构建成功，产物 108.1 MB。
- 详细验收与尚未移动化的桌面能力见 `docs/acceptance/V1_9_0_DEV79_ANDROID_UI_LAN_KEY_2026-08-23.md`。

## 2026-08-22 · Windows 测试节点跨 Mac 搭建手册 v2.0

- 将原节点调用说明整理为从零搭建手册，明确专用 Windows 标准账户、Private 局域网、受限 SSH 防火墙、每台 Mac 独立 Ed25519 身份和 host key 固定要求。
- 增加 Windows 本机 `127.0.0.1` 公钥登录预验收；只有本机闭环成功后才通知 Mac，避免把服务端配置问题转嫁给远端反复尝试。
- 增加 PowerShell 7、D 盘工作区、TEMP/TMP、SFTP 上传/远端/下载三次 SHA-256 一致性及故障定位顺序。
- 明确 VibeKits MCP 只监听各设备本机回环地址，不跨设备共享 bearer token、私钥或密码；补充 Codex MCP 注册示例和新任务工具发现要求。
- 文档只保留可公开核验的示例 IP、公钥指纹和脱敏输出，不包含私钥、密码或访问令牌。

## 2026-08-22 · v1.9.0-dev.78 清理经验算法与性能闭环

- 把人工清理判断固化为“发现、证据判定、目标规划、执行复核”四层：系统管理、当前 Vibekits 数据、用户文件和未知目录始终保护；磁盘再满也不能突破硬门禁。
- 新增 10 GiB 恢复规划器：先选自动安全项，再按容量从有明确归属、可重建的建议项中补足目标；证据不足时显示真实缺口，不用未知文件虚构达标。
- 修复扫描结束后长时间不退出的核心性能问题：嵌套候选去重由逐项遍历既有结果的 O(n²) 改为按类别/来源分组的祖先路径索引；50,000 项性能回归在 3 秒门槛内通过。
- 同路径规则合并改为系统保护优先、精确规则优先于泛化发现；避免广义日志/缓存识别覆盖更安全的应用专属规则。
- 扫描仍固定在两个后台 isolate，避免近满系统盘上的随机 I/O 竞争；同步元数据读取只发生在工作 isolate，UI 保持可操作和可取消。
- 新增 Espressif 组件下载缓存、Dart 分析服务缓存和哔哩哔哩媒体缓存精确规则；开发缓存超过 24 小时可进入“建议清理”，但永不自动删除。
- 本机最新版只读审计 77 秒完成：89,331 个候选、6.466 GiB 潜力；自动安全 0.533 GiB、建议清理 4.203 GiB、人工复核 1.731 GiB。本轮未删除任何真实文件，也没有为了 10 GiB 目标提升未知项目风险。
- 清理定向回归 15/15、静态分析 0 问题；详细证据见 `docs/acceptance/V1_9_0_DEV78_CLEANUP_EXPERIENCE_ALGORITHM_2026-08-22.md`。

## 2026-08-22 · dev.76 音频调试闭环

- 新增统一“音频调试”工作区：PCM/WAV 格式识别、多声道波形、内置播放、频谱快照、峰值/RMS/削波/静音/DC 偏置分析。
- RAW PCM 支持 8/16/24/32 bit、1～8 声道、常见采样率、有符号/无符号及大小端设置；修改参数后可重新分析。
- 大文件解析在 isolate 中执行；波形固定降采样至有界点数，避免占用 UI 线程。
- `.pcm/.raw/.wav/.wave` 拖入后自动路由；Harness 自动注册只读接口 `vibekits.audio_analyzer` 并进入统一调用日志。
- 引入 MIT `audioplayers 6.8.1` 作为跨平台本地播放层；测试生成 440 Hz PCM，并闭环验证 RAW 分析、WAV 封装与再次识别。

## 2026-08-22 · v1.9.0-dev.69 Android 真机性能与 macOS 构建链

- Android 真机性能：裁掉移动端无效的桌面 MCP 服务、文件关联和拖放监听；切换模块时释放隐藏的重型工作区。
- Android 可用入口：模型页默认进入本地 PP-OCRv6 tiny，不再先探测桌面 Harness Runtime。
- 真机闭环：Android 12 arm64 Release APK 构建、安装成功；三次冷启动平均 716 ms，稳定 PSS 约 102 MB，退出无残留进程。
- 构建体验：Gradle 限制为 3 GB/2 worker；使用 D 盘隔离缓存并规避 C/D 跨盘 Kotlin 增量缓存错误，避免构建拖慢整机。
- macOS：Windows 本机确认无法调用 Xcode 构建；macOS 14 GitHub Runner Run `32542306872` 已真实完成 Release 构建、压缩、校验和及 53,000,834 字节产物上传。

## 2026-08-22 · v1.9.0-dev.68 代理与轻量虚拟机可见、可用、可恢复

- 聊天记录和 dev41 文档确认代理/虚拟机早已进入范围，但旧实现只启动 Mihomo 和已有 QEMU 磁盘；入口又位于网络分类深处。现在“Clash Verge / 虚拟机”固定为左侧第二个独立工具，程序员计算器仍保持默认第一页。
- Clash 页主操作改为“启动并启用系统代理”：明确确认后先保存 Windows 当前用户代理，再启动内置 Mihomo 并切换到配置的 `mixed-port`；“停止并恢复原网络”恢复 ProxyEnable/ProxyServer/ProxyOverride。写入失败自动回滚，异常退出后的备份保留供下次恢复，不静默开启 TUN。
- QEMU 页新增 8～256 GiB qcow2 稀疏磁盘创建，并明确支持 Windows 7～11、x86_64 Linux/BSD 等 PC 系统；不虚假宣称 macOS 来宾或 ARM 支持。
- Harness 新增系统代理应用/恢复和虚拟磁盘创建工具；`proxy.start` 可在启动成功后原子启用系统代理，`proxy.stop` 可恢复旧值。Mihomo/QEMU 进程绑定 Windows Job Object，APP 进程关闭时由系统兜底终止子进程树。
- 真实 dev.68 Release 验收通过：Harness 启动 Mihomo、切换并恢复真实 Windows 代理、创建 1 GiB qcow2、无窗口启动 QEMU、读取运行状态并停止，全流程 4 秒。详见 `docs/acceptance/V1_9_0_DEV68_NETWORK_VM_CLOSED_LOOP_2026-08-22.md`。

## 2026-08-22 · v1.9.0-dev.67 ADB 语义工具与真机闭环

- Harness 新增 `vibekits.adb.shell`、`logcat`、`install_apk`、`push_file`、`pull_file`、`screenshot` 六个明确语义接口；模型无需再拼装通用 ADB 参数，且所有接口继续使用随包发布的 `adb.exe`、统一权限和可删除活动日志。
- Shell 只进入 Android 设备，不经过本机 shell；Logcat 默认有界读取；安装、拉取和截图要求绝对路径及显式覆盖；截图使用设备端临时文件并在结束时清理。ADB 工作区的 Harness 记录面板同步包含新接口。
- 自动合同测试覆盖六个接口和八次底层 ADB 调用。真实设备 `192.168.3.63:5555` 经同一 Harness bridge 完成连接、设备枚举、Shell、Logcat、推送/拉取字节一致性和截图；设备为 `huanglong`，截图 4,433,824 字节。
- Android Release 通过项目隔离 Gradle 缓存构建出 161.7 MiB APK；首次真机安装暴露固定 10 秒超时，执行层随即改为普通操作 30 秒、传输 2 分钟、安装 5 分钟的分级超时，超时仍会终止进程。修复后 `vibekits.adb.install_apk` 在真机返回成功，完整验收 15 秒通过。
- 详细证据见 `docs/acceptance/V1_9_0_DEV67_ADB_SEMANTIC_TOOLS_2026-08-22.md`。

## 2026-08-22 · v1.9.0-dev.66 工具集界面与智能体接口融合

- 保持“一个转换与检查工作区 + 分类 Tab”的简洁结构；二级工具条改为一次构建的水平选择器，增加左右浏览按钮，深处工具可被界面搜索直接选中并自动滚动到中央，不再出现选中了却看不到的情况。
- 每个工具说明旁直接显示 `智能体 · vibekits.<id>`，点击即可复制 Harness/MCP 工具 ID；界面和智能体继续复用同一个 ToolSpec、领域处理器与活动日志，不增加第二套接口表。
- 输入、参数、结果和主操作保持同一工作区；新增 `Ctrl+Enter` 快捷执行。宽窗口并排显示输入/结果，640px 窄窗口自动改为上下布局，避免溢出和遮挡。
- 小窗口布局、接口显示、快捷键真实执行和既有开发工具 Widget 回归 10/10 通过；30 项领域/Harness 自动发现与真实调用回归继续通过，静态分析 0 问题。
- 清理残留的旧 VibeKits 高 CPU 进程后独立性能复测 16/16 通过；转换与检查首开 307 ms、热切换 145 ms，Harness 调用记录 16 ms。

## 2026-08-22 · v1.9.0-dev.65 30 项高频微工具融合

- 对照 DevToys、CyberChef 和 Hexkit 的公开能力版图，先与现有能力清单去重；没有增加 30 个左侧孤立入口，而是在“开发工具 → 转换与检查”既有分类 Tab 中补齐 30 项高频本地操作。
- 新增 JSON 压缩/转义、XML 格式化/压缩、CSV/JSON 双向转换、JWT 解码/过期检查、2～36 进制、端序、字符码、chmod、SemVer、容量/时间单位、HEX/RGB、Query、正则/Glob、文本行处理、命名风格、换行符、HTTP 状态码和 MIME 查询。
- 30 项复用 `ToolSpec` 单一清单，自动进入界面、Harness `vibekits.<id>` 可执行目录和对应开发工具调用日志；全部为离线只读、有界输入，不复制上游源码，不新增第三方运行时。
- 新增 30/30 代表输入领域测试、30/30 Harness 发现测试、真实 Harness invoke 和统一工作区 Widget 闭环；同时给横向工具条增加稳定自动化标识，修复工具增多后旧 Base64 解码测试依赖可见文字的脆弱问题。
- 定向 Harness/领域回归 23/23、开发工具 Widget 10/10 通过；开发工具性能独立复测 16 个入口全部通过，转换与检查首次 366 ms、热切换 167 ms。
- 完整静态分析 0 问题，Windows Release 构建成功；PE 版本为 `1.9.0-dev.65+75`，Git `2.55.0.windows.3` 与 25 项自包含运行时核验通过。

## 2026-08-22 · v1.9.0-dev.64 清理经验决策引擎闭环

- 在当前 80 GiB 系统盘连续运行同一套 APP 只读规则：首次扫描 6:56，识别默认候选 8.163 GiB / 全部潜力 10.640 GiB；确认旧验收把 7 GiB 以上内容合并为“其他默认安全候选”，缺少归属和证据，不能直接执行。
- 新增独立清理决策引擎，将候选按可重建性、缓存语义、修改时间、软件归属、容量收益和磁盘压力分为“自动安全 / 建议清理 / 人工复核 / 保护不清理”；磁盘压力只改变排序，不能突破系统、用户数据和当前 APP 的硬保护。
- 扫描后的默认勾选与“智能选择”统一复用决策引擎；回收站改为永不默认勾选，证据不足项目不再为了达到 10 GiB 目标被加入计划。VibeKits 可执行目录、配置/报告和当前 WebView 数据加入运行期保护。
- 增加父子候选归并，避免路径嵌套重复计算；删除前继续复核类型、大小和修改时间。Win32 文件身份函数改为每个工作 Isolate 只解析一次，已知缓存的扫描阶段不再逐文件打开身份句柄。
- 同机第二次复扫候选 8.145 GiB / 全部潜力 10.622 GiB，耗时由 6:56 降到 2:21。最终纯只读审计扫描 46,900 项 / 10.620 GiB，用时 132 秒；新决策分账为自动安全 0.353 GiB、建议清理 3.514 GiB、人工复核 6.754 GiB、保护项 0 GiB。人工复核主要包含回收站，必须另行确认。本轮没有删除真实文件。
- 清理决策、扫描、后台取消、文件替换防护、删除报告、容量总结和多磁盘界面定向回归 30/30，通过完整静态分析（0 问题）和 Windows Release 构建；产物版本核验为 `1.9.0-dev.64+74`。

## 2026-08-21 · v1.9.0-dev.63 外部 Codex MCP 注册闭环

- 修复“APP 有工具但外部 Codex 无法发现”的生命周期缺口：VibeKits 启动即发布 loopback bridge，退出即删除短期连接文件，不再要求先进入 Harness 页面。
- 新增 Windows STDIO 启动器与项目级 `.codex/config.toml`；启动器使用随包 Node/MCP，必要时自动启动 Release APP，不依赖 npm/npx 或局域网 HTTP 服务。
- 外部调用复用 `vibekits.tools.v1` 单一目录和活动日志；无界面审批时写入、设备控制和破坏性工具默认拒绝，避免用普通 SSH/shell 绕过身份登记。

## 2026-08-21 · v1.9.0-dev.62 智能体节点工具合同补齐

- 新增 `vibekits.windows_node.helper_status` 真实只读处理器，返回当前 Release helper 固定路径、签名/manifest 可用性、协议版本、可执行动作和明确阻断原因；缺失 helper 时不伪装可用。
- 完整能力目录新增 `vibekits.windows_node.ensure_client_identity`，在 macOS Keychain 适配器完成前保持不可执行；既有 apply/enroll/revoke/rollback/verify 继续遵守签名 helper 和外部执行器门禁。
- 新增智能体接口文档，统一 11 个节点工具的调用顺序、风险、输入、输出、审批和失败处理；Harness 目录与实际调用回归 20/20 通过。

## 2026-08-21 · dev.58～dev.61 近期改动归档与 GitHub 备份

- `dev.58`：Windows 测试节点只读体检、GitHub 分层诊断/回环代理、受控 Git 备份领域逻辑和 Harness/UI 接入。
- `dev.59`：80 GiB 系统盘清理缺口分析、过期聚合候选和 10 GiB 潜力/实际释放分账。
- `dev.60`：修复移入回收站被误算为实际释放；以磁盘可用空间增量作为唯一验收事实，并增加半年未使用软件的 Prefetch 证据和正式卸载入口。
- `dev.61`：补齐签名 helper 安全合同、Ed25519 设备生命周期、跨设备证据报告、GitHub 代理状态漂移保护和受控备份竞态保护。
- 已归档需求、实现说明、自动测试、Release 校验和外部门禁；云端备份只包含 Git 已跟踪且经过验收的源码与文档，不包含根目录调试截图和未纳入发布门禁的临时 smoke 脚本。
- GitHub 备份目标为 `caucy2026/vibekits` 的 `main`，采用普通 fast-forward push，不使用 force、不删除远端引用；同时发布 `v1.9.0-dev.58`～`v1.9.0-dev.61` 标签。远端核验记录见 `docs/acceptance/GITHUB_BACKUP_DEV58_DEV61_2026-08-21.md`。

## 2026-08-21 · v1.9.0-dev.61 Windows 节点安全底座与跨设备证据合同

- 增加签名 UAC helper 的版本化协议、白名单 action、防命令注入、短期 nonce、防重放和一次性回执验证；客户端执行前同时核验 Authenticode、发布者、固定哈希和协议版本，并区分 UAC 拒绝、取消、超时、部分/进程失败和无效回执。
- 增加独立 Ed25519 设备注册表、严格公钥结构校验、重复指纹拒绝、禁用/恢复/撤销、最近连接时间、原子保存、active-only `authorized_keys` 和无秘密 onboarding；Harness 开放设备列表及 onboarding 两个真实只读工具。
- 增加跨设备 `NodeVerificationReport`/`WorkflowArtifact`，强制外部来源、固定 host key、SSH/Pwsh/D 盘 TEMP/SFTP 双向 SHA-256/取消无残留/断网重试/防火墙/重连等 11 项证据，拒绝 localhost 和伪通过。
- 节点安全新增 11/11、Harness 桥接 20/20，既有 Windows/代理/Git 领域 15/15 通过。签名 helper 实体、两台 Mac 和真实私有仓库 push 仍需外部证书、设备和两次独立审批，不以模拟结果伪报完成。

## 2026-08-21 · v1.9.0-dev.60 实际释放口径与软件使用建议

- 修复“越清理空间越少”的核心口径错误：移入同盘回收站只记为“仍在回收站”，不再计入实际释放；清理结束以磁盘可用空间前后增量为最终事实，累计清理也只累加真实增量。
- 清理计划改为“安全推荐、需人工确认、永久清空回收站”三级组成，明确展示计划容量及距离 10 GiB 的差额；下载和重复文件不会被智能计划自动加入。
- 删除器固定最后处理回收站，避免先清空、随后又把大量文件移入回收站造成可用空间再次下降；结果页同时显示实际释放和仍占磁盘的回收站容量。
- 软件建议引入 Windows Prefetch 最近启动证据，只在有可靠记录且超过 183 天时标记“半年未使用、建议卸载”；无法取得记录时明确显示“无法判断”，不把安装日期冒充使用日期。列表展示安装路径，并调用软件注册的正式卸载器。
- Prefetch 关联在后台 Isolate 执行，安装目录检查从递归全盘遍历改为最多 120 个首层项目；本机 64 个软件独立测量约 1.96 秒，界面先显示分析状态再异步合并结果。
- 删除/报告/注册表/软件聚合/界面定向回归 26/26 通过，完整静态分析 0 问题；实际 10 GiB 是否达标只在用户确认执行后按磁盘增量判定，不以候选容量虚报通过。

## 2026-08-21 · v1.9.0-dev.59 本机清理缺口分析与聚合候选

- 在当前 80 GiB 系统盘执行 APP 后台线程全量只读扫描：修复前只能识别 8.104 GiB，且其中 5.876 GiB 是系统回收站；确认原算法把“潜在可释放、需确认、默认推荐”混在一起，不能据此宣称实际通过 10 GiB 验收。
- 新增“过期直属子目录”聚合扫描策略：不跟随链接/联接点，按年龄、名称模式和有界条目数核验，将包含数万小文件的智能体任务或构建版本聚合为一个可审核候选，显著降低列表噪声和后续删除任务数。
- 当前机器新增识别 Antigravity/Gemini 旧任务调试产物约 1.552 GiB、旧会话库约 79 MiB、Gradle 8.5 过期发行包约 280 MiB、360 浏览器 `.old` 旧内核约 576 MiB；当前 Gradle 9.3.1、360 当前版本、智能体运行时和用户数据均不进入这些规则。
- 更新后 APP 全量只读复扫识别 10.566 GiB 潜在候选、46,724 项；其中上述新增项全部标为“需确认”且默认不勾选。清理页新增“推荐 / 需确认 / 系统管理 / 可释放潜力”分账，并明确潜力不等于实际释放；顶部 10 GiB 验收仍只按清理后的磁盘增量判断。
- 清理器定向回归 18/18、当前机器全量只读扫描 1/1 通过；本轮没有删除用户文件，实际释放量仍待用户在 APP 内确认候选后验证。

## 2026-08-21 · v1.9.0-dev.58 Windows 测试节点、GitHub 代理与受控备份

- 将外部输入稿迁移为 `25_WINDOWS_TEST_NODE_AND_GITHUB_BACKUP.md`，同步阅读顺序、需求总账和三条融合工作流；未取得签名 helper、两台 Mac 与私有仓库实证的项目继续标为待验收。
- Windows 测试节点新增普通权限只读体检和短期幂等计划：固定 `D:\KEMI-Test`、D 盘 30 GiB 门禁，合并 Windows/硬件/网络/OpenSSH/防火墙/运行时/电源/账户/公钥事实；摘要篡改、过期、路径越界或阻断项会拒绝执行。签名窄权限 UAC helper 尚未交付，因此 apply/rollback 不伪装为可用。
- GitHub 网络诊断补齐 GCM 标签、TCP 443、DNS、TLS/HTTPS、SSH 22/443、WinINet/WinHTTP/环境/Git 代理、hosts 和内置 Git `ls-remote`；代理候选从 Mihomo/Clash 真实 loopback 监听发现并分别验证 HTTPS/Git，不假设 7890。修复只写 `http.https://github.com.proxy`，验证失败自动恢复旧值。
- Git 工作区新增安全备份：已有 remote 预览、秘密阻断、构建/大文件警告、短期状态摘要、明确文件集合；commit 与 push 两次独立审批，push 只进入 `backup/` 分支且远端 SHA 二次核验，不提供 force/删 ref/改 tag。秘密扫描进入独立 Isolate，避免卡住界面。
- ToolSpec/Harness 统一注册新增 ID；已完成的 inspect/plan/proxy/backup 工具进入可执行目录，依赖签名 helper 或跨设备证据的节点工具只进入完整能力目录并明确不可用，防止模型误调用。定向领域、桥接、Git/诊断/节点界面及 SSH/SFTP 回归（含真实 Windows 只读探测）49/49 通过。

## 2026-08-21 · v1.9.0-dev.57 清理分析列表自适应满屏

- 移除磁盘分析卡片固定 370 像素高度，分析结果和扫描中间结果使用清理工作区全部剩余高度，软件/磁盘列表不再只露出两行。
- 空间分析与清理候选改为互斥工作视图：点击“分析全部占用”进入满屏分析，点击“扫描可清理项”切回满屏候选，避免两个长列表互相挤压。
- 多磁盘结果切换会保持分析视图；实时扫描列表同样自适应剩余高度。Cleaner Widget 定向回归 12/12、定向静态检查 0 问题、Windows Release 与 22 项内置运行时核验通过，并新增 800×600 下卡片延伸到底部的布局断言。

## 2026-08-20 · v1.9.0-dev.56 磁盘分析明细与本地恢复

- 软件占用和磁盘占用列表的每一项均可点击查看完整明细：真实路径、字节数、安装/数据/缓存组成、发布者、版本、归属、修改时间、统计完整性、判断依据和安全建议；路径可直接复制。
- 磁盘占用不再只截取前 40 项，全部结果通过惰性列表展示；APP 软件详情明确列出注册表安装路径及扫描关联到的全部目录。
- 分析完成后将多磁盘结果和已安装软件信息保存到 `%LOCALAPPDATA%\Vibekits\Reports\latest_drive_analysis.json`。重新进入清理页只恢复上次结果，不启动后台重扫。
- 清理缓存或移入回收站后不再自动触发耗时分析；界面保留上次快照并提示用户点击“分析全部占用”后才重新计算。
- 快照 JSON 编码和文件读写整体放入独立 Isolate，避免大结果保存占用 UI 或留下未释放文件句柄；列表补齐 Material 点击反馈层。
- 快照序列化、页面重入不扫描、条目详情和 Cleaner 既有功能定向回归 20/20 通过；定向静态检查 0 问题；Windows Release 编译通过，EXE 内嵌版本 `1.9.0-dev.56+66`，22 项必需运行时齐全。

## 2026-08-20 · v1.9.0-dev.55 物理容量口径与 10 GiB 验收状态

- 原生“Vibekits 正在启动”调整为窗口水平、垂直正中心。
- 修复空间分析进度把目录逻辑长度显示成磁盘占用的问题；Windows 组件存储硬链接会被多个路径重复遍历，进度栏现在只显示磁盘 API 的物理已用/总容量，目录列表明确标注不可相加。
- 清理工具栏和完成总结新增 `10 GiB 验收` 状态；只按实际累计释放量判断，未达到时显示还差多少，不再让候选量或逻辑扫描量冒充验收结果。
- 当前本机目标仍为 FAIL：较大剩余项包括 1.30 GiB Codex 运行时、0.95 GiB Gradle wrapper 和 5.89 GiB 可能含误移恢复文件的回收站，不能为凑容量默认删除。
- Cleaner Widget 11/11 通过。

## 2026-08-20 · v1.9.0-dev.54 即时启动与清理入口分工

- Windows 原生窗口在 Flutter Engine 和插件初始化前先显示轻量启动面；实机测得进程启动到窗口可见 171 ms，测试时窗口可响应。
- Flutter 首帧只构建导航、标题和状态栏骨架，当前工作区下一帧挂载；Harness 的凭据、端口和 DSH 初始化再延后一帧，不再挡住窗口出现。
- 清理入口改为“扫描可清理项”和“分析全部占用”：前者按规则寻找可清理候选，后者只读解释整个磁盘空间，两者空状态文案直接说明分工。
- 启动首帧测试、Cleaner 11 项定向测试通过，静态检查 0 问题，Windows Release 编译通过。

## 2026-08-20 · v1.9.0-dev.53 进程回收与空间归类对账

- Windows Runner 将自身及其创建的工具进程统一放入 `KILL_ON_JOB_CLOSE` Job Object；APP 退出后 Harness、ADB、Git、代理、虚拟机和 WebView 等继承子进程由系统统一结束，原有单进程绑定保留为受限启动环境下的兜底。
- 磁盘分析顶部直接显示“已归类/目录统计合计”和“未归类/系统保留”；遇到 NTFS 硬链接时明确显示重复量，并提示物理占用以磁盘“已用”为准，避免目录数字相加后误判。
- 软件占用列表新增安装路径；优先采用 Windows 卸载注册表路径，缺失时回退到空间扫描识别出的真实安装目录，并支持按路径搜索。
- Cleaner 定向测试 17/17 通过，静态检查 0 问题，Windows Release 原生编译通过。

## 2026-08-20 · v1.9.0-dev.48 APP 10 GiB 清理链路

- Windows 外部规则库升级为 v6/23 条，补充 Gradle/Pub/npm/Puppeteer/ccache、VS Code C++ 索引、企业微信升级包/诊断日志/可再下载小程序运行包、多个更新器缓存与 Codex 旧临时文件。
- Gradle、Pub、npm、pip 大型开发缓存的有界扫描上限提高到 100000 项，修复原 20000 项截断造成的大容量漏报。
- 新增 Windows 回收站容量/项数查询和明确确认后的无 UI 清空；修复其与系统盘 `.log` 清单共用盘符时被路径去重覆盖。
- Windows 删除改为每批 128 项调用 Shell 回收站，全部批次完成后才统一清空并复查剩余字节，避免最后一批移入时间晚于清空而伪报释放。
- APP 后台扫描链路在本机扫出 11.284 GiB 候选，安全验收计划约 11.28 GiB；28 项定向测试通过，`flutter analyze` 0 问题，Windows Release 成功。因计划要求不可恢复清空回收站，实际删除等待用户知情授权，未伪报 10 GiB 验收通过。
- 版本更新为 `1.9.0-dev.48+58`。

## 2026-08-20 · v1.9.0-dev.47 本机清理联调与工具列表可见性

- 在 C 盘仅剩 2.29 GiB 的真实机器运行最新版清理器，发现外部规则合并向固定长度列表 `addAll` 会抛异常；修复为后台 Isolate 内可变副本，并增加能稳定复现的回归测试。
- 快速清理扫描 44 秒完成：安全候选 3.121 GiB、需复核 1.014 GiB、共 4.134 GiB；原“系统盘全部 `.log`”默认规则超过 10 分钟仍未完成，因此从快速默认扫描移到显式深度清理，规则目录升级到 v10 并迁移旧默认配置。
- 真实只读审计进一步识别 Temp、Gradle、Pub、npm/Puppeteer、企业微信升级包/日志、VS Code C++ 索引、ccache 和回收站等约 9 GiB低风险/可再生成内容；达到 10 GiB还需用户明确批准永久删除企业微信约 1 GiB可重新下载小程序运行包。本轮因不可恢复删除审批未通过而未谎报释放量。
- “当前工具的 Harness 记录”移出左侧列表底部，改为右侧工作区顶部紧凑按钮，不再压缩或遮挡导航；“网络与虚拟化”入口改名为“Clash Verge / 虚拟机”，搜索 Clash 或虚拟机均能直接找到，内部仍用两个 Tab 融合操作。
- 版本更新为 `1.9.0-dev.47+57`。验收见 `docs/acceptance/V1_9_0_DEV47_LIVE_CLEANER_TOOL_VISIBILITY_2026-08-20.md`。

## 2026-08-20 · v1.9.0-dev.46 清理任务入口与外部规则库

- 将 360、Windows Storage Sense、BleachBit/CleanerML、dua-cli 的工作模型整理为 `24_CLEANER_COMPETITIVE_ARCHITECTURE.md`，明确只借鉴产品/算法模式，不复制专有实现；后续以软件身份、MFT/USN 增量索引、锁定进程、增长趋势和 Harness 计划为超越路线。
- 清理扫描结果从内部类别重组为“推荐清理、软件缓存、大文件 / 下载、不常用软件、深度清理”五个任务入口；技术类别仍保留在明细和审计中。没有可靠使用证据时只列已安装软件并调用官方卸载器，不伪造“不常用”判断。
- 新增随包 Windows 外部规则数据库 v5，首批覆盖 NuGet、Playwright、Hugging Face、PyTorch、Docker、Teams 与 Visual Studio 安装缓存。规则只允许有界扫描和回收站动作，畸形/危险规则逐条拒绝且不影响内置规则。
- 清理候选新增显式 `安全 / 需确认 / 系统管理` 风险和影响说明；谨慎/系统项默认不选，分类批量选择会跳过所有高风险项。
- 版本更新为 `1.9.0-dev.46+56`。轻量闭环结果和 Release 证据见 `docs/acceptance/V1_9_0_DEV46_CLEANER_ARCHITECTURE_2026-08-20.md`。

## 2026-08-20 · v1.9.0-dev.45 多磁盘占用与安全清理

- 清理页启动后在后台调用系统卷 API，列出当前终端全部可用本地、可移动、网络和内存磁盘；每个磁盘即时显示总量、已用、剩余和类型，默认只选系统盘，支持逐盘勾选、全选及刷新。
- “空间分析”改为“分析所选磁盘”，选中的卷依次在独立 Isolate 中扫描，每个卷内部保留 3 个有界 worker；扫描可取消，不阻塞 UI，也不会同时扫满多个物理盘。
- 多盘完成后显示每盘已用、剩余和安全可清理合计，可一键切换查看该盘的软件占用、普通数据、异常项和具体可清理目录。
- 数据盘的项目、媒体和普通用户目录只统计、不提供删除；只有明确命名或识别出的 Cache/Temp/Logs/CrashDumps 等可再生目录允许确认后移入回收站，未知目录也由原先可复核删除收紧为受保护。
- Windows 本机真实枚举 C/D 两卷通过；卷发现、数据盘删除边界、双盘勾选/分析/结果切换与原有清理页回归通过；版本更新为 `1.9.0-dev.45+55`。
- Windows Release 构建成功，文件/产品版本均为 `1.9.0-dev.45+55`；真实进程启动 6 秒后保持响应。
- 详细证据见 `docs/acceptance/V1_9_0_DEV45_MULTI_VOLUME_CLEANER_2026-08-20.md`。

## 2026-08-20 · v1.9.0-dev.44 软件占用与系统异常分析

- 系统盘分析不再只展示根目录：合并 Windows 三个卸载注册表视图与实际 `Program Files / ProgramData / AppData` 扫描，按软件聚合安装、软件数据、可再生缓存/日志和总占用。
- 软件列表默认按异常等级和占用排序，支持按软件名/发布者搜索；每项显示安装、数据、可清缓存、合理性判断以及体积未知的明确状态。
- 系统扫描现在单独产出 `Cache / Code Cache / GPUCache / Logs / CrashDumps` 等精确子目录；“清理缓存”只把确认可再生的用户/共享缓存移入回收站，不删除配置、数据库或用户文件。
- “卸载”读取 Windows 正式卸载命令并启动厂商/MSI 卸载器，安装目录保持保护；注册表三个视图并行加载，不阻塞 UI，系统盘扫描继续在独立 Isolate 中运行并可取消。
- 系统页显示剩余空间压力、Windows 20–40 GiB 启发式基线、各类别占用和需要关注的异常数量；Harness `vibekits.cleaner.analyze_drive` 同步返回 `softwareStorage` 结构化结果。
- 本机三个 Windows 卸载注册表视图并行识别 64 个软件，耗时 1213 ms；软件聚合、精确缓存归属、异常判断、回收站清理、正式卸载器和 Harness 工具桥定向回归 44/44 通过；版本更新为 `1.9.0-dev.44+54`。
- Windows Release 构建成功，自包含 22 项运行时通过；真实窗口 4843 ms 出现且进程可响应，文件/产品版本均核对为 `1.9.0-dev.44+54`。
- 详细证据见 `docs/acceptance/V1_9_0_DEV44_SOFTWARE_STORAGE_2026-08-20.md`。

## 2026-08-20 · v1.9.0-dev.43 全窗口点击与文件打开性能

- 一级导航不再因记录上次页面而通知应用根节点重建；高频页面、最近文件和工具状态改为 250 ms 合并后台写盘，并串行化配置保存。
- 15 个开发工具改为按需构建、访问后保留状态、仅布局当前页；返回 ADB、串口、SSH、虚拟化等页面时不再重复初始化，也不让全部隐藏页面参与每帧布局。
- 文件分类改为异步文件类型检查；已知文档只读取最多 512 B 验证伪装的压缩包/图片/SQLite，不再统一预读 36 KiB；文档加载先绘制进度反馈，内存内容不再重复访问文件元数据。
- 新增全点击性能矩阵，覆盖 5 个一级工作区、15 个开发工具、3 个关键弹窗、Markdown 窗口和 7 类文件路由；详细数据见 `docs/acceptance/V1_9_0_DEV43_CLICK_LATENCY_2026-08-20.md`。
- Windows Release 构建成功，自包含 22 项运行时通过；冷启动主窗口 5454 ms 出现且进程可响应，文件/产品版本均为 `1.9.0-dev.43+53`。
- 版本更新为 `1.9.0-dev.43+53`。

## 2026-08-20 · v1.9.0-dev.42 Android arm64 真机检查点

- 新增 Android 工程、INTERNET 权限和可复现构建环境准备脚本，固定 JDK 17、Android platform/build-tools 36 与 NDK 28.2。
- 修复 Windows 跨盘 Kotlin 增量缓存错误和 sqlite3 arm64 原生资产构建阻塞；Release APK 成功产出。
- Android 首次启动避开桌面专属 DSH Web 入口，直接进入可操作的开发工具；用户手动切换和页面恢复逻辑仍保留。
- 使用项目随包 ADB 在 `192.168.3.63:5555` 真机完成安装、冷启动、热恢复和程序员计算器实际输入；未发现 FATAL/ANR。
- 版本更新为 `1.9.0-dev.42+52`；详细证据见 `docs/acceptance/V1_9_0_DEV42_ANDROID_DEVICE_2026-08-20.md`。

## 2026-08-20 · v1.9.0-dev.41 网络代理与轻量虚拟机

- 开发工具新增“网络与虚拟化”融合工作区，右侧使用 Clash Verge/轻量虚拟机两个 Tab；Clash 配置、虚拟磁盘和 ISO 均使用原生文件选择器，启动与停止不阻塞 UI。
- 代理没有复制另一套 Web UI，而是内置 Clash Verge 使用的 Mihomo 1.19.29 内核；构建准备阶段验证 SHA-256，Release 运行时不联网下载，也不依赖机器上安装 Clash Verge。
- QEMU 采用官网 Windows 下载页列出的 11.1.0 x64 构建，仅提取 x86_64 系统模拟、qemu-img、必需 DLL 与固件。准备阶段校验上游 SHA-512，发布门禁检查可执行文件与来源清单。
- 新增六个 Harness 工具：运行时检查、运行状态、代理启动/停止、虚拟机启动/停止。参数经结构化合同传递，禁止任意命令行；真实 PID、退出码和有界 stdout/stderr 进入现有模块日志。
- 系统代理和 TUN 尚未自动开启：二者会修改操作系统网络，需要后续实现“保存旧值—批准—应用—恢复”的专用闭环，当前启动 Mihomo 只启动本地内核并明确说明边界。
- 版本更新为 `1.9.0-dev.41+51`。

## 2026-08-20 · v1.9.0-dev.40+50

- 建立 Harness 脱敏工作状态投影，工具审批前、执行中、成功和失败都实时更新。
- 新增 RustDesk 远程分享入口：发现已安装客户端、读取本机 ID、自动推导 Web Client、启动官方主机和打开网页端。
- 新增统一开发对象和“下一步建议”，通过 `ToolSpec` 自动供 Harness 调用。
- 真实验证本机 RustDesk `--get-id` 和自建服务器 `/web` HTTP 200；不记录远程密码或完整设备 ID。
- 详细证据见 `docs/acceptance/V1_9_0_DEV40_RUSTDESK_WORK_STATUS_2026-08-20.md`。

## 2026-08-15

### P0 设置闭环

- 将占位设置页替换为可操作表单：系统/浅色/深色主题、恢复上次 Tab、日志级别、缓存上限、模型目录和解压安全限制。
- 设置以临时文件 + 原子改名保存到 `%LOCALAPPDATA%\Vibekits\settings.json`，损坏或非法字段安全回退默认值。
- 自定义模型目录已接入本地模型列表/导入/删除；解压最大条目数与单文件展开上限已接入压缩包预检。
- 自动测试增至 103 项；`flutter analyze` 无问题，103/103 测试通过，Windows Release 构建通过。

### P0 Windows 清理任务闭环

- 扫描器新增协作式取消令牌和 100ms 节流进度，界面显示当前路径、已检查项、候选数量与累计大小；取消后保留部分结果且不删除文件。
- 清理从批量布尔结果改为逐项复核与执行，文件在扫描后消失或变化时跳过，成功/跳过/失败和实际释放字节分别统计，取消不记为失败。
- 清理确认页显示最终项目数、大小与回收站可恢复提示；完成页提供报告入口。
- JSON 报告写入 `%LOCALAPPDATA%\Vibekits\Reports`，不持久化候选路径与文件内容；报告写入失败不会覆盖真实清理结果。
- `flutter analyze` 无问题，108/108 自动测试通过，Windows Release 构建通过。

### P0 清理白名单持久化

- 白名单接入统一设置文件，应用重启后恢复；管理对话框改为系统目录选择，支持查看和移除已有项目。
- 路径统一绝对化、分隔符规范化并按 Windows 大小写无关语义去重；边界匹配避免 `C:\Keep2` 被 `C:\Keep` 误保护。
- 扫描结果过滤和清理计划生成都会应用白名单；运行期间设置变化时立即移除受保护候选及其选择状态。
- `flutter analyze` 无问题，111/111 自动测试通过，Windows Release 构建通过。

### P0 清理多范围与文件身份

- 扫描范围扩展到用户/Windows 临时目录、Edge/Chrome/Firefox 缓存和用户崩溃转储；Windows 临时目录默认关闭。
- 候选默认选择遵循风险规则：浏览器缓存和转储默认选择，用户临时文件仅选择超过 24 小时的项目，高风险项不默认选择。
- 用户选择的扫描范围写入统一设置，至少保留一个有效范围。
- 通过 `CreateFileW + GetFileInformationByHandle` 记录卷序列号和文件 ID；同路径替换且大小/修改时间相同时仍能识别并跳过。

### P0 解压任务与安全闭环

- ZIP/TAR/单流格式使用 64KB 分块写入、同目录 `.part` 临时文件和完成后改名，界面显示当前文件、比例、写入量、速度和取消。
- 覆盖策略通过 Windows `ReplaceFileW` 原子提交已完成临时文件，不再先删除旧目标。
- 7z 改为安全暂存：条目路径预检后写入隔离目录，再执行冲突策略；可终止外部进程并清理暂存目录。
- 四种冲突策略全部接入，包括逐文件询问；文件头识别优先于扩展名。
- 修正 TAR.GZ/TGZ、TAR.BZ2/TBZ2、TAR.XZ/TXZ 的解码路径，加入总展开大小、压缩比和 Windows 磁盘空间预检。
- 创建界面补齐 ZIP/TAR/TAR.GZ 格式选择和目录打包；目录不跟随链接，保留相对结构并执行输入限制。
- `flutter analyze` 无问题，127/127 自动测试通过，Windows Release 构建通过。

### P0 模型管理安全闭环

- 强制单模型 100MB 上限，导入先写 `.part`，超限或失败不进入模型目录。
- SHA-256 改为文件流式计算；导入成功后保存文件大小和完整哈希清单。
- 每次列表重新校验，界面显示校验通过、文件已变化或未登记；清单损坏时安全降级，不隐藏模型。
- 自动测试增至 129 项。

## 2026-08-16

### 科技风响应式界面重构

- 主窗口升级为宽屏侧边工作台，并在小于 1180px 时自动切换为紧凑顶部导航；1024×700 下保留完整内容宽度。
- 新增浅色/深色语义调色板，统一面板、边框、正文、次级文字、输入框、按钮、滚动条和状态反馈。
- 顶栏增加当前模块说明、本地运行状态和集中任务入口；内容区采用单层主面板，减少视觉噪声。
- 五个业务模块移除写死的浅色背景与文字颜色，深色模式可完整渲染。
- 新增宽屏、1024×700 和深色主题 Widget 回归测试。
- `flutter analyze` 无问题，132/132 自动测试通过，Windows Release 构建通过。

### 融合入口、文件关联与真实缓存清单

- 建立统一支持格式清单；压缩选择器和 Windows 注册仅声明 ZIP/TAR/GZ/BZ2/XZ/7z 等真实可打开格式，不再把仅能识别的 RAR/ISO 伪装为已支持。
- Release EXE 使用当前用户注册表接入 Windows“打开方式/默认应用”，不抢占默认程序；从资源管理器启动时自动路由到解压缩或文档模块。
- 文本解码扩展到 Dart/JS/TS/Python/C/C++/C#/Go/Rust/PowerShell/SQL 等源码脚本，以及 `.env`、`.gitignore`、Dockerfile 等开发配置；继续复用 UTF-8/UTF-16/GBK/GB18030/Big5 解码链。
- 增加 `Ctrl+O` 当前模块打开和文档 `Ctrl+F` 查找，保留现有 `Ctrl+1~5`、`Ctrl+,`。
- 清理范围增加 DirectX 着色器、Windows 错误报告、VS Code/Discord、npm/pip/Gradle 和更多 Chromium 缓存；开发缓存默认关闭。
- 清理结果改为可展开类别，逐项显示真实来源、文件名、完整路径、大小和修改时间。
- 增加 `tool/register_file_types.dart`，供安装阶段或开发验证时无界面写入当前用户文件关联；已核对 `.dart`、`.zip` 注册存在且未注册当前不能解码的 `.rar`。
- `dart format lib test tool` 无改动，`flutter analyze` 无问题，137/137 自动测试通过，Windows Release 重新构建成功。

### 智能清理、拖拽路由与 Windows 右键闭环

- 清理新增 NuGet、Maven、Cargo、Go、Android、Pub、Yarn、pnpm、完整 npm、VS Code 插件下载缓存，以及超过 24 小时的已知调试临时目录；默认扫描范围通过目录版本迁移自动补齐，开发/下载/旧插件候选仍遵循高风险默认不选。
- 下载目录只建议未完成下载和超过 30 天的安装包/压缩包；VS Code 插件只在同一标识存在多个版本时列出旧版本内容，当前最高版本不进入候选。
- 删除器返回 Windows Shell 错误码和占用/权限建议；确认页可对浏览器、应用、开发、插件下载及调试缓存启用永久删除兜底，绝不用于下载建议和旧插件。
- 万级候选按类别、大小排序，超过 100 项默认折叠并分批加载；扫描范围按类别分组，提供推荐、全部、清空预设。
- 当前电脑最终只读实扫发现 26 个范围、17033 项、约 2.11GB，其中插件下载缓存约 423.7MB、开发缓存约 1.53GB；4 个不可读位置被真实计数。
- 生成的隔离缓存完成真实 Windows 回收站删除和强力清理闭环；未触碰真实用户缓存。
- Windows Runner 接入 `WM_DROPFILES`，拖入文件后跳过目录/未知格式，只通知唯一最佳工具；重复拖入通过请求序号重建对应工作区。
- Windows Runner 使用当前用户命名互斥体和 `WM_COPYDATA` 实现单实例文件转发；Release 实测第二进程携带文件启动后以代码 0 退出，系统仅保留一个同路径实例。
- 为所有真实支持扩展名注册 `SystemFileAssociations` 右键入口：压缩文件显示“用 Vibekits 解压或查看”，文档/源码显示“用 Vibekits 查看或解码”；`.zip` 与 `.dart` 已实查注册表命令。
- 移除本地模型页尚无运行时支撑的 OCR/ASR/TTS 假工作区，只保留真实模型管理；永久删除模型前增加二次确认。
- `flutter analyze` 无问题，145/145 自动测试通过，Windows Release 原生编译通过。

## 2026-08-16

### DEV-004 批量重命名闭环与版本升级

- 将通用文本框式“批量重命名预览”替换为专用工作区：原生目录选择、查找替换、前后缀、大小写、序号和即时双栏预览。
- 规则默认保留扩展名，也允许用户明确查找扩展名进行转换；仅处理当前目录文件，不递归修改子目录。
- 写入前检查 Windows 非法字符、保留名称、路径长度、已有目标和多个来源指向同名；存在问题时主按钮不可用。
- 确认后采用“源文件 → 唯一临时名 → 目标名”两阶段执行，避免连环目标占位；失败恢复也采用两阶段，保留可恢复结果。
- 自动测试在隔离临时目录中完成真实改名、内容保持、临时文件清理和故障注入恢复；Widget 测试覆盖选择目录、预览、确认和执行。
- 移除状态栏虚假的 CPU/内存占位，界面只显示真实版本和本地模式；应用版本升级为 `1.1.0+2`。
- `flutter analyze` 无问题，149/149 自动测试通过。

### M6 重复文件完整闭环

- 新增“重复文件”专用工作区，选择文件夹后可设置最小文件大小和是否包含子文件夹，扫描支持进度与取消。
- 扫描先按文件大小预筛，只对同大小候选流式计算完整 SHA-256；相同物理文件的硬链接去重，唯一大小文件不读取文件身份或内容哈希。
- 结果按可释放空间排序，每组标记“建议保留最新”，默认不勾选；“选择每组建议项”只批量选择，其后仍需二次确认。
- 删除复用清理模块，执行前再次核对文件身份、大小和修改时间，只移入 Windows 回收站，不提供永久删除；匿名 JSON 报告不记录私人路径。
- 自动测试覆盖大小预筛、完整哈希、递归开关、取消、分组复核、选择、确认、删除和报告；生成文件完成真实 Windows 回收站验证，保留副本内容完整。
- 修复窄窗口下最小文件下拉框溢出、完成信息重复显示，以及报告保存失败遮蔽删除结果的问题。
- 应用版本升级为 `1.2.0+3`；`flutter analyze` 无问题，154/154 自动测试通过。

### v1.3.0 全文件自动处理、文件哈希与首个本地推理闭环

- 修复 Markdown 预览空白：移除嵌套滚动布局，使用单一滚动区承载 `MarkdownBody`；Markdown 每次打开默认进入可读预览，源码仍可一键切换。文档工具栏在窄宽度隐藏次要编码/换行控件，避免按钮看得见却点不到。
- 拖入与系统启动参数统一走内容路由：所有文件逐项给出结果，多文件保留完整清单；压缩包按文件头优先，已知文档按格式打开，模型交给模型仓库，未知文件按首 64KiB 自动选择文本或 Hex，文件夹/链接/读取失败明确显示原因，不再静默丢弃。
- Windows 任意文件右键增加“用 Vibekits 自动处理”；模型格式显示“导入到 Vibekits 本地模型”。右键多选采用 `Player` 模式，120ms 聚合多个单实例转发事件，避免只保留最后一个文件。
- “文件哈希”由通用文本路径框升级为专用工作区：选择多个文件后自动计算，默认 SHA-256，可切换 MD5/SHA-1/SHA-512；使用分块流式读取，显示逐文件进度，支持取消、重新计算、逐项复制和复制全部。
- 接入 `sherpa-onnx 1.13.5` Windows CPU 运行时与精选模型清单；下载由用户明确触发，显示进度并可取消，安装前核对固定 SHA-256，失败文件不进入模型目录。
- 固定并测试两款 MIT Silero VAD ONNX：Sherpa 兼容版 643,854 字节（SHA-256 `9e2449e1…1fd6`）和 v6 2,327,524 字节（提交 `76e3dc4`，SHA-256 `1a153a22…88e3`）。两者均在独立 Windows x64 测试进程中对官方 `lei-jun-test.wav` 完成真实离线推理。
- 模型页改为“已安装 + 精选小模型 + 语音片段检测”工作台；宽屏左右分栏，窄屏使用“模型/运行”页签。VAD 输入为 16kHz WAV，结果显示片段起止、时长、总人声时长、耗时和运行时版本，全程不上传音频。
- 首次加载原生运行时遇到 Windows 错误 193；确认 DLL 为 x64 后定位为依赖搜索路径问题，显式预加载同目录 `onnxruntime.dll` 后真实推理通过。
- 应用版本升级为 `1.3.0+4`。

## 项目级记录

### 决策记录

| 决策 | 结论 | 依据 |
|---|---|---|
| 技术路线 | Flutter UI + Windows 原生能力（libarchive/ONNX 等 C/C++） | 一套 UI 双端（Windows/Android），低内存 |
| 交互原则 | 每个界面对标一款主流应用，不自创操作习惯 | `08_WINDOWS_UX_CONVENTIONS.md` |
| 文档阅读格式 | 25 个轻量后缀 + 未知格式手动文本/Hex；排除 Office/PDF | 参考 kemi-office，`06` §5 |
| 开发顺序 | M0 骨架 → M1 工具 → M2 文档 → M3 结构化 → M4 解压 → M5 清理 → M6 网络/文件 → M7 模型 → M8 发布 | `03` 实施计划 |

### 环境

- Flutter 3.47.0 stable / Dart 3.13.0，位于 `D:\tools\flutter`（克隆自 GitHub，初始下载极慢，已完整检出 `4cf24164`）。
- Visual Studio Build Tools 2022 17.14.37 安装到 `D:\VSBuildTools`（MSVC 14.44.35207、CMake）。Windows SDK 按其固定位置装在 C 盘 `Windows Kits\10`。
- `flutter doctor`：VS/Chrome/Windows ✅，Android cmdline-tools 缺失（Windows 首版不受影响），Flutter 未入 PATH（以全路径调用）。

### 里程碑状态

| 里程碑 | 状态 | 备注 |
|---|---|---|
| M0 环境与骨架 | ✅ 完成 | 响应式侧栏/顶部导航、快捷键、深浅主题、状态栏；Debug/Release 构建通过 |
| M1 开发工具 | ✅ 首批完成 | 22 个离线工具；46/46 测试通过；DEV-001~003、APP-001/003 自动通过 |
| M2 文档阅读 | ✅ 完成 | 文本/日志/Markdown/配置/Diff/Bin 查看器；GB18030/Big5 解码、大文件流式索引 |
| M3 结构化阅读 | ✅ 完成 | CSV/TSV 表格、JSON/XML 树、HTML/EPUB/SVG 安全渲染 |
| M4 解压缩 | ✅ 完成 | zip/tar/tgz/gz/bz2/xz/7z 列表/解压/创建、路径穿越防护、冲突策略 |
| M5 Windows 清理 | ✅ 完成 | 临时目录扫描、三态选择、白名单、回收站删除（FFI） |
| M6 网络/文件工具 | ✅ 完成 | HTTP/DNS/端口/CIDR、文件哈希、批量重命名、重复文件 |
| M7 本地模型 | 🔄 首个推理闭环 | 模型导入/下载/哈希校验/删除；Silero VAD 双模型真实 Windows 离线推理通过，OCR/ASR/TTS 待接入 |
| M8 发布 | 🔄 待人工验收 | 169/169 自动测试、双 VAD 真推理、Release 构建/双参数启动及当前用户文件关联实测通过；正式 UI 截图与原生拖拽证据待补 |

### 已解决问题

1. `flutter.bat.lock` 被残留进程占用 → 终止残留 dart/git 进程并删除锁。
2. flutter test 紧凑报告器在窄终端崩溃（RangeError）→ 用 `--reporter expanded`。
3. `ShortcutActivator/SingleActivator` 属于 `widgets`，`LogicalKeyboardKey` 属于 `services` → 分别导入。
4. `crypto` 顶层函数与静态方法重名 → 加 `crypto.` 前缀。
5. `Uint8List` 缺 `dart:typed_data` 导入。
6. Tab 项窄宽度溢出 → 文本 `Flexible + ellipsis`。
7. C++ 源码中文注释触发 MSVC C4819（代码页 936）→ 原生代码注释改 ASCII。
8. 窗口默认 1280×720/标题小写 → 改为 1280×800/`Vibekits`，新增 `WM_GETMINMAXINFO` 最小 1024×700。
9. `charset_converter` 走 MethodChannel（异步且依赖原生插件，无法单元测试）→ 移除；GBK 用纯 Dart `fast_gbk`，GB18030/Big5 留待原生层接入。
10. `flutter_markdown` 已停更 → 改用 `flutter_markdown_plus`。
11. `csv` 8.0 移除 `CsvToListConverter` → 改用 `CsvDecoder`；`xml` 7 的 `XmlText.text` 弃用 → 改用 `value`。
12. Dart RegExp 不支持 `(?i)`/`(?s)` 内联标志 → 改用构造函数 `caseSensitive/dotAll`。
13. `archive` 4.0.9 的 xz 解码器类名为 `XZDecoder`。
14. 回收站删除经 `SHFileOperationW` FFI 实现（结构字段用 `Pointer<Void>` 而非 `IntPtr`）。
15. 7z/rar/iso 与 GB18030/Big5 需原生层，纯 Dart 无法可靠实现，已明确标注为“待原生层接入”。
16. GB18030/Big5 解码 → 新增 `NativeCodec`（kernel32 `MultiByteToWideChar` FFI，cp 54936/950/936），补齐编码硬限制。
17. 大文件文本读取（>64MB 内存溢出）→ 新增 `FileLineIndex` 流式行索引（RandomAccessFile 64KB 块扫描）+ 按需读行解码，文档 Tab 走 `_loadStreaming` 分支。
18. 7z 列表/解压 → 下载 `7za.exe` 到 `native/7za/`，新增 `SevenZip`（`7za l -slt` 解析 / `7za x`），ArchiveTab 增加 7z 条目渲染与选择。

### 已知限制

- 复制/保存、缩放 150%/200% 可读性待人工复核。
- 网络工具、文件工具属 M6；本地模型属 M7。
- Android 端 cmdline-tools 未装，后续做平板时补。
- rar / iso 仍受限：7za 不内置 rar 解压与 iso 挂载，需引入 unrar 或 7z 完整版（含 7z.dll）后接入。
- OCR/ASR/TTS 推理仍待接入；sherpa-onnx/ONNX Runtime 和两款小型 VAD 已接入并通过真实推理，模型由用户主动下载，测试模型不随 Release 强制打包。

## 约定

- 每次里程碑结束更新本文档与 `docs/acceptance/M<n>_<date>.md`，并同步 `04/07` 验收矩阵状态。
- 遇到技术卡点记录到“已解决问题”，不静默绕过。

## 2026-08-16 · v1.4.0 文件融合与程序员工作区

- 版本升级为 `1.4.0+5`。程序员计算器、SQLite 只读数据库管理、源码编辑、SSH/SFTP/端口转发、完整 API 请求、Git 只读工作区和 GitHub 安全诊断均进入统一开发工具页，不增加首页一级模块。
- Markdown 默认渲染预览；源码保留 BOM/编码并用外部修改复核和原子替换保存。`.bin` 超过 256MiB 时切换到固定 1MiB 窗口，支持 64 位偏移、十进制/Hex 跳转和跨块文本/字节搜索，2GB+ 稀疏文件通过。
- 接入官方 PP-OCRv6 tiny 检测/识别/字典模型，三资产完整校验后事务安装；官方登机牌样图真推理得到文本块、坐标和置信度。图片预览补齐八种 P0 编码矩阵、首帧解码、EXIF 方向、256MiB/1 亿像素预算和明确失败回退。
- 压缩后端升级为官方 7-Zip 26.02 `7z.exe + 7z.dll`，RAR5/ISO/ZSTD 等真实夹具通过；不提供 RAR 创建，不绕过统一路径、链接、展开规模、空间、冲突和暂存安全合同。
- 清理补齐 Visual Studio、JetBrains/Android Studio、Cursor/Windsurf、Xcode/SwiftPM/Homebrew/CocoaPods 已知缓存；pnpm 范围从整个根目录收紧到 store。下载与可证明旧插件保持高风险默认不选，不扫描项目依赖或源码目录。
- 创建 macOS Flutter 工程，加入 `public.data` Open With、Swift 文件事件排队/Dart 就绪握手、macOS 模型目录、`~/.Trash` 可恢复移动和 Command 主快捷键。因当前为 Windows 主机，Xcode/arm64 Release 与 macOS ONNX 仍明确待实机。
- 最终 `flutter analyze` 无问题，226/226 全量测试通过；Windows Release 构建成功并携带 Markdown 启动 5 秒保持运行。发布目录核对 ONNX 桥、ONNX Runtime、SQLite 和 7-Zip 完整，当前用户文件关联/右键命令实查通过。
- 供应链和验收细节见 `16_THIRD_PARTY_COMPONENTS.md` 与 `acceptance/V1_4_0_FUSION_TOOLS_2026-08-16.md`。本节取代上文旧里程碑中“RAR/OCR 尚未接入”的历史限制描述。

## 2026-08-16 · v1.5.0 体验、清理、远程数据库与 Harness

- 统一需求重新收敛为“极简融合入口 + 高频主工作区 + 搜索/上下文微工具”，明确清理、计算器、数据库、远程、网络、文件搜索、截图、本地模型和 DeepSeek Harness 的唯一入口与完成定义。
- 清理扫描和删除移入独立 Isolate，I/O 主动让出；扫描/清理均可取消。完成后立即显示本次、累计次数/容量、系统盘总容量/可用/已用及成功/跳过/失败，报告异步写入不再阻塞价值反馈。
- 文档最近打开记录跨重启保存，侧栏一键清空；保存失败只提示，不影响本次阅读。程序员计算器重做为 Windows Programmer 操作模型，支持进制行、位宽、A～F 启停、位运算/移位/循环移位及键盘输入。
- 数据库工作区接入 PostgreSQL TLS、对象列表、100 行分页、500 行查询上限和只读 SQL；连接资料持久化，密码直接进入 Windows Credential Manager/macOS Keychain。因第三方安全存储插件要求额外 ATL，改为轻量系统适配；Windows 真凭据写入/读取/删除通过。
- PP-OCRv6 tiny 与两款 Silero 模型成为 Release 内置安装资源。OCR 安装抽成纯离线服务：隔离暂存、逐文件 SHA-256、全部通过后原子导入，断网不影响首次演示。
- 接入 DeepSeek 官方 Harness 开发者预览：固定 `@deepseek-ai/dsh@0.1.0-rc.5`，提供 Node/npx 环境检查、项目选择、启动、就绪地址识别、自动打开控制台、日志和停止进程树。当前网络下载探测超过 2 分钟无输出后取消，因此仍标记“适配完成、官方包实启待证据”。
- 首次 Release 构建发现 `flutter_secure_storage_windows` 缺 ATL；移除后第二次构建被旧 EXE 进程锁定；确认并结束本项目测试实例后第三次构建成功。最终版本 `1.5.0+6`，Analyze 无问题，234/234 测试通过，Release 携带 `README.md` 隐藏启动 5 秒未提前退出，内置模型/ONNX/SQLite/7-Zip 产物核对完整。

## 2026-08-16 · v1.6.0 极简工具集合、截图 OCR 与应用内智能体

- 开发工具左侧删除 Base64、JSON、时间、正则、网络查询等同构碎片入口，只保留需要独立任务流的计算器、数据库、远程、API、Git 和文件工作区。微工具统一进入“转换与检查”，右侧以类别 Tab + 紧凑选项切换，共享输入/输出、复制、清空和“结果作为输入”；搜索具体工具名称仍会自动定位。
- 模型页重新设计为仅有“截图 OCR”和“DeepSeek 智能体”两个入口；本地模型清单与安装移入次级管理对话框，VAD 兼容运行时保留但不占首屏。
- 新增 Windows/macOS 原生区域截图适配。Windows 清空剪贴板后打开系统区域截图并等待新图片，macOS 使用 `screencapture -i`；完成后自动预览并立即进入现有 PP-OCRv6 tiny 推理，取消不复用旧截图。
- DeepSeek Harness 从浏览器控制台入口升级为应用内 Codex 风格任务流：固定官方 RC5 headless profile，选择并持久化项目目录，底部输入任务，stdout/stderr 流式展示，进程结束立即恢复输入，运行中可停止完整进程树。API 密钥仍由 Harness 官方配置管理。
- 版本升级为 `1.6.0+7`。核心 Widget 回归覆盖左侧收敛/搜索定位、截图一次即 OCR、智能体参数/流式输出/结束恢复；Analyze 无问题，235/235 全量测试通过，Windows Release 构建成功，EXE 携带 README 隐藏启动 5 秒保持运行。双远端备份结果见本版验收记录。
- 实现提交 `593e6a9` 已同步本地 `backup` 与 GitHub `cloud`；最终发布记录提交和 `v1.6.0` 标签在同一批次继续同步。

## 2026-08-16 · v1.6.1 全界面复核与 Codex 风格持续会话

- 逐项复核解压缩、系统清理、文档阅读、开发工具和本地模型五个工作区，补充 1024×700 最小窗口逐页切换回归；移除没有真实任务价值的“任务 0”入口，统一“系统清理”命名，并把状态文案改为“文件默认本机处理”，避免把联网 API/智能体误写为全离线。
- DeepSeek 智能体按成熟桌面代理习惯升级为持续会话：空状态任务建议、工作区和环境状态、新任务、`Enter` 发送/`Shift+Enter` 换行、流式进度、停止、Markdown 回复、复制和耗时/退出状态。后续任务携带有界历史上下文，切换到 OCR 再返回仍保留会话和运行状态。
- 压缩空状态同步真实后端能力，明确展示 RAR、ZIP、7z、TAR、ISO、WIM、DMG；修复应用内版本显示与 `pubspec.yaml` 脱节，正式版本更新为 `1.6.1+8`。
- `flutter analyze` 无问题；DeepSeek/主界面定向回归 22/22 通过；全量 `flutter test --reporter expanded` 238/238 通过。Windows 正式 Release、真实启动、产物哈希和 Git 双远端结果见 `acceptance/V1_6_1_UI_REVIEW_CODEX_RELEASE_2026-08-16.md`。

## 2026-08-17 · v1.7.0 文件搜索与扫描响应性

- 新增文件搜索独立工作区。默认只需选择目录、输入关键词、按 Enter；文件名/内容模式直接切换，类型、最小大小、修改时间、隐藏项和递归收进折叠筛选。内容读取限制为单文件 8 MiB，拒绝符号链接，结果上限 500。
- 搜索结果提供定位、复制路径和“计算 SHA-256”；进入文件哈希后自动携带文件并立即计算，不再重复选文件。1024×700 无溢出，正常、无结果、截断、隐藏项、内容行号、取消和工作区销毁均有测试。
- 针对“扫描时窗口无法拖动/关闭”的反馈完成线程审计：清理目标发现、文件搜索、文件哈希、重复文件扫描/删除、批量重命名规划/执行、纯 Dart 压缩读取/创建全部迁出 UI Isolate。原有清理扫描/删除、数据库、图片和模型推理保持 Isolate；7-Zip、Git、SSH、DeepSeek 保持独立进程。
- 工作 Isolate 使用独立取消端口；搜索/哈希/重复文件进度约 100 ms 限频，避免小文件或 64 KiB 分块产生的消息风暴继续占满 UI。重复文件删除新增“停止处理”，关闭文件搜索工作区 1 秒内完成取消回归。
- 版本升级为 `1.7.0+9`。`flutter analyze` 无问题；全量测试、Windows Release、真实启动与双远端提交见 `acceptance/V1_7_0_FILE_SEARCH_RESPONSIVENESS_2026-08-17.md`。

## 2026-08-17 · v1.8.0 MySQL/MariaDB 与后台串口终端

- 数据库工作区新增 MySQL/MariaDB，与 PostgreSQL 共用一个极简连接对话框；切换类型自动给出 3306/5432 等主流默认值。对象浏览、100 行分页、最多 500 行只读 SQL、TLS、最近连接、系统安全凭据与删除记录均复用同一交互。
- 远程数据库每次操作进入短生命周期 Isolate，15 秒超时或用户点击“停止”会杀掉工作 Isolate 与连接；SQL 防护拒绝多语句、写入和管理关键字。没有在 UI Isolate 内等待 socket。
- 新增串口调试独立工作区：自动枚举/手工端口、可编辑波特率、数据位、校验、停止位、流控、文本/HEX、CR/LF/CRLF、发送历史、带时间/方向日志、清空和后台保存。高级参数默认折叠，首屏只需端口、波特率和打开。
- 串口原生会话由独立 Isolate 持有，接收数据以 100ms 聚合刷新，最多保留 2MiB/2000 条；关闭先恢复界面，再后台释放句柄。macOS 工程增加串口设备权限声明。
- 首轮全量测试暴露“原生端口快速失败早于测试挂错误处理器”的竞态，修正测试时序并补统一工具入口后，串口/数据库定向与顶层入口均通过，全量 271/271 通过。测试过程中还识别并终止了 5 个历史遗留、无窗口且长期高 CPU 的 `flutter.bat test` 批处理进程；后续测试必须允许 Flutter 写 SDK 缓存，避免批处理在锁前空转。
- 当前机器未枚举到物理 COM 设备，也未安装 MySQL/MariaDB 服务；因此本版只把原生资产加载、端口枚举/失败、UI 模拟收发、真实 TCP 握手等待/取消写成已验证，真实硬件回环和真实数据库成功连接明确留待补证。
- 版本升级为 `1.8.0+10`；正式 Release、产物核对、启动冒烟和 Git 双远端结果记录在 `acceptance/V1_8_0_MYSQL_SERIAL_2026-08-17.md`。

## 2026-08-17 · 发布清单与原子验收动作收敛

- 将历史 Windows 功能/UI 矩阵降为用例库，新增 `17_RELEASE_COMPLETION_CHECKLIST.md` 作为唯一当前范围和研发顺序，明确区分软件基线、待实证、待研发和后续增强。
- 新增 `18_ATOMIC_ACCEPTANCE_CASES.md`。剩余 P0 被拆成固定前置、编号动作、唯一预期、失败判定和证据；禁止用“正常、好用、流畅、完整、基本可用”等主观词判定。
- 格式、图片、数据库、缩放和工作区等重复场景使用固定参数表，每个参数生成独立记录，禁止汇总成“部分通过”。真实串口、数据库、DeepSeek、清理并发和 macOS 证据均不能由模拟测试替代。
- 本轮只重构文档和验收合同，不修改 v1.8.0 应用二进制，不虚增产品版本号；下一研发动作固定为 `REM-101-A`。

## 2026-08-17 · R9 远程会话与交互终端检查点

- 远程会话资料接入统一设置：新增、编辑、收藏、最近使用、重启恢复和删除；密码/私钥口令只进入 Windows Credential Manager 或 macOS Keychain，普通 JSON 明确不编码敏感字段。
- SSH 从普通 OpenSSH 管道升级为 `dartssh2` 远程 PTY + `xterm.dart` 终端渲染。首次连接必须确认 OpenSSH SHA256 主机指纹；已绑定指纹自动核对，变化时显示高风险提示，不启用自动信任。
- 终端支持多标签输出隔离、窗口尺寸同步、搜索计数/首项高亮、清屏、单行直接粘贴和多行发送前预览确认。连接阶段可立即取消，迟到的连接建立后自动关闭。
- REM-101/REM-102 新增记录模型、凭据隔离、指纹、安全粘贴、多标签和取消 Widget/单元测试；`flutter analyze` 无问题，全量自动测试 282/282 通过。
- 源码版本更新为 `1.9.0-dev.1+11` 作为可恢复检查点；R9 退出条件未完成前不创建 `v1.9.0` 正式标签。
- 用户新增 ADB 必须能力已写入产品需求、发布清单和 ADB-106-A～H 原子验收：官方 Platform-Tools、USB/无线调试、Shell、文件、Logcat、截图及 DeepSeek 逐项审批。当前只完成需求入账，尚未声明代码完成。

## 2026-08-17 · R9 SFTP 双栏检查点

- 在同一远程会话资料和主机指纹验证上接入 SFTP，不新增重复连接入口；密码/私钥口令仍只从系统凭据读取。
- 文件工作区采用本地/远端双栏：目录刷新、选择、箭头传输与双向拖放共用同一状态机；同名文件只提供覆盖、重命名、跳过、取消，默认焦点为“跳过”。
- 上传先写远端随机临时文件再改名；下载先写本地 `.vibekits.part` 再改名。传输显示字节进度，可取消；取消和失败清理临时文件，失败保留重试入口。
- 修复 Widget 测试夹具在虚拟异步时钟中等待真实异步文件 I/O 导致的假卡死，改用同步夹具后列表/上传下载、冲突默认动作、取消 3/3 通过；相关 6 个文件定向 Analyze 无问题。
- 源码版本更新为 `1.9.0-dev.2+12`。REM-103 软件主路径完成，真实 SSH/SFTP 服务端的哈希、断网和大文件证据保持待实证；下一代码项为 REM-104 端口转发管理。

## 2026-08-17 · R9 后台端口转发检查点

- 将旧的单条本地转发表单重做为转发列表：同一远程资料下支持本地转发、远程转发和 SOCKS5 动态转发，可连续添加、逐条停止或全部断开。
- 本地与 SOCKS5 固定监听 `127.0.0.1`，远程转发固定请求服务端 `localhost`，不因默认值把端口暴露到局域网或公网；端口占用直接显示具体端口且不新增运行项。
- 整条 SSH 连接、监听 socket、通道桥接和数据泵均运行在专用后台 Isolate。UI 只发送添加/停止命令和接收状态，关闭工作区会关闭全部通道并立即终止后台 Isolate。
- 参数、安全边界与后台 Isolate 失败释放单元测试 3/3 通过；转发表 Widget 用例覆盖三种类型、端口占用、逐条停止和全部断开；相关 4 个文件定向 Analyze 无问题。
- 源码版本更新为 `1.9.0-dev.3+13`。REM-104 软件主路径完成，真实 SSH 服务端的端口访问、冲突计时和关闭后端口/进程快照仍待实证；下一代码项为 REM-105 系统远程桌面。

## 2026-08-17 · R9 系统远程桌面检查点

- 远程工作台新增“桌面”模式。Windows 以参数数组启动系统 `mstsc.exe /v:host:port`，macOS 以 `/usr/bin/open vnc://host:port` 交给系统屏幕共享；不内嵌或自研 RDP/VNC 协议。
- 桌面模式只显示主机和端口，不显示 SSH 用户名、私钥或密码输入；普通记录只保存名称、主机、端口和模式，旧 SSH 记录切换为桌面时删除其系统凭据。
- 主机/IPv6/端口逐项校验，进程不经过 shell，参数中没有密码；系统客户端缺失时分别给出 Windows 可选功能或 macOS 屏幕共享的可行动错误。
- 系统参数与错误映射、脱敏记录共 9/9 通过；桌面模式保存和一次启动 Widget 通过；相关 7 个文件定向 Analyze 无问题。当前 Windows 实查存在 `mstsc.exe 10.0.19041.5965`，未擅自连接外部主机。
- 源码版本更新为 `1.9.0-dev.4+14`。REM-105 软件主路径完成，Windows 真实目标启动和 macOS 实机仍待实证；R9 下一代码项为 ADB-106。

## 2026-08-17 · R9 ADB 路径与设备状态检查点

- 开发工具新增独立 ADB 工作区，自动从 `ANDROID_SDK_ROOT`、`ANDROID_HOME` 和 PATH 查找用户已安装的官方 Platform-Tools；必须是实际存在且文件名为 `adb/adb.exe` 的可执行文件。
- 通过参数数组运行 `adb version` 和 `adb devices -l`，不经过 shell；显示绝对路径、精确协议版本，以及 `device / unauthorized / offline / unknown` 状态和型号、序列号。
- ADB 命令运行在独立子进程，10 秒超时会终止子进程；界面只接收最终快照，销毁工作区后忽略迟到结果。未授权和离线设备给出可行动提示。
- 版本/设备解析、基础 Widget 和独立入口 4/4 通过，相关 6 个文件定向 Analyze 无问题。本机真实探测为 ADB `1.0.41` / Platform-Tools `31.0.3-7562133`，绝对路径 `D:\work\allwin\platform-tools\adb.exe`；官方 server 成功启动，当前无 USB/无线设备和 mDNS 服务。
- 源码版本更新为 `1.9.0-dev.5+15`。本检查点只关闭 ADB-106-A/B 的路径、版本和状态基础层，不宣称 Shell、文件、Logcat、截图、APK、无线配对或智能体审批已完成。

## 2026-08-18 · 大模型首页与 DeepSeek 社区工作台检查点

- 一级导航将“本地模型”改为“大模型”并移动到第 1 项；首次启动默认直接进入 DeepSeek，OCR 作为同页辅助入口。图片或模型文件由系统打开/拖入时仍显式路由到 OCR/模型处理，不被默认页覆盖。
- 页面恢复从易错的数字下标升级为稳定工作区 ID；旧版 `lastTab` 按原顺序迁移。除一级工作区外，同时保存大模型页的 DeepSeek/OCR 子页，异步加载设置后恢复，不因导航重排打开错误页面。
- DeepSeek 宽屏工作区按 MIT 社区项目 Kun（原 DeepSeek-GUI）的 Code 工作台习惯重排：项目/会话侧栏、新建会话、工作区切换、当前模型、配置、流式消息、停止和底部输入；窄屏自动收起会话侧栏，空状态可滚动，无最小高度溢出。
- DeepSeek 正式模型保持 `deepseek-v4-pro` / `deepseek-v4-flash`；定向设置、会话、宽屏侧栏、导航、拖入路由和 OCR 回归 28/28 通过。版本更新为 `1.9.0-dev.6+16`。

## 2026-08-18 · Harness 官方功能基线与 Codex 风格界面检查点

- 纠正上一检查点的参考对象：一级入口由“大模型”改为“Harness（智能体）”，不再以 Kun 桌面客户端作为设计目标；官方 `deepseek-ai/deepseek-harness` Web UI 的工作区、会话、对话、工具、目标/计划、任务、模型、权限、插件和设置成为能力基线。
- 全局主题改为接近 Codex 的中性色：暖灰画布/侧栏、白色工作面、灰色边界和近黑主操作；移除青绿色大面积强调和点击水波动画。Harness 页面保留 Vibekits 自有布局，压缩顶部工作区栏，空状态改为任务导向，不复制 DeepSeek 宣传官网。
- 官方 npm registry 查询确认 `@deepseek-ai/dsh@0.1.0-rc.7`。移除把 `/chat/completions` 普通模型流冒充 Harness 的实现，恢复官方 headless 进程适配；Windows 通过 `node.exe + npx-cli.js` 参数数组启动，API Key 仅经子进程环境变量传递，停止时终止完整进程树。
- 修复 Harness 环境探测在 Flutter 组件测试中残留外部进程计时器的问题：测试环境明确跳过 Node 探测，生产环境仍真实检测 Node 22.19+/24+ 与 npx。
- `flutter analyze` 0 问题；Harness 与主界面组件测试 25/25 通过，覆盖启动恢复、窄窗口、深色主题、任务流式输出/停止、OCR 切换和文件拖入。版本更新为 `1.9.0-dev.7+17`。
# 2026-08-18 Harness 编译期运行时与 Vibekits 工具桥

- 将“用户只输入 Key 即可工作”和“Harness 可调用 Vibekits 全部工具”写入产品需求、实施计划和需求台账。
- 删除 Harness 任务启动中的 `npx --yes` 在线安装路径；改为只读取安装包内 Node、官方 CLI manifest 和预置 profile，缺件时立即报告安装包损坏。
- 新增 `tool/prepare_harness_runtime.ps1` 和 Windows Release 打包校验。构建机固定准备 `@deepseek-ai/dsh@0.1.0-rc.7`，运行时不依赖用户 Node/npm/PATH。
- 新增版本化 `vibekits.tools.v1` 工具桥；首批闭环 SHA-256、ADB 设备枚举和 ADB 网络连接，设备控制要求一次性明确审批，未接处理器的功能不会暴露给模型。
- 定向测试 4/4 通过，相关 Dart 静态检查无问题。官方 npm 资产准备再次运行约 90 秒无输出后主动取消，因此当前仍不能标记“真实 Key 实启完成”。

## 2026-08-18 · Harness 全工具 MCP 闭环检查点

- `vibekits.tools.v1` 从首批 SHA-256/ADB 扩展到所有当前适合无状态自动化的能力：编码/哈希/格式/网络微工具、程序员计算器、文件哈希与搜索、SQLite 检查/只读查询、Git 只读检查、HTTP、GitHub 诊断、串口枚举/一次收发，以及 ADB 枚举/连接/受控通用命令。
- APP 启动仅绑定 `127.0.0.1` 随机端口的工具服务，使用 256 位随机 Bearer Token；官方 Harness 通过内置 `@deepseek-ai/dsh-mcp-client` 与随包 Node/MCP 服务发现工具。进程结束/停止时强制关闭工具服务。
- 只读工具直接执行；ADB、串口、HTTP 与文件变更等风险工具在 APP 内显示工具、目标和参数，只允许“拒绝”或“允许一次”。没有 UI 审批器时默认拒绝。SSH/SFTP 与远程数据库不会向模型暴露明文密码，待以保存资料别名和会话句柄设计专用接口。
- 真实协议闭环已通过：模型请求包含 `mcp__vibekits__sha256` → 官方 dsh 发出 MCP 调用 → APP 计算 `abc` 的 SHA-256 → 工具结果进入下一轮模型请求 → 最终回复 `VIBEKITS_FULL_STACK_OK`，进程退出 0。
- 桥接领域测试 9/9、回环/MCP 协议测试 3/3、官方 Harness 全栈测试 1/1 通过；版本更新为 `1.9.0-dev.8+18`。

## 2026-08-18 · DeepSeek Key 与真实模型发现检查点

- 删除设置页写死的 `deepseek-v4-pro/deepseek-v4-flash` 当前选项，默认改为 DeepSeek 兼容 API 的 `deepseek-chat`，并保留 `deepseek-reasoner` 与自定义模型入口。
- 新增“验证 Key 并加载模型”：使用当前 Key 和 API 地址请求有界 `/models`，401/403、超时、超大响应、空列表和不兼容 JSON 都给出明确原因；Key 只进入 Authorization 请求头，不写日志或项目文件。
- 模型列表按端点真实返回值去重排序，用户选择后原样传给官方 Harness。端点解析/鉴权测试 1/1、设置页加载和选择、Harness 全栈回归 5/5 通过，`flutter analyze` 0 问题。
- 版本更新为 `1.9.0-dev.9+19`。
- Harness 运行时原先由 CMake 逐文件安装，增量 Release 在 3.2 万文件扫描阶段超过 10 分钟；改为 Windows `robocopy` 增量复制后同一 Release 构建 12.2 秒完成，失败码大于 7 才终止构建。

## 2026-08-18 · v1.9.0-dev.15 项目会话与权限语义闭环

- Harness 会话存储由“每工作区一份消息列表”升级为“项目 → 多会话 → 各自消息”，保存活动会话 ID；新建会话不再清空覆盖旧记录，切换项目前写回当前会话，重建界面后恢复上次活动会话。
- 存储格式升级到 v2，限制每项目 40 个会话、每会话 80 条消息并采用原子替换；读取到 v1 单会话文件时自动迁移为首个历史会话，API Key 仍不进入项目/会话文件。
- 权限语义增加重建级验收：选择“完全访问权限”并保存，销毁和重新创建 Harness 后，以 destructive 风险的注册工具验证直接返回允许且批准框为 0；工具白名单和底层参数限制不变。
- `flutter analyze` 0 问题；完全访问重建测试 1/1、项目多会话新建/切换/重建测试 1/1 通过。版本更新为 `1.9.0-dev.15+25`。

## 2026-08-18 · v1.9.0-dev.16 Harness 调试目录归档

- Harness 模型设置新增可选择的“调试文件目录”，保存到 AppSettings 并跨重启恢复；默认路径为当前 Vibekits 可执行文件同级 `tmp`。
- 保存时真实创建 `logs`、`screenshots`、`temp`。Harness 子进程注入专用日志/截图目录以及 `TEMP/TMP/TMPDIR`；stdout/stderr 同步写入按 UTC 时间命名的日志，进程结束追加真实退出码。
- 截图 OCR 复用同一配置并写入 `screenshots`，不再写系统临时目录。API Key 仍仅经子进程环境变量传递，日志转发只接收进程输出。
- `flutter analyze` 0 问题；设置持久化、目录领域层、日志 Key 脱敏、目录选择保存、完整 Harness 与截图自动 OCR 回归合计 15/15 通过。版本更新为 `1.9.0-dev.16+26`。

## 2026-08-18 · v1.9.0-dev.17 Windows 智能清理规则库与有界并行扫描

- 新增 Vibekits 自有 Windows 清理规则库 v1，共 22 条明确路径规则，覆盖 Explorer 缩略图/图标缓存、WER、CBS/DISM/Panther 日志、Windows Update/传递优化候选及 VS Code、Cursor、Windsurf、Docker Desktop、GitHub Desktop、Postman、Slack、Discord、Teams、NVIDIA 等常用软件日志或缓存。
- 规则按 Windows build、风险、默认选择、最小年龄、文件名包含/排除模式和说明执行。Downloads、Windows.old、驱动包、Prefetch、注册表及系统托管缓存不做默认删除；不以通用扩展名扫描整盘。
- 后台扫描从一个工作 Isolate 升级为最多两个有界扫描 Isolate，实时聚合进度、候选、字节数和不可读目录；取消令牌同时终止两个工作线程，仍保留准确的部分结果。
- 未复制许可不明确的 Winapp2 基础规则或 GPL-3.0 BleachBit 代码；路径依据 Microsoft 官方文档独立实现。
- `flutter analyze` 0 问题；规则/版本/模式/年龄测试、扫描/取消及双工作线程合并回归 16/16 通过。版本更新为 `1.9.0-dev.17+27`。

## 2026-08-18 · v1.9.0-dev.18 Kudu/constUP/BitCleanerX 算法吸收

- 审阅 Kudu 的 JSON Schema 规则设计、最小保留时间、bounded anchor/direct file allowlist 安全规范，constUP Garbage Cleaner 的详细 dry-run、规则过滤、垃圾预防说明和异常大日志规则，以及 BitCleanerX 的跨平台配置化扫描模型。
- 规则模型新增 `maxDepth` 并贯穿发现、Isolate 传输和扫描器。用户目录顶层规则固定 `maxDepth: 0`，只检查直接文件名白名单，项目、文档和其他子目录即使存在同名文件也不会进入候选。
- 规则库升级到 v2/29 条：新增 JetBrains `java_error_in_*.hprof/.log`、WSLg `RdClientAutoTrace-*.etl`、Gradio 临时文件、.NET TelemetryStorageService、Scoop 下载缓存和 Delphi 许可旧日志；大型堆转储/ETL/许可日志保持谨慎且默认不选。
- 没有复制 Kudu/constUP/BitCleanerX 源码或规则文件；按许可证和安全边界独立实现，保留来源与差异记录。
- `flutter analyze` 0 问题；清理规则、深度、模式、年龄、取消和双工作线程回归 17/17 通过。版本更新为 `1.9.0-dev.18+28`。

## 2026-08-19 · v1.9.0-dev.19 系统盘审计与 macOS 清理规则

- Windows 清理规则库升级到 v3，增加系统盘根目录大型 `.log/.etl/.dmp/.hprof` 诊断文件的深度 0、64 MiB、24 小时谨慎规则，并对用户确认的 `C:\estlog` 增加递归旧日志候选；两类均默认不选，必须复核后清理。
- 新增独立 Isolate 系统盘分析器，递归计量每个根目录/文件，识别 Windows、安装程序、用户数据、恢复启动、系统管理文件、日志缓存与未知项；跳过链接/联接，支持取消、I/O 让步、不可读与统计不完整标记。
- 清理页新增空间分析和清理后自动分析，展示系统盘总量、已用、剩余、已归类和未归类空间；完整 JSON 保存所有根项目、大小、类别、合理性和判断原因，不读取文件正文。
- 新增 macOS 清理规则库 v1（26 条），覆盖 Xcode/Simulator、SwiftPM/Homebrew/CocoaPods、主流语言包缓存、浏览器和开发应用明确缓存目录；不扫描项目、模拟器设备数据和用户文档。
- `flutter analyze` 0 问题；清理界面、规则、扫描、取消、系统盘统计与报告合并回归 27/27 通过。版本更新为 `1.9.0-dev.19+29`。
- Windows Debug 构建成功，产物 `FileVersion` / `ProductVersion` 均核对为 `1.9.0-dev.19+29`；主窗口启动与版本显示定向冒烟 1/1 通过。

## 2026-08-19 · v1.9.0-dev.20 Harness 原生权限与自包含 Git

- 修正权限菜单只作用于 Vibekits MCP 的缺口：三档权限现在传入官方 Harness 沙箱，原生工具审批通过随机令牌回环接口进入同一 App 审批器，桥不可用时默认拒绝。
- Windows 引入固定且校验 SHA-256 的官方 MinGit `2.55.0.windows.3`；Git 工作区与 GitHub 诊断只解析 App 私有运行时，Release 缺失时构建失败，不再依赖用户 PATH。
- Harness 工具目录增加两个版本对比和本地安全分支创建；后者需要权限批准且不会切换当前工作区。SSH 本地端口转发改用编译进 App 的 `dartssh2`，不再调用系统 ssh。
- `flutter analyze` 0 问题；官方 dsh 同轮 MCP+pwsh 原生审批/真实落盘全栈、权限持久化、会话切换、内置 Git 检查/Diff/分支和远程工作区定向回归通过。
- Windows Debug/Release 构建成功，二者版本资源为 `1.9.0-dev.20+30`；Release 自包含检查验证 Git/ADB/7-Zip/Harness/Node/串口/ONNX/OCR 共 17 项运行时或模型资产。版本更新为 `1.9.0-dev.20+30`。

## 2026-08-19 · v1.9.0-dev.21 跨用户瞬态目录智能发现

- 修正清理器只扫描当前用户 AppData 和已点名 `C:\estlog` 的盲区；现在枚举 `C:\Users\*`（含 Default 模板用户）的 Roaming/Local，以浅层目录语义和直接日志文件证据发现未知软件缓存、日志、崩溃与临时目录。
- 智能发现不依赖 `ESTLOG` 专名：只扫描最多两层应用目录、每层有界条目，跳过链接；日志目标仅纳入超过 7 天的 `.log/.etl/.dmp/.hprof/.trace/.tmp/.bak`，明确排除数据库与配置。目标默认参与扫描，但候选是高风险且默认不选择。
- 本机 `C:\Users\Default\AppData\Roaming\ESTLOG` 实查存在但为空，因此不虚报可释放空间；模拟旧日志被发现、JSON 配置与当前日志被排除的一对一测试通过，清理发现/后台/界面合并回归 22/22 通过。版本更新为 `1.9.0-dev.21+31`。

## 2026-08-19 · v1.9.0-dev.22 全盘日志清单与可审计清理日志

- 复核 BleachBit CleanerML、Winapp2 和 Czkawka 的规则/排除/警告/预览/停止标记设计；未复制 GPL 上游源码或规则文件，按 Vibekits 的安全合同独立实现。
- 清理规则目录升级到 v8：Windows 新增后台系统盘 `.log` 清单（最多 25000 个候选、16 层、仅超过 7 天）；未知用途全部高风险且默认不选，不把文件后缀当作自动删除依据。
- Harness 当前设置的调试目录真实接入清理：`logs`、`screenshots`、`temp` 中超过 24 小时的日志、调试截图和临时文件进入候选，当前任务文件保留。
- 清理报告升级 v2，记录真实路径、来源、大小、修改时间、状态和原因；清理页新增“清理日志”，可浏览每次运行及逐项明细，也可逐条删除记录。
- `flutter analyze` 0 问题；清理规则、Harness 年龄门槛、删除、原子报告读写删除、后台取消和界面定向回归 28/28 通过。版本更新为 `1.9.0-dev.22+32`。

## 2026-08-19 · v1.9.0-dev.23 官方 Harness Web 整体移植

- 对照 DeepSeek 官方仓库 Web UI 指南和 Release 内固定 `@deepseek-ai/dsh@0.1.0-rc.7` 的完整 Client UI 产物，建立 `19_OFFICIAL_HARNESS_WEB_PARITY.md` 作为唯一操作基线。
- Windows Harness 正式入口改为启动内置 `dsh web` 并用 WebView2 嵌入官方生产界面；删除 Flutter 前置选项目/输 Key 和自制对话交互路径。
- 项目、有序多会话、重命名、排序、Fork、Archive、Ungrouped、搜索、模型、权限、plan/goal/jobs/subagent/tool trajectory 全部回归官方 Host/Client 数据模型。
- 官方 Web 允许在无环境 Key 时启动，用户按官方 Settings → Models 配置；旧凭据仅作为进程环境初值，不进入命令行或日志。
- 默认模型设置改为仅首次创建，后续 App 启动不再覆写用户在官方 Settings 中保存的配置。
- Vibekits 工具保留为官方 MCP client 下的扩展，继续共用 App 领域服务、审批、取消和真实审计记录；不接管官方原生会话和权限 UI。
- Release 首次实启发现 App 关闭后 `node.exe` 可残留；Windows Runner 新增专用 Job Object 生命周期通道，Harness 进程树绑定 App，即使 Dart 未来得及 await `dispose` 也会由内核回收。
- 静态分析 0 问题；首轮 Harness/Web/工具桥及主界面 46/46 通过，进程生命周期修复后定向 25/25 通过；Windows Release 构建通过。App 实启后官方动态回环端口返回 HTTP 200/12076 字节 HTML，强制结束 App 后 Harness node 残留为 0。

## 2026-08-19 · v1.9.0-dev.24 ESTLOG 与系统盘真实占用审计

- 修复 `C:\ESTLOG` 已存在但旧配置不扫描的问题：Windows 规则库升级到 v4、清理目录版本升级到 v9，旧用户自动合入独立 ESTLOG 目标；只列出超过 24 小时的 `.log`，不依赖有 25000 项上限的全盘日志清单。
- 本机只读实查 `C:\ESTLOG` 共 24 个文件，24 个均符合规则，共 39,313,476 字节（37.49 MiB）；没有在研发验收阶段直接删除真实用户数据。
- 修正系统盘报告把普通文件长度之和误当物理占用的表达：卷总量/已用/剩余继续取真实磁盘 API，目录统计明确标为“目录逻辑量”；当逻辑量超过已用量时显示 NTFS 硬链接重复计数并写入 v2 JSON 报告。
- 本机 C 盘只读审计为总量约 80.00 GiB、已用 73.27 GiB、剩余 6.74 GiB。DISM 报告组件存储资源管理器显示 7.09 GB、实际 7.04 GB、可回收包 1 个且不建议清理，因此不把 WinSxS 作为手工删除项。
- 用户数据进一步定位到 AppData 20.60 GiB、`.cache` 1.97 GiB、`.gemini` 1.65 GiB、`.codex` 0.89 GiB；明确区分可再生缓存、软件升级残留、工作区/会话数据和安装程序，不按目录大小盲删。
- `flutter analyze` 0 问题；清理目录、系统盘分析和界面定向测试 26/26 通过。版本更新为 `1.9.0-dev.24+34`。

## 2026-08-19 · v1.9.0-dev.25 官方 Harness 凭据闭环纠偏

- 对照内置官方 `@deepseek-ai/dsh@0.1.0-rc.7` 的凭据提供器确认：进程环境变量是最高优先级只读源，官方 Models 页的可写源是 `$DSH_HOME/.credentials.yaml`；旧集成注入 Key 正是输入框只读的原因。
- Windows 官方 Web 启动不再注入 `DEEPSEEK_API_KEY`、`DEEPSEEK_BASE_URL`、`DEEPSEEK_MODEL`，也不再由 Vibekits 首次写入默认模型；Key、端点、模型目录和默认模型恢复由官方设置链路持久化及热更新。
- 旧版 Windows Credential Manager / macOS Keychain 中的 Key 仅迁移一次：不覆盖官方已存值，迁移落盘后删除旧副本；密钥不进入代码、安装包或日志。
- 修正 App 内常量仍停留在 `dev.21+31` 的版本显示问题，版本统一更新为 `1.9.0-dev.25+35`。

## 2026-08-19 · v1.9.0-dev.26 Harness 输入与二级选择闭环

- 为嵌入式官方 Web 增加 Ctrl+V/Cmd+V 粘贴转发：读取系统文本剪贴板后只写入当前获得焦点且可编辑的 input/textarea，通过原生 `input` 事件进入 React 状态；内容不写日志、不进入 App 设置。
- 定位模型/推理等级二级菜单在 WebView2 中点击即关闭的根因：焦点迁移的 `relatedTarget` 为空，官方组件在 `onClick` 前执行了关闭。兼容补丁不再把空焦点误判为外部点击；有明确外部焦点时仍正常关闭，保留官方两级菜单行为。
- 官方权限内部值保持 `read-only`、`workspace-write`、`danger-full-access`，中文界面显示为“只读”“工作区读写”“完全访问”，完全访问风险确认同步中文化。
- 新增可重复执行且遇到上游结构变化会失败的 `tool/patch_harness_runtime.mjs`，运行时准备脚本自动应用补丁，避免只修改本机构建缓存。

## 2026-08-19 · v1.9.0-dev.27 Harness 可操作性与会话删除闭环

- Key 粘贴由 WebView 外层兜底改为官方 Models 输入控件内直接处理 Ctrl+V/Cmd+V，避免焦点进入 Web 内容后快捷键丢失；Windows Release 用虚假测试 Key 实机粘贴成功，截图后取消编辑，未写入真实凭据。
- 官方模型菜单实机显示 `DeepSeek-V4-Flash` / `DeepSeek-V4-Pro`，推理等级实机显示 Off / Low / High / Max；权限入口保持官方内部标识，中文界面显示“只读 / 工作区读写 / 完全访问”。
- 保留官方“归档会话”的可恢复语义，新增独立“删除会话”操作。删除前二次确认并停止 Harness 运行态，只允许规范会话 ID，随后精确移除会话目录、`workspace.json` 引用和 `session_projcache.json` 投影；路径穿越拒绝和真实索引删除夹具 2/2 通过。
- 定向 Analyze 0 问题；Windows Release 构建成功，自包含校验 17/17 通过；EXE 与界面版本均为 `1.9.0-dev.27+37`，Release 已打开供人工测试。

## 2026-08-19 · v1.9.0-dev.28 Harness 删除体验与真实数据验收

- 复现 dev.27 删除后全屏显示“正在启动官方 DSH Web”并最终超时：会话目录和索引已经正确删除，失败点是紧接停止后的 DSH 瞬态重启；手工重试可恢复。
- 删除重连改为保留当前官方工作台，顶部 2px 进度条和底部状态条反馈后台重连，不再用空白启动页遮住整个界面；首次启动失败自动等待 800ms 后重试一次。
- Windows Release 真实删除当前空白会话，界面从“其余 5 个会话”变为“其余 4 个会话”，后台重连成功。经用户明确授权后使用同一 UUID 校验和精确路径删除服务清除 27 个历史调试会话；会话目录和投影缓存为 0，重启后官方仅自动建立 1 个可输入的空白会话，工作区、源码、模型设置和 API Key 均保留。
- 定向 Analyze 0 问题、会话删除测试 2/2、Windows Release 构建和 17 项自包含校验通过；版本为 `1.9.0-dev.28+38`。

## 2026-08-19 · v1.9.0-dev.29 目录配置与响应速度优化

- 全局设置新增 Harness 调试临时目录和工具/模型下载目录；默认分别为 EXE 同级 `tmp` 与 `%LOCALAPPDATA%\Vibekits\downloads`，保存后重建 Harness 页面并立即采用新配置。
- 外部模型下载改为目标目录 `.part` 暂存、SHA-256 验证后原子改名；内置工具继续固定随 Release `tools` 发布。
- 主导航改为按需挂载重型工作区，点击先完成页面切换再初始化目标页；Harness 启动并行初始化 WebView2 和 DSH，首页不再同步扫描/哈希 OCR 模型。
- 官方 Harness 的菜单与弹层过渡压缩为 60–120ms，保留原版交互和后端任务状态，不伪造执行完成。

## 2026-08-19 · v1.9.0-dev.30 Harness 本地冷启动恢复

- 使用 Release 内置 Node/DSH 脱离 APP 复现：关闭 Vibekits MCP 后，官方 `dsh web` 冷启动仍可能超过 60 秒才开放回环地址，确认不是联网下载或工具桥导致。
- 为 Agent/Web 两条启动路径启用位于 Harness 用户目录的 Node 持久编译缓存；DSH 仍完全从 `tools/harness` 本地运行。
- 就绪探测由固定次数改为存活进程最多等待 3 分钟，每 5 秒显示已用时间；进程提前退出立即报错，避免超时后反复杀进程重启。
- 子进程日志新增 PID、本地 URL 和 bundled 标记；进程退出前等待 stdout/stderr 排空并即时 flush，启动失败不再留下只有 exit code 的空日志。

## 2026-08-19 · v1.9.0-dev.31 离线隔离与系统盘实时占用清单

- 网络功能维持页面级隔离：APP 不执行阻塞式全局联网检查，断网只影响主动发起的模型/API/GitHub/远程操作；本地五大工作区和内置工具继续工作。
- 系统盘分析从单根串行改为独立 Isolate 内 3 个有界 I/O worker，优先返回日志、未知和软件目录；扫描过程中每完成一项就实时上屏，可随时取消。
- 同一次文件遍历生成 Windows 组件、Program Files 软件、ProgramData 软件数据、用户目录和 AppData 软件数据拆分，列表和 JSON 报告均保留负责人、父目录、合理性和删除策略。
- 日志缓存与未知根项目可在占用列表中二次确认后移入回收站；系统、启动、Windows 和程序安装目录没有直接删除入口。删除进入独立 Isolate，完成后自动刷新占用。
- 定向空间分析与 Cleaner Widget 共 13/13 通过，覆盖真实字节、取消、软件拆分、未完成实时显示和列表回收站回调。

## 2026-08-19 · v1.9.0-dev.32 文档格式与关闭操作闭环

- 文档阅读侧栏和空状态新增“支持格式”入口，按文本/源码、Markdown、结构化数据、Web/EPUB/SVG、二进制和无后缀文件名列出统一注册表中的全部格式，不再保留已经过期的 M3 占位说明。
- 已打开文档的工具栏始终显示“关闭当前文档”；存在未保存编辑时沿用放弃修改确认，关闭后保留最近打开记录并回到空状态。
- 新增 Widget 回归，覆盖完整格式说明入口和打开后关闭。

## 2026-08-19 · v1.9.0-dev.33 SSH 会话直达 SFTP

- 交互式 SSH 登录成功后新增绿色“SFTP 文件”按钮，对齐 Xshell 调用 Xftp 的主流操作；点击后直接进入内置双栏文件管理。
- SFTP 不再重新建立连接或索要密码，而是在当前已认证 `dartssh2` 客户端上打开独立 SFTP channel；关闭文件区只关闭该 channel，原终端和多标签继续运行。
- 新增 Widget 回归，验证按钮复用同一会话、无密码弹窗、进入 SFTP、返回终端以及终端保持在线。

## 2026-08-19 · v1.9.0-dev.34 Harness 工具闭环与后台文件分析

- Harness 新增保存会话适配器：列出 SSH/SFTP 会话、执行有界 SSH 命令、列目录、上传和下载；复用系统凭据并强制匹配用户已确认的主机指纹，密码和私钥不进入模型参数、返回值或审计日志。
- 远程数据库新增保存会话列表、对象检查和只读 SQL；覆盖 PostgreSQL、MySQL、MariaDB，复用 Credential Manager/Keychain，仍由短生命周期 Isolate 执行并保留 15 秒上限。
- 文件哈希改由 Harness 调用后台 Isolate；新增重复文件扫描和磁盘占用分析工具，均只读、结果有界，不阻塞窗口消息循环。
- 新增“开发工具左侧每个入口必须存在 Harness 可执行适配器”的目录合同测试，防止以后只加界面入口却漏接智能体。
- 工具桥与内置 Node MCP 联合闭环 18/18 通过，完成工具发现、调用、权限拒绝/批准、真实审计记录与敏感凭据脱敏；定向 `flutter analyze` 0 问题；Windows Release 构建成功，自包含资产 17/17、EXE 文件/产品版本均验证为 `1.9.0-dev.34+44`。

## 2026-08-19 · v1.9.0-dev.35 跨工具任务链

- 新增 Harness `remote.open_interactive` 工作流工具。智能体只提交主机、可选用户名和端口，App 立即切换到 SSH 工作区；密码始终由用户在原生安全输入框录入，不经过模型。
- SSH 登录成功后自动在同一个已认证客户端上开启 SFTP channel 并显示双栏文件；已有匹配会话复用记录、用户名、端口、私钥和系统凭据，不重复认证。已有其他终端标签继续运行。
- 新增 Harness `ocr.capture_screen` 工作流工具：用户框选区域后，本地 PP-OCRv6 tiny 自动识别，文字、坐标、置信度、图片尺寸、耗时和运行时作为结构化结果回到智能体。
- 建立 `20_INTEGRATED_DEVELOPER_WORKFLOWS.md`，以后按完整任务链而不是工具数量推进；明确 ADB/串口/日志、API/数据库/Git、文件与清理的共享上下文和下一步动作。
- Harness 工具桥 16/16、远程工作区 14/14（合计 30/30）通过；覆盖意图切页、一次密码、同会话 SFTP、截图 OCR 审批与结果回传；定向 Analyze 0 问题。
- Windows Release 构建成功，自包含校验验证 Git/ADB/7-Zip/Harness/Node/串口/ONNX/OCR 等 17 项运行时或模型资产；版本更新为 `1.9.0-dev.35+45`。

## 2026-08-19 · v1.9.0-dev.36 发布质量门禁与清理解释闭环

- 建立机器可执行的任务链验收清单和 fast/release 两级门禁。报告自动记录命令退出码、耗时、逐项日志、产物版本和 SHA-256；缺少测试或证据路径会直接失败。
- 系统盘分析新增统一容量解释层：按剩余比例、绝对体积、磁盘占比、目录类型和可再生性区分正常、偏大、需复核与优先处理；明确 Windows 10/11 的 20–40 GiB 仅为逻辑量启发式参考，不构成删除授权。
- 清理界面和 Harness `vibekits.cleaner.analyze_drive` 共享完全相同的软件归属、磁盘压力、判断依据和安全建议。清理页新增“让 Harness 解释”，一键切换智能体并填入受约束任务，不自动发送或删除。
- 门禁真实发现并修复两处风险语义回归：小型未知目录不能标为正常；小型日志虽通常无需处理，删除前仍必须显示需复核。另修复 Windows PowerShell 5 读取 UTF-8 清单失败。
- 真实 `C:\` 只读扫描检查 832987 项：80.00 GiB 总量、73.81 GiB 已用、6.19 GiB 剩余、10 个不可读位置；Windows 约 31.81 GiB，Users 约 27.42 GiB，Program Files 两目录约 19.51 GiB，用户 Temp 约 2.38 GiB。未删除任何真实文件。
- Release 门禁最终 PASS：清单 1/1、容量解释 6/6、清理链 15/15、Harness/远程 30/30、全量 Analyze 0 问题、Windows Release 构建和 17 项自包含校验通过；EXE SHA-256 为 `1C24D08D9C6226E8268EFA072A60847E735069BF4C283E01A83EF915C8938EA2`。

## 2026-08-19 · v1.9.0-dev.37 任意文件 Diff 与分模块工具日志

- 新增独立“文件 Diff”工作区：两个文件输入/选择、交换、编码识别、左右行号、新增/删除/未变统计、忽略空白/大小写、只看差异、复制统一 Diff 和保存 `.diff/.patch`。
- 比较在独立 Isolate 中执行，单文件上限 8 MiB/50000 行；常规内容使用精确 LCS，超大完全差异使用有界块比较，避免二次方内存拖死界面。
- Harness 新增只读 `vibekits.file_diff`，与手工界面共用 `FileDiffService`；调用结果结构化返回，审计日志只记录路径、统计和截断标记，不保存文件正文。
- Harness 工具日志由全局开关扩展为“全局 + 当前模块”策略：默认开启，可单独关闭某一工具模块，ADB/串口/SSH/Diff 等互不影响；现有记录仍可逐条或按模块删除。
- 静态分析 0 问题；真实文件、界面、日志策略、Harness 桥与开发工具目录合同定向测试 30/30 通过。完整 Release 门禁 8/8 项 PASS，11 条任务链清单有效，自包含资产 17/17；EXE SHA-256 为 `B8819E2EFEDB8A8E51AC2B734273B4A6626CA967DA9B35497057E911ACDA0618`。

## 2026-08-20 · v1.9.0-dev.38 能力自动发现与开源微能力融合

- 建立 `22_CAPABILITY_INTEGRATION_STANDARD.md` 最高准则：新增能力必须统一注册、自动向 Harness 描述使用/禁用场景、共用领域服务、具备上下游、默认后台有界并进入分模块审计。
- `ToolSpec` 成为界面搜索、Harness IDs、模型选择描述和日志模块的单一来源；删除 `dev_tools_tab.dart` 中重复维护的 Harness ID switch。复杂工具显式声明 IDs，同构微工具随 `run/runAsync` 自动进入 MCP 工具目录。
- 复核 ripgrep、fd、jq、delta、yq、ast-grep、tokei 和 hyperfine 的任务模型与许可证。首批融合安全只读 JSON 路径查询和 Tokei 风格代码统计；不复制第三方源码/UI，不开放 hyperfine 式任意命令执行。
- 代码统计在 Isolate 中执行，最多 50000 个源码文件、单文件 8 MiB，跳过 `.git`、依赖和构建目录；JSON 查询只支持确定性的点路径、数组下标和通配，不执行任意表达式。
- 定向与全量 Analyze 0 问题；领域、界面、自动工具发现、真实 Harness 调用和日志合同回归通过。完整 Release 门禁 9/9 项 PASS，13 条任务链清单有效，自包含资产 17/17；EXE SHA-256 为 `01A81BA10AE603EE54B8F2D422008986A76AAE623B3153060F170898C27688EB`。

## 2026-08-20 · v1.9.0-dev.39 开源能力融合后续项当日收口

- 原“JSON 路径查询”原位升级为 JSON/YAML/TOML/XML 统一结构化查询；自动识别格式，支持确定性点路径、数组、通配和 XML 属性，不执行任意 jq/yq 表达式，不修改源数据。
- 新增 Tokei/ast-grep 工作流衔接：代码统计先提供项目规模与主要语言，代码结构搜索再在独立 Isolate 中定位类、类型和函数声明，结果直接供 Harness 选择具体文件；最多扫描 50000 个文件、返回 500 项、单文件 8 MiB。
- 文件搜索吸收 ripgrep/fd 的默认行为：读取根 `.gitignore` 常用 glob、默认跳过匹配项并启用 smart case；明确不把轻量 matcher 宣称为完整 Git ignore 引擎。
- 安全性能基准吸收 hyperfine 的预热、多轮采样和百分位思想，但只允许 SHA-256、JSON 解析、Base64 三种内置操作，输入 1 MiB、5～200 次；模型无法借此执行任意命令。
- 新能力均通过 `ToolSpec` 自动进入现有“转换与检查”、Harness MCP 目录、使用边界描述与分模块日志，没有新增左侧孤岛。定向回归 48/48 及新增 Harness 真实调用通过；完整 Release 门禁 10/10 项 PASS，14 条任务链有效，自包含资产 17/17；EXE SHA-256 为 `FD3DFB2DA82268B9321902040C8DDC08E5BA81CD74A8FB54D81F9D0D1DD2731F`。

## 2026-08-20 · v1.9.0-dev.52 系统清理安全恢复

- 真实清理暴露 dev.51 严重边界错误：智能选择纳入“需确认”运行包和活动开发依赖，造成 QQ/企业微信相关文件及 Pub 缓存缺失；本轮 10 GiB 目标判定 FAIL。
- 智能选择改为严格高置信白名单；开发缓存、模型、运行包、未知目录、回收站和软件本体只分析展示，必须用户逐项选择。
- 快速删除仅限安全可再生成候选，采用 8 路有界并发；单软件缓存增加停止和 30 秒硬超时，聚合厂商/系统目录禁用一键清理。
- 软件占用从 Microsoft/Tencent/WindowsApps 聚合层继续拆到具体产品或包；卸载只调用官方卸载器。
- 隔离 104 个损坏 Pub 空壳目录并把项目缓存恢复到 D 盘；清理模块静态分析 0 问题、定向测试 20/20、Windows Release 构建通过，版本 `1.9.0-dev.52+62`。
- dev.52 前台真实闭环清理 2.28 GiB，磁盘实际可用空间增加 2.24 GiB（至 4.39 GiB），成功 614、跳过 1、失败 4，任务正常退出；回收站约 5.89 GiB 因可能包含上一轮误移文件而保留。

## 2026-08-22 · v1.9.0-dev.70 Harness 本地启动优化与发布入口纠正

- 用户截图确认实际打开的是 `dev.63+73`。源码版本更新不等于用户入口已更新；本轮先关闭锁住旧 EXE 的进程，再从固定 `build/windows/x64/runner/Release/Vibekits.exe` 覆盖构建，并核对文件/产品版本均为 `1.9.0-dev.70+80`。
- 找到 Vibekits 自身启动热路径中的强制 I/O：DSH stdout/stderr 每个小块都调用一次 `flush`，同时 Flutter 每个输出块都整页 `setState`。改为日志 2 秒有界批量落盘、诊断文本 100 ms 合并刷新，退出时仍强制完整落盘。
- Harness 运行时清单解析在进程内缓存；运行时定位与调试目录准备并行；`cordis.patch.yml` 内容未变化时不再重写和强制刷盘，避免重复失效文件缓存及触发扫描。
- Windows Release 实机测量：优化后首轮 DSH 子进程创建到官方 `dsh web` 日志就绪为 4192 ms，第二轮为 3886 ms；脱离 App 的官方 DSH 热缓存对照约 3426 ms。端到端第二轮为 5650 ms，其中 App 首帧到 DSH 子进程创建约 1764 ms。
- 3 秒硬门槛尚未闭环：官方 `@deepseek-ai/dsh@0.1.0-rc.7` Web profile 仍需加载大量模块和 Vibekits MCP 插件，且 Windows 未签名的 313 MB 多文件运行时会受文件缓存/安全扫描影响；一次受干扰重启达到 15.8 秒。不得把“3 秒显示加载壳”伪写成“3 秒可执行任务”。
- `deepseek_harness_test.dart` 13/13 通过；Windows Release 构建通过并已前台运行。后续要达到稳定 3 秒，需完成 DSH 单文件/快照构建或官方插件延迟装载实验，并按冷启动、热启动和系统重启后三档分别验收。

## 2026-08-22 · v1.9.0-dev.71 Clash Verge 原生可操作工作区

- “Clash Verge / 虚拟机”不再只提供配置路径和启动按钮。代理页改为可直接工作的原生控制台：订阅/本地 YAML 管理、当前配置、运行状态、系统代理、规则/全局/直连模式、代理组与节点、连接/流量统计和 Mihomo 实时日志集中在同一页。
- 订阅下载使用 HTTPS（本机测试允许 loopback HTTP），限制 32 MiB；完整订阅 URL 保存在系统凭据库，普通清单只保留名称与来源主机。配置、当前选择和更新时间持久化，下次打开仍可继续使用。
- 启动时只允许回环控制端口；配置未声明控制器时自动分配本地端口。所有节点、模式、连接和流量数据来自内置 Mihomo REST，不使用模拟数据。
- 系统代理切换前保存 Windows 原设置，停止时恢复；Mihomo 或控制器启动失败也立即回滚代理并结束进程，避免网络残留。小窗口改为单一滚动区域，大窗口使用配置侧栏与控制台双栏。
- 新增配置持久化/凭据隔离/订阅更新、控制器状态/模式/节点和完整入口 Widget 回归，定向测试 13/13、Analyze 0 问题；Release 内置 Mihomo/QEMU 真实启停、REST 模式切换与 Windows 系统代理恢复通过，EXE 文件/产品版本均为 `1.9.0-dev.71+81`。

## 2026-08-22 · v1.9.0-dev.72 Clash 订阅兼容性与可诊断性修复

- 实机反馈“同一订阅在 Clash Verge 可用、Vibekits 添加失败”后确认旧实现没有保存下载阶段日志，失败发生在 Mihomo 启动前时无法追踪。本轮新增 `tmp/mihomo/subscription.log`，界面“订阅与运行日志”同步显示下载路线、脱敏主机、阶段和错误，不记录 token 或完整 URL。
- 解除错误的 240 字符 URL 限制；Windows Credential Manager 写入上限从误用的 512 B 修正为系统支持的 2560 B，600 字符订阅凭据真实写入、读取、删除通过。
- 订阅请求增加 Clash Verge User-Agent；在 Windows 系统代理已启用时优先使用该代理，失败后自动回退直连。解决仅能通过现有代理访问订阅服务时 Dart 直连失败的问题。
- 接受不携带 `mixed-port` 的标准代理列表 YAML，并在生成运行配置时补入本地 `mixed-port`、`allow-lan: false` 和回环控制器；修正代理组条目被错误计入节点数量的问题。

## 2026-08-22 · v1.9.0-dev.73 Clash Verge 标准 Profile 启动链路

- dev.72 实机日志确认订阅已下载 31,057 字节，但节点显示 0。结构检查确认 Profile 含 32 个行内映射节点；节点计数改为按 `proxies` 列表项统计，兼容 `- name:` 与 `- {name: ...}` 两种标准 YAML 写法。
- 使用 Release 内置 Mihomo 对真实 Profile 执行 `-t` 发现实际阻塞：运行包缺少 `Country.mmdb`、`geoip.dat`、`geosite.dat`，导致 GEOIP 规则启动时尝试在线下载并因 DNS 失败。补齐与当前 Clash Verge 相同且经过 SHA-256 固定的 GeoData，纳入 CMake Release 和自包含校验。
- Mihomo 启动前把内置 GeoData 原子准备到独立数据目录，并执行 30 秒有界配置预校验；只有校验通过才创建常驻进程和切换 Windows 系统代理，避免错误配置破坏网络。
- 同一份用户 Profile 在补齐 GeoData 后真实校验 38 ms 完成并返回 successful；最终 Release 进一步真实启动该 Profile，REST 返回代理组及不少于 32 个节点，进程正常退出。自包含资产 28 项通过，运行不再依赖 GitHub 临时下载。

## 2026-08-22 · v1.9.0-dev.74 节点测速与显式关闭

- 运行态顶部固定显示“全部测速”和“关闭代理”；关闭操作明确停止 Mihomo、取消当前测速并恢复启动前的 Windows 系统代理。
- 每个代理组增加独立测速按钮，节点下拉直接显示 `ms` 或“超时”；全量测速采用 6 路有界并发并实时显示进度，测速期间仍可立即关闭代理。
- 测速直接调用内置 Mihomo 标准 `/proxies/{name}/delay` 接口，不在 Flutter 层伪造网络延迟。轻量接口测试通过；使用用户真实 32 节点 Profile 启动并完成真实延迟请求，6 秒内验收通过。

## 2026-08-24 · v1.9.0-dev.97 Android Harness/清理/OCR 真机闭环

- Android Harness 不再等待或检查 Windows Node/DSH，直接使用移动端原生模型调用链；真机显示“Harness 就绪”。
- Android 清理器只允许 App 私有缓存、临时文件、Harness 日志/截图，页面和扫描不再退出。
- 为 APK 新增 `libvibekits_onnx.so`，与内置 `libonnxruntime.so` 动态对接；`192.168.3.62` 上 PP-OCRv6 真实图片推理返回 15 行文字。
- Release ARM64 APK 构建、安装、冷启动、主模块切换、资源采样和双屏退出通过；完整证据见 dev.97 验收报告。
# 2026-08-23 · v1.9.0-dev.80 清理算法三平台隔离

- 新增清理平台策略，Windows、macOS、Android 的目标发现、规则加载和删除边界互斥。
- Android 只允许应用私有缓存；macOS 谨慎规则恢复人工确认；非 Windows 不再调用 Windows 全盘分析。
- 删除执行前新增第二道平台路径校验，防止发现规则错误导致越界删除。
- 轻量回归 37 项通过，Flutter analyze 0 问题；Android arm64 Release 使用项目隔离 Gradle 缓存构建成功。

# 2026-08-23 · v1.9.0-dev.81 智能体原生验收与 Harness 演进门禁

- 根 README 改为正式产品简介，明确“智能体理解与规划 + 本地工具确定性执行 + 真实证据回流”的定位。
- 音频分析新增有界时间轴，Harness 可直接返回谐波与噪声热点时间段；合成信号专项 2/2 通过。
- 新增每周 Harness 上游检查 Workflow：只在隔离目录准备精确候选版、执行兼容补丁和 CLI 冒烟、建立升级工单，不直接替换客户端固定版。
- 修复 GitHub 诊断建议优先级：SSH 22 阻断但 443 可用时，优先给出 SSH over 443，不再被凭据提示遮蔽。
- Clash 标准左栏新增独立“轻量虚拟机”入口，进入 QEMU 工作区不再要求代理已启动；同时修复 ListTile 材质层的点击/水波纹异常。
- 第一次全量跑次 471 通过/5 环境跳过/3 失败；拆分修正后，拖拽/OCR 21/21、开发工具 10/10、真实 Harness 1/1 专项通过。第二次全量复跑 473 通过/5 环境跳过/1 失败，唯一失败是 VAD 测试误依赖已清理的 Pub DLL；改用 APP 自包含 ONNX Runtime 后真实 Silero 推理 1/1 通过。聚合结果 474 通过/5 环境跳过/0 未修复失败，Flutter Analyze 0 问题。

# 2026-08-23 · v1.9.0-dev.82 Windows/Android 系统资源诊断

- 开发工具新增独立“资源诊断”：打开即出 UI，后台呈现 CPU、内存、GPU、磁盘、Top 进程、异常建议和证据来源。
- Windows 首版 CIM/WMI 探针在本机单项冷启动约 17 秒，完整查询超过 25 秒；主链改为 `.NET ComputerInfo`、`DriveInfo` 和 `Get-Process` 双采样后，真实专项约 3 秒通过。
- Android 本机与内置 ADB 共用固定只读脚本；CPU 使用 `/proc/stat` 两次采样，内存使用 `/proc/meminfo`，并读取 `/data`、Top 进程和可用的 KGSL GPU 指标。
- Harness 新增 `vibekits.system.resources`，默认连续采样 3 次，可配置 1～10 次；返回平均/峰值和重复 Top 进程，调用自动进入现有工具日志。
- 定向领域测试、真实 Windows 探测、界面、工具目录和 Harness 回归通过；Flutter Analyze 0 问题。

# 2026-08-23 · v1.9.0-dev.83 全工具智能体闭环门禁

- 新增只读 `vibekits.system.capability_check`：模型可在任务前核对全部公开工具的处理器接线、风险分布和不可用原因；任何公开空壳都会使合同测试失败。
- 30 个高频微工具全部逐项经过 Harness 真实调用、结果断言和模块日志断言，不再以单个 SemVer 调用代表全部工具。
- 全量首跑发现资源页销毁后 PowerShell 探针仍保留超时计时器；新增可取消探针控制器，页面或 APP 销毁立即结束进程，并补真实 30 秒休眠进程的 3 秒内回收测试。
- 官方 Harness 模型到 Vibekits 工具、原生授权和最终回复单独复验 8 秒通过。最终使用单并发发布门禁，482 项通过、5 项环境门禁跳过、0 失败；Flutter Analyze 0 问题。

# 2026-08-26 · v1.9.0-dev.106 Harness 连续启停 100 次验收

- 新增可重复执行的 Windows Release 压力脚本：每轮启动独立 APP、进入 Harness、从本轮日志解析真实 DSH PID/回环 URL、验证 HTTP 200，再经托盘退出并核对子进程回收。
- 旧版预跑在第 16、30、31、32 轮复现“Harness 正常但退出命令超时”；定位为 Windows runner 在处理托盘 `WM_COMMAND` 前先转发 Flutter 插件，消息可能被插件消费。
- runner 改为优先处理托盘打开/退出生命周期命令。修复后 5/5 预检通过，正式 100 轮从零执行为 100/100 通过：窗口、Harness HTTP、APP 正常退出、Harness 回收均无失败。
- Harness 就绪平均 21.220 秒、P95 23.864 秒；退出平均 6.126 秒、P95 9.041 秒。稳定性通过，但稳定 3 秒就绪目标仍未完成，详见 dev.106 验收报告。

# 2026-08-26 · v1.9.0-dev.107 Harness 能力认知与接口目录

- 从实际 `ToolSpec` 和 `VibekitsHarnessToolBridge` 生成 `37_HARNESS_CAPABILITY_CATALOG.md`，明确区分 5 个产品一级页面、78 个开发能力条目、18 个独立工作区、146 个定义接口和 125 个当前可执行接口。
- 新增 APP 自有的 DSH `AGENTS.md` 指令块：每次准备 Harness Home 时自动写入或更新，保留用户在同一文件中的其他内容；官方 `dsh-agent-instructions` 会在每个工作区首轮加载它。
- Harness 被要求先调用 `vibekits.system.capability_check`，再按实时 MCP Schema 回答功能数量和参数；有专用接口时禁止用 shell、系统 ADB/Git 或第三方程序绕过 APP。
- 目录同步、指令幂等合并/用户内容保留、公开工具处理器自检 3/3 通过；当前 125 个公开接口的 `missingHandlers` 为空。
- 经用户明确授权后，使用已保存凭据向真实 Harness 提问功能数量和接口调用方式；模型主动调用 `vibekits.system.capability_check`，返回 146 个定义接口、125 个可执行接口、0 个缺失 handler，并按模块说明 Schema、权限和典型调用链。
- 真实 Harness 退出代码 0、工具调用日志成功、完成标记存在；脱敏记录未发现 API Key 或 Bearer 内容。联网验收测试默认跳过，只有设置 `VIBEKITS_RUN_LIVE_HARNESS_CAPABILITY=1` 才会使用真实模型，避免普通回归误耗配额。
- 首轮真实回答能识别 146/125 个接口，但把产品一级页面数报告为未知；能力检查随后补充 `productHierarchy`，直接返回 5 个一级页面、78 个开发能力条目、18 个独立工作区和业务模块清单，产品事实不再只依赖提示词。
- 第二轮真实模型复验准确回答 5 个一级页面、14 个业务模块、78 个能力条目、18 个工作区和 146/125 个接口，不再出现“页面数未知”；模型完成标记、能力调用日志和退出码再次全部通过。

# 2026-08-26 · v1.9.0-dev.108 Harness 冷启动异常修复

- 用户现场 dev.107 日志证明 DSH 从进程创建到监听端口耗时 84.858 秒，截图中的 50 秒等待不是旧版本或 UI 计时错误。
- 运行时包含 32,761 个文件；通过官方 Cordis Patch 禁用当前产品不用的 `llm-pi-ai`、`session-telemetry-otel` 和 `client-hmr`，避免 Windows 冷启动扫描多提供商 SDK、遥测和 HMR 依赖；DeepSeek、会话、工作区、MCP 和授权链保持不变。
- dev.108 Windows Release 连续两轮真实 APP 启停均通过：Harness HTTP 就绪 5,622/5,099 ms，平均 5,360.5 ms；APP 均正常退出，DSH 子进程均回收。现场异常等待下降约 93.7%。
- 稳定 3 秒目标仍未诚实达成，后续需要单文件/Node Snapshot 级运行时优化；不允许通过 APP 退出后残留 DSH 进程伪造热启动。

# 2026-08-26 · v1.9.0-dev.109 Harness 幽灵会话删除闭环

- 定位“删除后仍显示、打开报 `session-not-found`”的根因：旧实现直接修改持久化文件，但 DSH 进程仍持有内存投影，同时已清空的临时工作区目录没有被移除。
- 删除流程改为先正常停止当前 DSH，再精确删除会话目录、两个官方索引及空工作区目录，随后在原 WebView 内刷新 Harness；避免旧内存投影在退出时把垃圾索引写回来。
- 启动维护只识别并清除 VibeKits 旧验收生成的 `vibekits_harness_native_*` 临时工作区，不触碰真实用户工作区。
- 本机实际删除 15 个旧自测目录，遗留数为 0；剩余 2 个真实工作区和 2 条有效索引会话。6 项相关 Harness 回归通过，Windows Release 构建成功。
- dev.109 前台实测 APP 正常响应；DSH 从进程创建到回环地址就绪 3.934 秒。

# 2026-08-26 · v1.9.0-dev.110 Harness Codex 阅读密度与滚轮

- 右侧会话主容器增加 WebView2 滚轮转发：代码块、终端和工具结果可先独立滚动，到达边界后自然继续滚动整个对话；左侧工作区列表不受影响。
- 助手正文、用户气泡和输入框统一为 14 px/22 px，推理步骤、工具摘要与压缩上下文统一为 13 px/20 px，代码为 12.5 px/19 px，接近 Codex 的信息密度。
- 脚本使用幂等标记，页面加载后再次补注入，兼容 WebView2 导航时序；注入失败不会阻断 Harness 主功能。
- JavaScript 语法检查、UI 合同测试和 Windows Release 构建通过；dev.110 前台进程正常响应，本次 DSH 后端 3.963 秒就绪。

# 2026-08-26 · v1.9.0-dev.113 Harness 会话滚轮、字体与顶部操作闭环

- 纠正 dev.110/111 的无效滚轮验收：此前自动化落在没有滚动范围的“新会话”页；本次固定打开已有长会话，向右侧正文发送真实 Windows 鼠标滚轮，并以滚动前后窗口截图核对内容位置。
- WebView 导航完成后重新注入会话 UX；Flutter 原生指针信号和页面捕获阶段均提供滚轮转发，目标子控件无法定位时回退到当前可见会话主滚动容器。
- 助手正文和用户消息降至 12 px/18 px，标题按 16/15/13 px 分级，推理与工具摘要保持 12 px/18 px，缩小后的密度接近 Codex。
- 明确区分 VibeKits“远程分享”和官方 DSH“导出会话日志”：两个按钮分开排布并中文标注；前者通过中继网页查看/交互工作状态，后者只导出当前会话日志。
- 轻量 UI 合同测试 1/1 通过，Windows Release 构建成功；dev.113 真实滚轮前后截图 SHA-256 分别以 `DD524716`、`15E3F3B1` 开头，证明页面发生实际位移。

# 2026-08-26 · v1.9.0-dev.114 SSH 密码认证入口修复

- 修复远程连接页把用户输入的密码误当成 `identityFile` 校验，导致 SSH 请求尚未发出就报“私钥路径不是可访问的普通文件”的现场问题。
- SSH/SFTP/端口转发统一增加“密码 / 私钥”认证方式；默认密码，密码框支持粘贴、回车连接、隐藏/显示，并继续复用系统安全凭据。
- 只有选择“私钥”时才显示并校验私钥文件；密码只进入内存中的 `secret` 参数，不写入普通设置或日志，同一已认证会话继续直接复用于 SFTP。
- 专项界面测试确认密码字符串进入 `secret` 且 `identityFile == null`，1/1 通过；目标 `119.96.24.110:39281` 不带凭据 TCP 实测 53 ms 可达；Flutter Analyze 无问题，Windows Release 构建成功。

# 2026-08-26 · v1.9.0-dev.115 SFTP 双栏目录导航

- SFTP 本地与远端面板分别增加“后退到上一目录”和“返回上级目录”，两侧历史独立，不会相互影响。
- 进入子目录、手动输入路径、后退和上移都经过相同的真实目录刷新；本地磁盘根和远端 `/` 会自动禁用上移，失败导航不污染历史。
- SFTP 专项回归 4/4 通过，其中真实扫描本地临时目录，模拟远端 SFTP 列目录，双侧分别验证“进入→后退”和“进入→上一级”；Flutter Analyze 0 问题。

# 2026-08-26 · v1.9.0-dev.116 远程历史、自动凭据与多设备状态

- SSH/SFTP 首次成功认证后自动建立历史记录，主机、用户、端口、已确认的主机指纹和最近使用时间写入设置；密码只写入 Windows 凭据管理器/macOS 钥匙串。
- 下次从“最近会话”选择设备后可直接连接，无需再输入账号密码；主机指纹变化时仍必须重新确认。
- SSH 终端标签可同时保持多台设备，切换标签时主机资料与所选设备同步，输出不串线。
- `vibekits.remote.list_profiles` 现在同时返回持久化历史、最近使用时间、在线/离线、当前连接数和会话类型，不暴露凭据。
- 远程工作台回归 16/16、Harness 远程桥接 1/1 通过，Flutter Analyze 0 问题。

# 2026-08-26 · v1.9.0-dev.117 网络抓包与智能体闭环

- 新增“网络抓包（PCAP）”工作区：过滤预设、实时包列表、停止保存、另存为、已有 PCAP 读取和协议/端点统计。
- 合并 WinDivert 2.2.2 开源源码、LGPL 许可证和 x64 运行时；自研只读 helper 输出标准 LINKTYPE_RAW PCAP，不依赖 Npcap/Wireshark/PATH。
- 新增 `capture.status/start/stop/read/analyze` 5 个 Harness 接口，UI 与 Harness 共用任务状态并沿用可删除的工具调用日志。
- 本机管理员模式真抓 5 包/1,911 B，真实文件再由 Harness 读取；专项 3/3、开发工具 UI 11/11、静态检查 0 问题、Windows Release 构建通过。
- 普通权限驱动打开失败会在 4 秒内明确提示管理员运行，不再进入假启动或无限等待。

# 2026-08-27 · v1.9.0-dev.123 Android 清理、长连接、自迭代与 DSH 首启

- Android 清理显式使用平台返回的应用私有缓存路径，发现和删除采用同一边界；移动端空结果页增加居中扫描入口，扫描后可直接重新扫描和清理选中项，62 真机扫描完成且 APP 保持存活。
- ADB 与串口增加真实会话型接口：`session_open/status/read/write/close` 跨多次 Harness 调用保持同一连接，ADB 心跳执行真实 `get-state`，关闭桥接或 APP 时统一释放。
- Harness 增加 `project.iteration_inspect/project.build`：固定顺序执行工作区检查、Analyze、能力/桥接测试和目标 Release 构建；只生成候选产物，不自动覆盖、安装或发布自身。
- 自动能力目录更新为 161 个定义接口、139 个可执行接口、0 个公开 handler 缺失；`AGENTS.md` 的常用链路同步长连接和自迭代协议。
- 新增标准归档语料（ASCII、中文路径、空文件、0～255 二进制），ZIP/TAR/TAR.GZ/真实 7z/ZIP/RAR5 全部通过；修复部分 7-Zip 输出仅以 `Attributes = D` 标记目录时的提取计划错误。
- DSH 首启根因定位为官方 profile fallback 每次同步遍历约 3.2 万文件并逐包修复链接；改为单个只读 `node_modules` Junction，现场从 47.852 秒降至 5.094 秒，不再出现 30～50 秒假死。系统盘 0 B 可用也被确认会放大该问题，已仅清理 1.22 GiB Flutter/Pub 临时构建缓存。
- 定向回归 42/42 通过，Flutter Analyze 仅保留 1 条既有风格提示，Windows Release 构建通过。Android Debug 夹具构建因 Gradle 长时间无输出主动终止，不冒充真机删除实证。

# 2026-08-27 · v1.9.0-dev.124 Harness 官方 rc.2 与 100 次完整重启

- 固定运行时从官方 `@deepseek-ai/dsh@0.1.0-rc.7` 更新到 `0.1.1-rc.2`，补齐候选版必需 peer，使用 `--no-open`，运行阶段不执行 npm/npx 或联网安装。
- 将 portable Node 编译缓存前移到打包阶段；全新临时用户目录首次就绪 6.425 秒，消除现场 30～50 秒首启卡死，但未把当前 6～10 秒结果冒充为 3 秒达标。
- Windows Runner 在退出末端只回收当前 APP 的后代进程树；确定性 `--open-harness` 压测入口取代依赖焦点的快捷键，门禁同时检查 DSH、WebView2 和全部子进程。
- Release 完成 100/100 次“彻底退出→重新启动→HTTP 200→彻底退出”，残留进程 0；就绪中位数 6.869 秒、P95 8.005 秒、最大 9.698 秒。
- Harness/UI 回归 14/14 通过；Flutter Analyze 无新增错误，保留 1 条既有 GitHub 代理风格提示。验收见 `acceptance/V1_9_0_DEV124_HARNESS_RC2_100_RESTART_2026-08-27.md`。
- 最终重编产物追加 3/3 实启实退且零残留；构建后高负载现场为 12.088～12.610 秒，因此本版只承诺消除 30～50 秒卡死，不虚构稳定 3 秒或稳定低于 10 秒。

# 2026-08-27 · v1.9.0-dev.125 Android 63 APK 安装暴力测试

- 新增 Harness 本地聚合压力入口：由智能体单次发起，100 轮 ADB 安装、boot_id 判定和串口增量监听在本机执行，原始串口与异常 Logcat 不上传模型。
- 专用启动参数仅临时预批准明确的 ADB/串口接口；正常启动仍沿用逐次/会话权限，不写入永久完全访问。
- 设备 `192.168.3.63:5555` 实测 100/100 次覆盖安装成功，失败 0，boot_id 变化 0，未检测到系统重启；会话均已关闭。
- COM33（CH340，115200、8N1、RTS/CTS）全程打开但累计 0 B，串口“实际收到日志”子项明确未通过，未用 boot_id 结果冒充串口通过。
- Windows Release、Node 语法检查与定向 Analyze 通过；Android dev.125 构建受 Gradle 分发下载超时阻塞，压力段使用当天已有 Release APK并记录 SHA-256。
- 验收见 `acceptance/V1_9_0_DEV125_ANDROID63_APK_INSTALL_100_SERIAL_STRESS_2026-08-27.md`。

# 2026-08-27 · v1.9.0-dev.126 Harness 剪贴板修复

- 修复外层 Ctrl+V 已截获、但官方 DSH `contenteditable` 富文本输入框未被旧粘贴逻辑识别，导致快捷键无响应的问题。
- 粘贴同时支持普通 input/textarea 与官方富文本编辑器，新增 Ctrl+Shift+V、Shift+Insert；Ctrl+C 可复制输入框选区或会话正文选区。
- 增加仅显式测试参数启用的 WebView2 本机调试端口，以及可重复的物理输入验收脚本；正常启动不会开放调试端口。
- 剪贴板专项合同 1/1、WebView2 内核复制/粘贴 2/2、Windows 真实窗口鼠标点击与物理 Ctrl+V/Ctrl+C 2/2、定向 Analyze 和 Windows Release 构建通过。
- 验收见 `acceptance/V1_9_0_DEV126_HARNESS_CLIPBOARD_2026-08-27.md`。

# 2026-08-27 · v1.9.0-dev.127 Android 53 Harness ADB/串口联合压力闭环

- 修复压力测试预批准只到外部 MCP、未进入内嵌官方 Harness 工具桥的问题；授权集合现在贯穿 APP 到官方 Harness，并在会话重启后保留本次明确预批准，消除隐藏授权等待导致的 5 分钟 `fetch failed`。
- Android 压力 MCP 正式纳入运行时准备、Windows Bundle、Bundle 校验和 DSH MCP 注册；COM33 对齐 SecureCRT 的 115200/8N1/无流控配置。
- 真实 Harness 调用 `android__apk_install_stress_100`，向 `192.168.3.53:5555` 连续覆盖安装 APK 100 次：100 成功、0 失败、boot_id 变化 0、未检测到重启，ADB/串口长连接均成功关闭，测试后 APP 正常响应。
- COM33 全程成功打开并逐轮读取，但累计 0 B；串口接收子项明确未通过，不用 ADB 结果冒充串口日志。
- Windows Release、Bundle 校验、Node 语法和目标静态检查通过；验收见 `acceptance/V1_9_0_DEV127_ANDROID53_HARNESS_ADB_SERIAL_STRESS_2026-08-27.md`。

# 2026-08-28 · v1.9.0-dev.128 Android 53 串口与系统信息完整闭环

- 针对 dev.127 串口 0 B 未通过项，增加一次无副作用 Android 控制台 echo 回环；COM33 实收 79 B，包含回环标记和 `console:/ $`，确认端口、115200/8N1/无流控和收发线路均正确。
- 回环后 100 轮保持串口被动监听，同时在首轮和每 10 轮保存有界 Logcat；最终 11 份、256,361 B，串口读取失败 0。
- 修复测试权限集合遗漏 `serial.session_write` 导致回环隐藏等待授权的问题；正常启动权限不变。
- 官方 Harness 单次真实调用在 `192.168.3.53:5555` 完成 100/100 次 APK 安装，失败 0，boot_id 变化 0，无崩溃/看门狗/重启关键字；ADB 与串口会话均成功关闭，APP 仍正常响应。
- Windows Release、29 项 Bundle 校验和 Node 语法通过；验收见 `acceptance/V1_9_0_DEV128_ANDROID53_ADB_SERIAL_LOGCAT_100_2026-08-28.md`。

# 2026-08-28 · v1.9.0-dev.129 Android 53 全新安装、启动与串口并行监控

- 修正旧压力流程只执行 `adb install -r` 的验收偏差；每轮现在强制执行卸载旧包、无 `-r` 全新安装、`am start -W` 启动和 `pidof` 存活校验。
- COM33 串口由独立 worker 贯穿卸载、安装和启动，三个阶段分别排空增量；发现 boot_id 变化或串口重启标记会立即保存串口尾部、2000 行 Logcat、`ps -A -T` 最后线程及 Activity/Window/Package/pstore/dmesg 后停止。
- 官方 Harness 在 `192.168.3.53:5555` 真实调用一次聚合工具完成 100 轮：卸载 100/100、全新安装 100/100、启动 100/100、PID 100/100 且 100 个 PID 均不同。
- 总耗时 21 分 9 秒，单轮 10.645～16.102 秒；boot_id 唯一值 1、串口读取失败 0、11 份 Logcat 共 268,068 B，无重启、Watchdog、Fatal signal、VibeKits ANR。
- Node 语法、Windows Release 和 29 项 Bundle 门禁通过；验收见 `acceptance/V1_9_0_DEV129_ANDROID53_FRESH_INSTALL_LAUNCH_SERIAL_100_2026-08-28.md`。

# 2026-08-28 · v1.9.0-dev.131 Harness 全工具自动配置与串口技能

- 新增 `vibekits.serial.auto_detect`：不发送未知协议字节，自动选择 USB 串口并分阶段探测常用波特率、数据位、停止位、奇偶校验和 8 种流控组合；返回逐次尝试、字节样本、评分、置信度和可直接传给长连接的 `selected`。
- `serial.transact` 补齐完整帧格式和流控参数，不再只接受波特率；短任务与长连接使用同一配置语义。
- 新增 `vibekits.system.describe_tool`，运行时精确返回任意公开工具的类型、必填项、默认值、枚举、范围、风险及自动配置原则。163 个定义接口、143 个当前可执行接口均可查询，逐项合同测试通过。
- 新增 `assets/harness/SERIAL_SKILL.md` 并注入官方 DSH `AGENTS.md`：可枚举、可检查、可复用或可安全试探的配置由 Harness 自动完成；只在缺少账号/身份、密码/API Key/Token/私钥口令、业务目标或破坏性确认时询问用户。
- 模拟串口验证自动选出 115200、8-N-1、RTS/CTS 且发送次数为 0；Harness 工具桥与能力目录回归全部通过，Windows Release 构建通过。
- 最终真实 Harness 会话调用 `capability_check` 和 4 次 `describe_tool`，精确列出串口参数并明确不要求用户选择串口、波特率、帧格式或流控。验收见 `acceptance/V1_9_0_DEV131_HARNESS_AUTOCONFIG_SERIAL_SKILL_2026-08-28.md`。

# 2026-08-28 · v1.9.0-dev.132 Harness 调试空间与可追溯诊断

- 清理扫描新增 Harness 调试目录独立汇总：显示配置路径、logs/screenshots/temp 分项占用、总文件数、总占用和 24 小时前可释放容量。
- Harness 调试证据即使在系统盘低于 5% 或 10 GiB 恢复计划中也不再自动勾选；默认只推荐展示，由用户统一选择可清理项。
- `harness-work.jsonl` 持久化记录启动、就绪、等待授权、工具执行、成功和失败阶段；`harness-web-*.log` 继续保存脱敏 stdout/stderr，工具审计继续记录目标、参数摘要、结果与耗时。
- 官方 Harness 界面新增独立“运行日志”入口，可刷新、选择文件并滚动查看有界尾部；新增 `vibekits.harness.diagnostics` 只读接口，供 Harness 自查最近运行日志和工具调用记录。
- 目录容量、24 小时保留策略、低磁盘不自动选择、日志可读取与敏感字段脱敏等针对性回归 34/34 通过；本次改动文件静态检查无问题。
- Windows Release 构建与 29 项随包运行时检查通过；最新 APP 实启后 9.2 秒进入 Harness ready，`harness-work.jsonl` 真实写入 starting/ready，现场调试目录识别 469 个日志文件、47,226 B 总量、24,214 B 达到可清理保留期。
