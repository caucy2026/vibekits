# 新会话整理交互：本机验证记录

## 目标

点击“新会话”先创建并进入空白会话，在该会话正文显示旋转动画和整理阶段，完成后显示来源卡片并继续开发。失败在会话内重试，复用已创建会话。菜单统一图标和文字对齐，仅混入少量暖黄色。

## 本轮修正

- 不再直接删除官方 React 菜单节点，改由 Escape 事件走官方关闭逻辑。
- 官方新建入口是仅有图标的按钮，识别其 aria-label/title，而非只检查 textContent。
- 已有空会话被官方侧栏隐藏；后台创建成功不能代表界面已成功选中，必须单独验收。

## 已验证

- 21 项会话适配器、来源解析、创建协调器和界面契约测试通过。
- 新增可执行按钮回归测试：aria-label、title、文字三种按钮分别只激活一次；官方菜单节点不被直接删除。
- 真实 Harness 后台端到端测试通过：新会话 session-c2ad665c-a161-4a3d-9aa4-d4fa99e83340 聊天时间线为空，摘要与来源关系持久化一致。
- macOS Release 编译成功，版本 1.9.0.223（2223）。
- 修改前候选程序重启后返回来源会话，判定交互验收失败，不能用后台成功日志替代。

## 早期阻塞记录（已在下方复测解决）

最新构建重新启动后，界面自动化连续返回 AXError.cannotComplete；重置连接和重启候选程序后仍未恢复。进程采样显示主线程在事件循环内，不能据此断言界面正常或程序卡死。

因此最新构建的真实点击、整理动画、准确选中新会话、重启恢复仍待完成。当前不得交付为“验收通过”，不得发布。

## 后续定位与永久修复

- AXError 根因已验证：同一 App 包 MacOS 目录的旧中继助手也被识别为 VibeKits。官方连接查询返回 idle、connections=[] 后，仅停止该旧候选助手，窗口读取立即恢复。
- 中继改为 Contents/Helpers 下独立后台 App，独立 bundle ID 与 LSBackgroundOnly；主程序优先定位包内新路径，签名脚本包含嵌套助手。
- 真实右键点击已进入空白新会话并显示“正在读取来源会话”，菜单正常关闭。
- 不再依赖隐藏的空白侧栏条目或同名标题选会话，使用官方持久化选中状态的真实 sessionId 恢复。
- 修正官方事件信封解析；旧测试误用裸事件，真实摘要只有空模板。回归测试现要求摘要包含真实来源正文。
- 新增 VibeKits 自有 context provider，通过官方 systemPrompt.context 扩展向对应会话提供有界交接摘要，保留官方 Harness 源码和原有提示。
- 实际官方 SystemPrompt 组装测试通过：指定会话获得摘要，无关会话不获得摘要，失去工作区成员关系时停止提供摘要。
- 页面增强初始化幂等，避免重复安装的旧观察器移除新来源卡片。

此处为修复阶段记录；最终候选实际结果见文末。

## 23:06–23:20 实际候选复测

- 右键“新会话”实际创建 `session-a55bae3f-48a4-460b-ab60-4370fb26f313`，来源为 `session-b882117e-3c6f-4a49-83df-5eb616ec57d4`；新页面空白、来源卡片可见，没有复制聊天时间线。
- 首次真实模型验收失败：“没有交接内容”。定位到持久化工作区键为 `/<workspace-id>/`，插件按裸 ID 查找不匹配。已支持实际存储格式，并加入回归。
- 修复后重新打包、退出重启，模型在只读验收中确认收到来源关系和压缩上下文，能够引用来源最后的 MC/MCP 需求，并纠正上一轮无上下文的结论。真实存储读取为 11547 字符，包含正确来源 ID。
- 重启恢复到原继续会话；来源卡片在对话顶部可见。
- 进程核验：一个 Vibekits 主程序，独立 Helpers 后台助手；未产生第二个同身份主程序。
- 继续回归发现右键“打开继续会话”直接刷新页面时，界面采集返回空窗口，两次复现；主程序存活且无新崩溃报告。已改为先派发 Escape 关闭菜单，再延后导航。对应可执行回归通过，重编译实测待完成。


## 最终本机结果（23:23）

本轮修复验收 PASS；此结论仅覆盖本次新会话与窗口控制故障，不代表历史全部需求、远程并发 100 轮或正式发布已完成。

- 最终 Release 再编译成功（1.9.0.223 / 2223）。34 项 Dart 回归全部通过，Node 菜单执行顺序与官方上下文组装测试通过，diff 检查通过。
- 修复后的“打开继续会话”实际点击成功，窗口状态与截图均正常，不再复现菜单刷新导致的空窗口采集。
- 从来源会话再次实际创建全新会话 `session-e436a31e-022f-4a18-90f2-05667e706066`，空白聊天界面有来源卡片。
- 仅输入“只读验收，不调用工具：用两句话回答交接资料中的来源会话 ID、最后一个用户需求。”模型在首轮直接回答正确来源 `session-b882117e-3c6f-4a49-83df-5eb616ec57d4`，并引用原会话最后的 MC/MCP 能力需求；没有依赖复制历史聊天。
- 点击来源卡片，实际返回原“多模态能力问答”及其 19:19 聊天记录，选中状态正确。
- 候选程序位置：`/Volumes/ORICO/kemi-build-cache/app-release-gate/vibekits/macos/dev223-remote-harness-final/build/macos/Build/Products/Release/Vibekits.app`。
- 尚未执行正式 Developer ID 签名、公证、商城发布；本次没有将候选覆盖商城正式包。
