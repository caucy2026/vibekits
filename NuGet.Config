<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <config>
    <!-- Keep Windows build dependencies with the D: workspace. -->
    <add key="globalPackagesFolder" value=".cache\nuget" />
  </config>
</configuration>
